# 진입 신호·가격 행동·시장 미시구조

- 초기 H1 상대충격, M30 범위압축, 단일·교차지수 상태 연구가 공유 포트폴리오의 기초가 됐다.
- 이후 tick/M1/M5/H1 구조, 추세·반전·돌파·동조·비가역성 등 폭넓은 진입 연구가 진행됐지만 Axis A A01~A73에서는 독립 완료조건을 만족한 생존자가 없었다.
- 현재 실행체의 여섯 진입 스트림인 RC16, RC4, Cross, Pressure, Return, Passive는 V6R6과 V7 구조 이전 동안 기회시각·방향·주문형태까지 동결한다.
- 새로운 진입 가설은 B75 및 Next 이주가 끝난 뒤 별도 단일 흐름에서만 재개한다.

## Frontier Unit 099 — exact legacy market-state router eligibility

- 2026-08-29 whole-map comparison selected one materially distinct fixed structure after the uniform signal-floor and global stop-headroom Optimization axes both lost profit and worsened drawdown. The source-free Program 1 / meso→macro family is `lab/research/legacy-market-state-router-optimization-eligibility-v1/`.
- Raw blobs at legacy commit `4c0899255c701e2c6b53e7f44457c431aef2ad76` reproduce the lineage-pinned router summary (`10,778` bytes / `95E899...9180`), source (`35,041` / `C5C964...F05A`) and immutable component kernel (`35,844` / `7CC33B...75A2`). The exact identity is pooled expanding Ridge alpha `10`, quarterly refit, past-label 2nd/98th clipping, ten fixed entry-known features and component-relative predicted-net rank `0.25`; no nearby model or policy tuning survives.
- Already-consumed native evidence is `+$556.08 / +$437.3044` actual/stressed at `16.50%` equity DD for the binding long path and `+$13.58 / +$11.5235` at `11.04%` for the reported recent economic window. Its legacy decision remains `VALIDATED_NUMERIC_PASS_ALTERNATIVE_NOT_PROMOTED`; these numbers identify the candidate but do not price the current anchor.
- The current selection candidate ledger contains `12,265` rows, including `7,352` `EXISTING_EXPOSURE` rows where the native signal is deliberately unevaluated. A router-blocked entry changes later occupancy and exposes those hidden decisions, recursively changing the virtual samples, quarterly model, ranks, ordinary admission and account path. Source-free subtraction is therefore structurally incomplete; this is a native-path requirement, not an economic failure.
- Unit 099 closes without Lab MQL or economics and retains exactly one fixed Optimization information seed: the exact router as a binary pre-admission layer on the five active `2 / 1.5 / 2 / 2.5 / 1.5` paired-month anchor components, with independent fixed-hold virtual books and every current execution/risk rule otherwise unchanged. One self-contained native Optimization campaign may open after the Unit commit; no router grid, alternative legacy candidate, component exception, threshold/window rescue or Live authority exists. Marker `FRONTIER_UNIT_099_CLOSED_RETAIN_EXACT_MARKET_STATE_ROUTER_NATIVE_OPTIMIZATION_SEED`.
