# Project Zeta Terminus Next Operating Direction

## Objective and continuity

Develop separately attributable economic capabilities for FPMarkets `US100`, `US30`, and `US500`, leverage `1:100`, and reference capital `$100`, including a genuinely high-turnover entry executable or shared-account portfolio and dynamic management over frozen verified entries. Next inherits the economic evidence boundaries from Project Zeta Terminus; document volume and experiment count are not performance. Legacy `Axis A/B/C` labels are provenance tags only. New Next work uses descriptive economic-family names.

The active migration is not a new research search. `NEXT-E01/V7` is a behavior-preserving engineering successor to B70 V6R6. Its parent entry opportunities, first-fill timing and direction, orders, sizing, costs, protection, RC4 management, and portfolio risk are frozen.

## Continuous Live-Dev optimization

The prior active user Goal was continuous economic optimization of the existing frozen Live-Dev behavior in the independent root `optimization/`. Exact-V8 Units U001-U004 and the mandatory whole-map comparison closed that currently ready internal frontier at `STATE-0499`. The user's later 2026-08-31 direction explicitly supersedes Optimization-only work with the independent V8 Challenge program below. The rules in this section remain frozen ancestry and govern again only if the user later reopens Live-derived Optimization.

- The optimization parent is a byte-pinned one-time copy of the exact active Live package, kept under `optimization/baseline/`. It is never executed with Live identity. Every active optimization campaign owns a self-contained root under `optimization/campaigns/<family>/`, with separate release/execution/Portfolio/Magic, state, output, configuration, evidence and dedicated physical Portable runtime.
- No optimization compile, test or optimization run may use the Master terminal, `live-dev/runtime/portable/`, a Lab Portable, or any state/log path owned by Live or Lab. The Master Live PID and dashboard remain outside the optimization boundary.
- Optimization methods and search algorithms are not otherwise restricted. Only a complete valid economic result may produce `ECONOMIC_IMPROVEMENT`, `NO_ECONOMIC_IMPROVEMENT` or another economic comparison. Missing environment, invalid history, invocation failure, parser/serialization fault, design defect or engineering defect is `CORRECTION_REQUIRED`, never an optimization success or failure.
- Optimization is not a mutation of the Live EA. After an exact Live-derived control is retained in each comparison, an isolated campaign may radically change or remove strategies, rebuild the portfolio, alter coordination, or run destructive economic experiments. Such candidates keep separate source and identity, remain inside `optimization/`, and are not Lab research or Live changes.
- Pure environment, design and engineering corrections have no numeric retry cap. They are recorded and rerun until the declared economic comparison is valid. If a design correction changes the economic search contract after any result was seen, version the campaign and continue; do not relabel the invalid predecessor as an economic failure.
- Keep the latest two completed months isolated from parameter selection by default. Search output is optimization information, not promotion authority; confirm selected candidates on the isolated latest period and compare whole-portfolio net, stressed net, drawdown, efficiency, turnover and component breadth before freezing an economic conclusion.
- The user's prospective risk preference is maximum actual and doubled-cost-stressed profit around a nominal `20%` MT5 equity-drawdown line. Use fast economic proxies to explore broadly and reduce the set before expensive MT5 real-tick work; proxy output remains selection information and cannot itself prove profit, MT5 drawdown or promotion readiness.
- Numeric decision lines are not automatic cliffs. The user's explicit 2026-08-29 judgment direction requires proportionate economic judgment for marginal misses: an overshoot materially no larger than the user's example of a nominal `<10%` line producing `10.6%..11.5%` may be accepted when complete profit, stressed-profit, temporal stability and holdout evidence support it. Record the nominal gate and the pragmatic effective gate separately, disclose the exact miss, and explain the judgment. This does not automatically waive a materially large miss or permit hiding adverse numbers.
- Maintain one optimization judgment stream. Subagents may gather bounded implementation or information inputs, but may not create a parallel candidate stream and may not be used to build or review validators, test harnesses, rule checkers or similar review machinery.
- A new clue may temporarily open one serial Lab Unit, allocated under the normal Program rules. The optimization campaign pauses while that Unit is active, resumes after the Unit closes, and remains the Goal. A Lab detour is optional and never becomes the continuing primary lane.
- Commit and push `main` at comparatively large phase boundaries such as lane/baseline freeze, a complete economic search stage, or a frozen successor campaign—not for every parser, environment or invocation correction.

### Exact-V8 long-horizon rehabilitation program

The user's 2026-08-31 direction opens a scoped long-horizon deep-optimization program for exact V8 and makes adjacent follow-up plus fine adjustment mandatory inside that program. V8 is its own economic parent, distinct from V7. This scope supersedes the general no-adjacent-successor rule only for serial exact-V8 Optimization Units; it does not reopen a closed Frontier Lab unit, authorize parallel work, rewrite a historical verdict or create Live authority.

- During this program, economic selection and tuning may use only the exact frozen V8 package, clean exact-V8 ledgers/results, naturally accruing V8 Live research logs and outputs generated prospectively by the active V8 Optimization Unit. V7 and historical Lab economics may establish lineage or identity separation only; they may not seed a parameter, rank a Unit, rescue a result or supply candidate economics.
- Optimization is not limited to exposed parameters. A V8 Unit may change source code and causal mechanisms, remove or disable an existing strategy, reconstruct membership from existing V8 components, and change admission, collision, priority, sizing, risk, hold, protection or exit behavior, provided the exact V8 control and the changed mechanism remain attributable and isolated. The agent selects these axes autonomously from V8 evidence rather than waiting for the user to enumerate each variant.
- A novel entry strategy remains Lab research and is prohibited while this Optimization-only direction is active. Do not add a symbol, timeframe, signal family, direction or opportunity timestamp that exact V8 would not emit; do not loosen or rewrite an entry condition to manufacture new opportunities. Suppressing, removing, reordering or resizing existing V8 opportunities is Optimization. If a diagnostic implies new-entry alpha, record it only as out-of-scope and do not open or implement it.
- Open exactly one newly named V8 Optimization Unit at a time. A Unit freezes one mechanism and its ordered deep stages before outcomes. It is not closed because one grid, threshold or candidate fails: broad mapping, adjacent basin search, finer local adjustment, causal state-dependent variants and temporal/cost stress must be exhausted as declared. Only when the full Unit has no robust actionable region may it close and the next V8-native Unit be chosen from new V8 diagnostics.
- Broad screening, adjacency checks and fine grids are proxy work; they must preserve V8 source hashes, disclose accepted-path and selection limitations, prefer stable plateaus over isolated maxima and may not claim native MT5 equity drawdown or newly admitted opportunities that the source path did not observe.
- Expensive MT5 work is scarce by default. One Unit may consume at most two valid economic paths: one exact contemporaneous V8-derived control and one unchanged proxy finalist. Necessary environment, invocation or engineering corrections are not economic paths and have no arbitrary retry cap, but they must be genuinely required and may not be used to expand or retune the economic lattice after outcomes.
- A proxy nonconfirmation does not trigger MT5. A proxy survivor authorizes at most the frozen control/finalist pair; it does not promote, mutate or restart Live. Any later release change still requires a separately named user-authorized Lab engineering handoff and a fresh `CURRENT_STATE.md` Live decision.
- No separate Lab economic-research or engineering Unit opens while this exact-V8 deep-optimization program is active. Optimization remains the sole development lane unless the user explicitly changes that direction; the generic optional Lab-detour rule above is suspended for this scoped program.

### Independent V8 Challenge program

The user's later 2026-08-31 Goal makes exact release `NEXT-E02-V8-PMLR1-b1c77d3b6356` the economic Challenge benchmark and authorizes serial development of a genuinely independent automated strategy until one valid complete candidate beats it. This is the explicit scope change anticipated by `STATE-0499`: the exact-V8 Optimization-only program remains closed ancestry, new-entry Lab research is open, and Lab is the sole active development lane. Live V8 remains an isolated running benchmark and receives no source, package, settings, state, process or authority change from Challenge work.

- Every Challenge candidate must use either `Python adapter + EA` or `ONNX + EA`. A single EA that internally owns both novel signal generation and execution is not a valid Challenge candidate. For a Python architecture, Python owns causal feature/model/decision production while the EA owns broker validation, risk, orders, protection, close, persistence and evidence. For an ONNX architecture, a Python-produced frozen ONNX model owns the learned decision function while the EA owns causal feature materialization, inference invocation and the same execution duties.
- Each architecture owns one self-contained root under `lab/research/<family>/`, a distinct execution/Portfolio/Magic/state/output identity, and a dedicated Git-ignored Lab Portable only if native work opens. It may not include, link, execute or write `live-dev/`, `optimization/`, `lab/mt5/` or another family. The frozen forward Lab engineering baseline remains immutable and supplies at most a one-time copied safety/execution shell after a proxy survivor exists.
- Candidate conception is first-principles and independent of V1-V8 signals, opportunities and component economics. Repository price observations, broker contracts and engineering interfaces may be used as attributable inputs; V8 results are used only as the final benchmark. Because prior results are already known, every family records known prior exposure and freezes its model, features, periods, costs, variants and stop rules before candidate outcomes rather than claiming epistemic blindness.
- Maintain exactly one Challenge family and one judgment stream. Finish its predeclared architecture bundle through causal fit, development, locked confirmation and conditional native stages. A valid adverse family closes without nearby outcome-driven repair; then recompare the available macro Programs and open one materially distinct successor. The continuing Goal does not stop merely because one family fails, and it ends only when complete native evidence verifies every V8 Challenge requirement.
- Use fast Python proxy work for broad architecture selection. Proxy evidence cannot prove a V8 victory. Only one unchanged proxy survivor may receive the complete paired EA and a scarce native candidate path. Environment, history, compilation, invocation, serialization and engineering defects remain correction states and do not consume or economically fail the candidate.
- The initial exact Challenge interval is `2024-01-01` through `2026-08-01` exclusive at reference capital `$100`, leverage `1:100`, FPMarkets US100/US30/US500 and doubled-cost stress. The valid V8 benchmark is actual/stressed `+$409.81 / +$367.818`, native relative equity DD `37.39%` and robust recovery `3.295860215`. A victory must exceed both profits, keep DD at or below V8, improve recovery, keep every declared calendar epoch actual/stressed positive and satisfy the declared turnover contract in one complete Python/ONNX-plus-EA native result.
- Keep selection and confirmation periods explicit. The first family uses 2024-2025 for development and locks 2026 January-July for at most one unchanged survivor. Later families must declare fresh or otherwise independently defensible confirmation authority before outcomes; they may not recycle an opened holdout under a new name.
- Challenge work grants no Live promotion. Any verified winner still requires a separately named Lab engineering handoff, a new frozen Live release, explicit user authorization and the full stopped-flat entries-disabled operational sequence before it could replace V8.

### 2026-09-06 user-authorized V7 return verification and conditional deployment

The user explicitly requested verification of exact final V7-RLO1 economics and, if satisfactory, replacement of V8 plus starting the EA and Korean dashboard. This opens one serial Lab engineering family `lab/engineering/v7-rlo1-return-requalification-v1/`; the independent Challenge remains paused. This is not a new Challenge candidate or an Optimization search.

- This scoped handoff may make one attributable physical copy of frozen V7-RLO1 at `optimization/baseline/NEXT-E01-V7-RLO1-b32e7e176f2e/` and exact V8 at `live-dev/package/active/` into separate candidate/control roots inside the family. It may copy validated PCR2 read-only reason/contract telemetry as instrumentation. No cross-root or cross-lane source include, link or execution remains after derivation. The CXR2 forward baseline is unchanged.
- Candidate economics are exact V7-RLO1: six components, original sizing/exits, position/aggregate risk `0.04/0.12`, no V8 multipliers. Control is exact V8 `2/1.5/2/2.5/1.5/0` at `0.04/0.18`. No outcome-driven tuning is authorized. New execution/Portfolio/Magic/state identities preserve retired identities.
- Run adjacent V8 then V7 on complete valid all-symbol real ticks over `2024-01-01..2026-08-01`, `$100`, `1:100`; a supportive long pair permits fixed recent `2026-07-01..2026-09-01` comparison. July is already observed: recent evidence is execution/cost confirmation, not pristine holdout. Pinned clean market history may be copied physically once into the dedicated Lab Portable, without sharing source/settings/state/output/runtime. Fingerprint drift invalidates the affected whole pair; necessary engineering corrections do not consume an economic path.
- Before outcomes, the primary return standard is positive actual/stressed long profit, at least 75% retention of both contemporaneous V8 profits, native relative equity DD at most nominal 20% (only a disclosed marginal overshoot up to 21.2% may receive existing proportionate judgment), at least 10 percentage points lower DD than V8, improved robust recovery, and positive actual/stressed calendar years. Recent evidence must retain positive pooled actual/stressed profit without material risk or execution deterioration; disclose each month, activity, breadth and exact comparisons. These are return standards, not the independent Challenge victory gates.
- A complete passing economic and engineering handoff conditionally authorizes one new frozen V7-economics Live release and its EA/dashboard start without another approval question. First prove V8 ownership and a stopped-flat boundary, preserve V8 and retired V7 state/logs, carry only attributable realized P/L, record exact new authorization in CURRENT_STATE, and complete entries-disabled create/recovery plus unchanged actual-tick, synchronization, sole-owner, fault and final `0/0 -> 1/1` gates. Closed markets cannot waive those gates. V8 remains the sole existing order identity until that boundary; no retired V7 executable/state restarts.

Implementation boundary recorded on 2026-09-07: exact verification, installation and entries-disabled EA/dashboard startup are complete. The assistant and the same-task heartbeat may perform read-only readiness checks and notification only. Any final trading activation must be performed directly by the user; the assistant must not enable entries/Algo Trading, execute Start-ZetaNextV7RLive, change new-entry authority to ENABLED or execute trades. This clarifies the remaining action without reopening the closed verification family or asking the user for permission again. CURRENT_STATE keeps actual new-entry authority DISABLED.

The user's subsequent request to handle the cumbersome manual steps authorizes a small direct-user front end under `live-dev/tools/`, with a root CMD entrypoint. Its lifecycle is operator preparation and user-operated handoff, not a Lab research family or an EA/package patch. The assistant may author, statically compile and commit this front end, but must not execute the activation entrypoint. Only the direct user's explicitly labelled button may change the operating authority record and invoke the existing unchanged flat-stop and detached Master sequence. No source, EX5, SET, risk, recovery, market or ownership gate changes; no new release or concurrent research stream is created. Cancel is inert, unrelated Git changes are excluded, and uncertain activation is never reported as OFF or handled by forcibly closing an owner.

The reported 09:15 KST user activation stopped at the unchanged US30 market gate before Live. Its correction belongs to these operator tools: retain attributable per-launch completion, check readiness before interrupting an existing OFF runtime, and return a proved completed normally stopped/flat failure to DISABLED authority plus entries-disabled EA/dashboard. Do not infer stopped ownership from a timeout or absent result, relax the 3-second gate, force-stop an owner, or automatically retry Live. The assistant may establish and restore this proved OFF boundary, but still cannot activate trading.

The user's later 2026-09-07 instruction, after explicitly requesting the former leave-it-on `1/1` workflow, authorizes **arm-and-wait startup for this already-installed exact V7R identity**. This prospectively supersedes the startup-market restrictions in the initial front-end and failure-correction paragraphs above. Current US30 update frequency, the 12-second/three-update/three-second-gap observation, global current-bar synchronization and the diagnostic's protected deployment-handoff windows are not prerequisites for ordinary user activation or same-release startup. Remove that diagnostic from both the pre-stop button path and the fresh-preflight Live starter; do not add a bypass switch, a market watcher or delayed automatic activation. The diagnostic and its historical metrics remain available as observations, not permission to start or trade.

The user-operated path still requires exact committed release/account binding, sole ownership, a normally stopped flat boundary, fresh healthy `0/0` recovery and an exact healthy `1/1` permission handshake. `1/1` means entry permission: the frozen EA waits for ticks and evaluates its existing strategy times, signal/data checks, maximum entry delay, executable quote age `3.0` seconds, broker sessions, persistence, protection and risk admission. A quiet/closed market alone does not reject startup; disconnected, mismatched or unsafe operation still does. Do not replay expired or consumed opportunities or change any EA/Include/EX5/base SET/risk byte. A runtime start during an existing decision window follows those unchanged rules and does not guarantee a fill. This is Live operator preparation, not a new Lab family or economic release. The assistant may implement, parse, review, record and commit this change while preserving current `DISABLED` new-entry authority; actual activation remains the direct user's button action.

### 2026-09-07 autonomous research resumption and storage reserve

The user explicitly edited the active Goal to resume autonomous development/research until a later pause and to keep at least 30 GB free. This supersedes the earlier Challenge pause prospectively; all historical closures and the frozen V7R Live package remain intact. Continue the existing independent Python-adapter/ONNX-plus-EA V8 Challenge, one serial research judgment stream at a time, unless the user changes its benchmark or architecture. The now-closed V7 return family does not become a candidate parent.

First close the bounded Program 7 `lab/research/broker-clock-authority-audit-v1/` evidence study of concrete clock-conversion concerns in Families 006-010. It may inspect source and development timestamps without rerunning prices or economics, and may append an attributable authority correction while preserving old files. It may not silently rescue a closed candidate or select a favorable clock from profits. Recompare Programs 1-5 and 7 after that audit before opening exactly one materially distinct or explicitly justified engineering-correction successor.

Keep the stricter existing 30 GiB reserve as the operational implementation of the user's minimum 30 GB. Before acquisitions, runtime copies and result-producing runs, account for their bounded output/log growth; check again during long runs. If capacity threatens the reserve, close or defer only the storage-growing research step and use the existing safe retention policy. No Live files, canonical evidence or unique inputs may be removed to meet it. This session starts with approximately 78.45 GiB free and needs no deletion.

The V7R read-only market/operating follow-up completed at 07:02 KST on 2026-09-07 and its same-task heartbeat is paused. Keep the verified EA/dashboard frozen in 0/0; final trading activation is a direct user step under the implementation boundary above. No repeated broker watcher or assistant trading activation follows from research resumption, and Live gates remain unchanged.

Later explicit user cancellation on 2026-09-07 permanently closes `lab/research/frbsf-news-sentiment-publication-readiness-v1/`. Do not resume/reopen that source question, complete its pending readiness judgment, acquire its values or derive successor work from it after an ordinary Goal resumption. Preserve existing records as cancelled history without inventing a source or economic result. The overall research Goal remains paused; this cancellation does not open a macro comparison or replacement study.

### 2026-09-07 V7 ONNX and online-learning development

The user's latest session direction replaces the independent V8 Challenge as the active development objective with continued development of the existing V7 using ONNX, online learning and existing EA/MQH information. Work continues serially until the user pauses it. The independent Challenge remains paused, every closed family stays immutable, and the cancelled FRBSF study never resumes. This scoped direction governs ahead of earlier V8-only parent, independent-entry and external-source research instructions.

- Reopen only the isolated Optimization lane. Freeze one attributable physical copy of exact installed V7R `NEXT-E03-V7R-RLO1-0bba2ca045fe` into `optimization/baseline/NEXT-E03-V7R-RLO1-0bba2ca045fe/`. Its existing original-V7 economics are the control. The first single campaign is `optimization/campaigns/v7-onnx-online-compounding-v1/`; subsequent campaigns require a new prospective mechanism declaration and serial closure of the preceding bundle.
- Use existing V7 signals, causal EA/MQH features and completed native V7 research ledgers. Exact completed V7 return-verification evidence and native price/contract observations may be copied once as attributed inputs; no Lab or Live source/runtime is executed or linked. Do not acquire external economic indexes, news, announcements, event datasets or alternative external signals. Official software documentation may establish implementation semantics only.
- Preserve all of `live-dev/`: source, package, operators, settings, state, logs, runtime processes and current user-operated authority. Research grants no trading activation, broker context polling or promotion authority. Every research EA is tester-only with its own identity and mutable namespace, and runs only in its own physical Optimization Portable.
- Optimize actual and doubled-cost-stressed compound growth, measured from complete equity paths, terminal wealth, geometric return, drawdown, recovery and calendar-period stability. A hockey-stick shape is an aspiration, never a fabricated or guaranteed result. Do not judge a changed sizing/stop policy by multiplying historical dollar profits alone.
- DD should improve, remain comparable, or weaken only slightly. For the first prospective campaign use native DD no greater than the adjacent exact-V7 control as nominal, with a disclosed effective ceiling of the smaller of control DD + 1.5 percentage points and control DD * 1.10. Any use of that tolerance requires better actual/stressed growth, recovery and temporal evidence; it is not an automatic waiver. Historical exact-V7 long DD is 19.502399843%, not a substitute for an adjacent control.
- Freeze model inputs, fit/development/confirmation periods, online update timing, sizing and the complete related role bundle before model or candidate outcomes. Online updates consume only completed labels available strictly before a new decision. No future label, full-path normalization or unacknowledged reopened holdout may influence earlier decisions. Proxy output is selection information; only complete adjacent same-contract all-symbol native real-tick runs can confirm improved economics.
- Retain the 30 GiB free-space floor and budget input/runtime/log growth before each producing step. Reclaim only provably unowned, ignored and unreferenced duplicate caches/staging according to the retention policy; preserve canonical evidence and every Live file. One root agent owns design and economic judgment. Bounded factual implementation assistance is allowed, but no delegated candidate stream or test/checker infrastructure.

After the first three offline bundles closed without a survivor, the same user-authorized implementation scope permits one serial native learning-engine campaign, `optimization/campaigns/v7-native-onnx-online-learning-v1/`. This implements actual ONNX inference and own completed-label online weight updates inside a tester-only physical V7 copy, initially as an observational learner with exact V7 trading behavior. It is a normal EA/model implementation and bounded real-tick input/economic comparison, not deployment, a failed-policy rescue, a test harness or a new broker-safety research unit. Its own prospective declaration freezes learning, periods and logs before running. One adjacent exact-control/learner pair can establish native operation and economic non-interference; it cannot claim ML profit improvement. No failed model, risk/exit allocation, Live source/state/process or closed-family executable is imported. Subsequent economic use of learned decisions still needs a fresh prospective contract inside the sole active stream.

### 2026-09-07 authorized V7 training-history exception

The user explicitly accepts the pending training-only99%real-tick proposal and reiterates that development must improve the existing V7 through completion, rather than pursue an independent strategy. For only `optimization/campaigns/v7-native-onnx-causal-admission-v1/`, the complete observed2024control and all eight fixed full2024training trajectories may use disclosed99%real-tick history. Preserve the original100%-criterion failures, source limitations and every fixed seed/model/causal/readiness/input condition. Final full2025selection and full2025-August2026confirmation still require100%real ticks and every original economic/DD/recovery/epoch gate. The authorization is recorded in `optimization/campaigns/v7-native-onnx-causal-admission-v1/evidence/TRAINING_HISTORY_USER_AUTHORIZATION_V1.json`. Isolation exists to protect Live while improving its frozen V7 economic parent; it grants no Live change, independent-strategy scope or closed-family reopening. Continue the single existing V7 improvement stream without requesting this permission again.

### 2026-09-08 authorized V7 native log-allocation completion

The user explicitly approved the pending two-model native retest proposal with `ㄱㄱ`. This is a bounded exception to the closed-family no-seed/no-adjacent limitation for only `optimization/campaigns/v7-native-log-allocation-completion-v1/`. It permits one copy of the exact336-byte ONNX graph and411-byte initialparameters named in `optimization/evidence/V7_LOG_ALLOCATION_NATIVE_USER_AUTHORIZATION_V1.json`, and a fresh originalV7-derived self-contained EA implementation. Preserve the old family, original proxy failures and every economic/history gate. Run the complete ordered full2025 control/static/control/online100%-real-tick bundle; only one fully eligible unchanged finalist may receive the existing continuous2025-August2026 confirmation pair after its complete remaining storage growth is funded. The prospective selection storage amendment funds allfour2025 roles without preloading unexecuted2024/2026 tick caches; required past bar warmup and every existing original file remain preserved. No old EA/Include/online state/Python execution, other closedmodel/pyramid exception,99%-history waiver, independent strategy, external economic data, Live/Lab mutation or promotion is authorized. Do not request this scoped permission again.

## Evidence and economic boundaries

- Hold out the latest two completed months from proxy selection by default. If they influence selection or tuning, they are current broker/cost/execution evidence only.
- Binding evidence is continuous all-required-symbol FPMarkets `Every tick based on real ticks`, `100% real ticks`. Lower-quality or discontinuous history cannot pass or rescue a candidate.
- A Tester report is also bound to the synchronized symbol specification, including financing. Every serial control/candidate matrix must record the terminal build, source/SET and exercised history hashes, plus before/after `symbols-*.dat` and required-symbol contract/swap fingerprints. Judge a candidate only against an immediately adjacent control with the same fingerprint; any mid-matrix fingerprint change invalidates the entire matrix and requires a clean rerun.
- A high-turnover entry executable must average at least three actual lifecycle-starting first fills per normal FPMarkets trading day in both binding-long and the isolated latest period. Zero-signal days remain in the denominator.
- A frozen-entry management family preserves verified signal generation, opportunity timestamps, and directions. It may change only causal sizing, hold, protection, breakeven, trailing, legal partial close, full close, or no action, subject to the safety layer.
- Proxy best-of-many output is selection, not profit proof. Prefer broad profitable plateaus and terminate cost-negative core assumptions without nearby threshold, feature, label, model, or exit-number rescue.
- Implementation defects are corrected and rerun; they are not economic failures.
- In the optimization lane, environment and design defects follow the same rule and cannot close a campaign economically. Only valid economic numbers may judge optimization performance.

## Macro research allocation

Frontier research, including any temporary Lab detour from optimization, is allocated across macro programs rather than continued as a chain of nearby micro-hypotheses. Every new research unit must name exactly one primary program from the active map below. A unit may observe secondary interactions, but that does not create another active stream or authorize an adjacent successor. Optimization campaigns are not numbered Frontier units.

1. `entry_signal_market_structure` — 진입 신호·시장 구조
2. `external_market_event` — 외부시장·이벤트
3. `order_time_session` — 주문·시각·세션
4. `position_management_exit` — 포지션 관리·청산
5. `portfolio_capital_risk` — 포트폴리오·자본·위험
6. `execution_recovery_broker_safety` — 실행·복구·브로커 안전; outside the current Goal and never opened by Frontier rotation
7. `diagnostics_causal_meta` — 진단·인과·메타 연구

At each durable unit boundary, recompare Programs 1-5 and 7 as a whole. Select the next primary program from research coverage, causal readiness, independence from recently closed work and expected information value; this is coverage-aware judgment, not mechanical round-robin scheduling. Recent concentration and an underrepresented ready program weigh against staying in the same neighborhood.

One declaration may freeze a single research bundle containing two or three genuinely related variants needed to answer one macro question. The variants are allocated once, judged under one contract and closed together. After closure, do not extend the family with a nearby threshold, feature, time window, subgroup, symbol, event, exit or sizing rescue. A passing unit may retain one explicitly bounded later seed, but that seed is not opened automatically: all active macro programs must first be compared again, and any later work requires a newly named family and declaration.

Outcome-free feasibility may reject a proposed bundle before declaration. A sparse or structurally confounded bundle is not rescued by widening its window or substituting an adjacent dataset after the density result is known. Invalid execution evidence closes that unit without making environment repair the next research topic; Program 6 remains outside this Goal.

## One stream and identity separation

Maintain one attributable development judgment stream to its next durable economic or engineering boundary, freeze source and evidence, then switch. No parallel or delegated candidate streams are allowed. Every legacy provenance family, Next descriptive research family, optimization campaign, V5, every V6 version, and V7 keep separate source, binary, settings, Portfolio ID, Magic, state paths, evidence, and verdicts. Combination requires a separate user decision and fresh shared-account evidence.

The inherited B75 task is complete in Next as `RC16 Explicit Frozen-Life HOLD Confirmation`. It audited only the frozen B45/B55/B60/B65/B68/B74 decision records and confirmed full-volume HOLD to the original catastrophic stop or fixed eight-M30 exit. It opened no new data, outcome, latest period, model, executable, identity, deployment, or Live change.

The user subsequently authorized one new serial Lab family on 2026-08-24: `전략 독립성·위험배분 연구` (`strategy-independence-risk-allocation`). It compares six separate `$100` single-strategy EAs with the frozen six-strategy shared `$100` first-come control, attributes overlap and risk-admission misses, and evaluates causal one-slot reservation using stored fit-period scores. Its declaration, selection isolation, variants and stop rules are authoritative at `lab/evidence/STRATEGY_INDEPENDENCE_RISK_ALLOCATION_DECLARATION_V1.json`. It grants no Live change or promotion authority.

V1 of that family is closed as `NO_POLICY_PASSED_RETAIN_FIRST_COME`. The 2024 control contained real blocked-winner headroom, but none of the three predeclared causal reservation policies passed both half-year stressed-net and drawdown gates. Per the fixed stop rule, 2025 and 2026 were not opened, no nearby rescue was attempted, and frozen V7 first-come aggregate-risk admission remains authoritative.

The user subsequently authorized one new serial Lab family on 2026-08-24: `예치자본·위험용량 연구` (`deposit-capital-risk-capacity`). It first screens economically distinct deposit use, lot-tranche, slot-breadth, concentration and drawdown-governor hypotheses on already-consumed 2024 Lab ledgers, then permits exactly three tester-only EA paths under the predeclared shortlist rule. Candidate-specific selection begins fresh in 2025; 2026 January-May may open only after a 2025 pass, while June-July and partial August remain excluded. Its authoritative boundary and stop rules are `lab/evidence/DEPOSIT_CAPITAL_RISK_CAPACITY_DECLARATION_V1.json`. It grants no Live change or promotion authority.

V1 of that family is closed as `NO_NON_CONTROL_POLICY_PASSED_CLOSE_RETAIN_FROZEN_V7`. Deposit-matched linear lot/risk sizing passed its structural scale-consistency anchor but did not improve percentage return, drawdown or efficiency. The breadth candidate and diagnostic fixed-lot ladder failed the fixed two-deposit efficiency, return-floor or drawdown gates. Per the stop rule, no 2026 confirmation or nearby rescue opened and frozen V7 sizing and first-come 12% admission remain authoritative.

## Source topology discipline

The filesystem must make the current baseline, an active experiment and closed evidence distinguishable without reading source code. These rules are mandatory for every future engineering or research task:

- The sole forward Lab baseline is the exact root named in `CURRENT_STATE.md` and `lab/README.md`. It is the frozen CXR2 root `lab/engineering/protective-exit-order-reconciliation-v1/mt5/` at commit `0d4032786cecb7d7e8a4c3074609db5b105fa107`, promoted after the user's explicit repair direction and durable state updates. A later successor baseline requires an explicit user decision and a durable state update.
- `lab/mt5/` is a frozen historical mixed V7/Frontier workspace. No new or modified `.mq5`, `.mqh`, EA wrapper or Adapter may be placed there. It is not a forward baseline.
- Every new Lab family owns one self-contained root under `lab/research/<family>/` or `lab/engineering/<family>/`, including its source, configuration, evidence references and Git-ignored runtime path. It may copy from the frozen Lab baseline once, but it may not include, link or execute source from another Lab family, `lab/mt5/`, `optimization/` or `live-dev/`.
- `optimization/` is a third isolated lane. Its frozen baseline is a one-time byte-pinned copy of the exact active Live package named in `CURRENT_STATE.md`. A campaign under `optimization/campaigns/<family>/` copies that optimization baseline once, changes identity and all mutable paths before execution, and never includes, links or executes `live-dev/`, `lab/` or another optimization campaign. Runtime and large generated outputs stay under the campaign's dedicated `optimization/runtime/` and `optimization/artifacts/` paths.
- A baseline contains one assembly EA and only the modules needed by that EA. Experimental wrappers and adapters stay inside their family root. A predeclared multi-EA comparison may contain multiple entrypoints only inside that comparison root.
- A family root has only two mutable states: the one currently active serial stream, or none. Once a verdict closes or promotes the family, its tracked source and configuration are immutable. A follow-up creates a newly named/versioned sibling root instead of reopening or extending the closed root.
- Evidence and documentation must use full root-anchored paths. A basename alone never identifies a source file. Frozen evidence trees are not physically reorganized merely for appearance; any later archival move requires a complete reference audit and a separate user-authorized boundary.
- Live promotion still copies only the exact verified files from one closed Lab engineering candidate into `live-dev/package/active/`, assigns a new release ID and hashes, records the source manifest and evidence, and leaves no Lab or optimization link. An optimization result has no direct promotion authority; a user-authorized promotion must first enter a separately named Lab engineering handoff family. The active Live package contains one EA, its required Include tree, one base SET and release/source manifests—never experimental wrappers or adapters.
- At every durable boundary, `CURRENT_STATE.md` and `lab/README.md` must still name the same sole forward baseline. If a proposed change cannot state its owning root and lifecycle before code is added, implementation does not begin.

## Live and Lab isolation

- `lab/` owns Lab research/engineering source, frozen controls, tester configuration and Lab evidence.
- `optimization/` owns the independent frozen Live-derived baseline, evolving optimization campaigns, their configuration/evidence and dedicated generated runtime/artifacts.
- `live-dev/` owns only a frozen release snapshot, Next-specific settings, operator tools, state, logs, and its own Portable terminal.
- Live, Lab and optimization never include or execute one another's paths and never share EX5, settings, state, logs or Portable runtime. Lab and optimization never write Live state. Promotion is a deliberate one-way frozen copy with new release identity and hashes.
- Legacy B70 V6R6 and superseded V7 releases remain stopped. Only the exact release named as owner in `CURRENT_STATE.md` has Live authority. A later engineering patch requires verified Lab evidence, a new frozen release manifest, explicit user authorization, a stopped-flat boundary, entries-disabled recovery and an exact final handshake before it may replace that owner.
- V7 uses Magic `260824701..260824706`, a new Portfolio ID and state namespace, and never adopts V6R6 state or positions. Only final project-attributable realized P/L may be carried into its prior-realized input at handoff.

## Safety and authorization

`CURRENT_STATE.md` is the sole Next Live authority. `DISABLED` means no real order, no periodic broker watcher, and no position/order/deal polling. A deposit never grants authority. Immediately before any first V7 real order, require exact account, broker, symbol contracts, ownership, flat exposure, risk, restart behavior, source/binary/settings hashes, `origin/main` identity, and an entries-disabled `0/0` preflight.

On duplicate-runtime risk, unknown ownership, corrupt state, broker/local mismatch, or minimum-volume risk breach, fail closed for new orders and keep existing owned risk safe. Never run legacy and Next order owners together. If a Next handoff fails before any Next order, legacy may be restored; if a Next order or position exists, do not restart legacy.

### 2026-08-31 exact V8 one-time user-accepted Live research exception

The user explicitly accepted the disclosed valid clean-history nonconfirmation and authorized a complete real-account research replacement plus the required top-level approval change for exactly release `NEXT-E02-V8-PMLR1-b1c77d3b6356`, Portfolio `ZT-PORT-NEXT-V8-PMLR1-20260831`, Magic `260831901..260831906`, canonical source/settings SHA-256 `B1C77D3B635626EAA000F3A605F2CB1BC5A4D0C43709E8C3B3F693469F126B95`, EX5 SHA-256 `E61CA9D50F8C6BF4849A9C2E857B08A6E9C4FD390B1B8DC0493EB741689D9274`, weights `2 / 1.5 / 2 / 2.5 / 1.5 / 0`, position risk `0.04` and aggregate cap `0.18`.

For only that already-translated package, `CURRENT_STATE.md` may authorize real new entries without a passing clean requalification or a separately rerun corrected Lab economic handoff. The V2 result remains `VALID_CLEAN_HISTORY_FIXED_PAIRED_CANDIDATE_NOT_REQUALIFIED_NO_LIVE_AUTHORITY`: actual/stressed `+$409.81 / +$367.818`, relative equity DD `37.39%`, control deltas `+$36.51 / +$25.1525`, actual/stressed multiples `1.0978x / 1.0734x`, robust recovery `3.29586` versus control `6.32347`, and Cross stressed `-$2.56`. None of those failed gates is relabelled, erased or used to lower a future research, Optimization, Lab or Live gate.

This exception changes economic approval only. Activation still requires a clean `main == origin/main`, exact package and account binding, one terminal owner, a verified flat stopped boundary, fresh entries-disabled `0/0` preflight, zero positions, pending orders, margin and planned risk, continuous actual ticks, synchronized current US30/US100/US500 M15/M30/H1 bars, the unchanged executable `3.0`-second market-freshness and evaluation-time guards, and zero safety, persistence, broker, foreign-exposure or protection fault. A failed operational gate leaves new entries closed. No other candidate, release, package byte, identity or risk contract inherits this exception; any such change returns to the normal promotion rules unless the user makes another explicit top-level policy decision.

## Engineering practice

Use the lightest normal implementation that preserves causal ordering, broker costs, rounding, minimum volume, shared-account risk, restart recovery, and bounded logging. Do not create CI, unit/integration/regression tests, separate validators, parity checkers, promotion checkers, test-only infrastructure, or a project-specific Skill. Verify V7 with MetaEditor compilation, normal fixed-window MT5 real-tick executions, post-result ledger comparison, and bounded entries-disabled operating observation. Normal MT5 parameter optimization and campaign-owned economic result recording are permitted optimization work, not test infrastructure.

## Research artifact retention and storage hygiene

Run one serial retention sweep on the first weekend of every calendar month, before creating any new Lab or optimization Portable when the system drive has less than `30 GiB` free, and whenever planned runtime plus log growth would leave less than `30 GiB`. The sweep never runs while a Live, Lab Tester, optimization Tester, MetaEditor or dashboard process owns a target path. Preserve tracked source/configuration, declarations/results/closures, manifests and every artifact named or hashed by durable evidence; preserve the sole canonical required-symbol history copy, active baselines, the active Live package, Live state/events and the canonical research candidate/lifecycle ledgers. Only Git-ignored closed-family/campaign Portable copies, generated Tester/optimization caches, unreferenced duplicate reports/logs, compile staging and temporary files are eligible. Resolve and record exact absolute targets before deletion; if uniqueness or evidence reachability is uncertain, retain the item. Bound detail telemetry at creation, reserve enough free space for EA logging throughout every run, and stop starting new paths before capacity becomes uncertain.

Keep `CURRENT_STATE.md` concise and current. Store historical transitions in numbered state chunks, human research summaries under `docs/lineage/`, and complete immutable references under `lineage/`.

### 2026-09-08 authorized V7 native winner-child completion

The latest direct user reply `ㄱ` explicitly approves the concrete bounded Family20 proposal SHA489D5DCEAA8B2FE8BC1E6A7AC2ED4D1A876BCC951D384DF373D2E8B0380D5867. This scoped exception permits only the exact1151-byte ONNX graph and13626-byte2024initialstate named in `optimization/evidence/V7_WINNER_CHILD_NATIVE_USER_AUTHORIZATION_V1.json`, fresh originalV7-derived source and its own runtime. Run the complete fixed six-path full2025 native bundle and, only for one eligible unchanged learned finalist, a separately funded continuous2025-Aug2026 paired confirmation. Preserve original failed proxy counts, minimum20children, every growth/DD/recovery/causal/100%-history gate and all closed families. No old EA/Include/Python execution or final2025state, unrelated model, independent strategy, external economic source, Live/Lab mutation or promotion is authorized. The user has not paused; proceed without asking this scoped permission again.


### 2026-09-08 user storage clarification

Preserve important artifacts, economic evidence, source, models and learned/persistent state. The user explicitly permits deleting rerunnable or regenerable files such as stopped Portable duplicates and generated Tester caches. Record exact targets and preserve any sole required history copy. The initial exact duplicate-cache plan preserves one byte-identical copy of every content; durable historical path/hash references remain, with any completed removal recorded as a retention action. This does not permit deleting important artifacts merely because they could be rerun, or touching Live/Lab/current-run inputs. Automatic tool-policy rejections are not authorization to use another shell or tool to bypass the block.

### 2026-09-08 authorized V7 native entry-compounding completion

The latest direct user reply `ㄱㄱ`, following plain-language explanations of the pending proposal, explicitly authorizes only Family23 `optimization/campaigns/v7-native-entry-compound-completion-v1/`. Proposal SHA E82893BE008C9C6EBCD90067E493EC8A64EA02B3FEC0247F81E996017DAAABCE and `optimization/evidence/V7_ENTRY_COMPOUND_NATIVE_USER_AUTHORIZATION_V1.json` define the scoped exception to closed Family1. Copy only its exact561-byte ONNX graph and1822-byte2024model metadata into fresh originalV7-derived source and a dedicated physical Optimization runtime. Complete the fixed full2025 control/static/control/online native bundle and, only for one unchanged eligible finalist, separately funded continuous2025-Aug2026 confirmation. Preserve the failed teacher-population screen, all fixed models/features/sizing/update formulas and every100percent-history/growth/DD/recovery/epoch/causal gate. No refit, tuning, independent strategy, old candidate code/state/teacher2025 execution, other closed-model exception, external economic data, Live/Lab change or promotion. Continue without asking this scoped permission again.

The latest user deletion instructions retain the already explicit regenerable-cache/staging cleanup authorization across follow-up turns. Confirm exact absolute targets and preservation internally, then complete authorized cleanup without another permission request. Important artifacts, source, models, state, final outputs and unique required history remain preserved. If a command is actually rejected, report only its returned error and established cause; do not invent an approval deficit or claim a general deletion prohibition. These instructions do not override or authorize bypass of a host/tool enforcement policy.

### 2026-09-08 current-work completion and user pause

The latest user direction is to finish only the current related work, then pause and deliver a full session development/results briefing. Family23's unchanged four-path native bundle and related accounting/preservation/reporting are now complete with no eligible finalist. Development is PAUSED_BY_USER and the improvement objective remains unmet. Do not open any successor, parameter/model/period rescue, retained-seed follow-up or new native research run until the user resumes. Existing Live-Dev files, processes and the exact user-activated authority block remain unchanged.
