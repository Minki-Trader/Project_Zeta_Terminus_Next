# Native V7 ONNX and online learning

This is the sole native learning-engine campaign. It physically derives exact V7R once from `optimization/baseline/NEXT-E03-V7R-RLO1-0bba2ca045fe/` into separate tester-only control and learner roots. It implements actual ONNX inference and online weight updates from the EA's own matured trade labels. All original V7 trading decisions and economic settings are initially retained.

This phase establishes an operational native learning path and captures attributable forecasts/checkpoints. It does not install a failed risk/exit/allocation policy or claim that observational learning increases profits. The exact control and observational learner are compared on ordinary 2024-2025 complete real-tick runs; identity, model activity, costs, economics and input integrity remain attributable. Live-Dev is untouched.

`evidence/DECLARATION_V1.json` owns the complete implementation/learning/run contract. Own physical Portable and large raw results belong to the matching `optimization/runtime/` and `optimization/artifacts/raw/` paths. The EA refuses non-Tester execution. Keep at least 30 GiB free and preserve all canonical artifacts. No tests, validator, parity checker, promotion machinery or broker watcher.
