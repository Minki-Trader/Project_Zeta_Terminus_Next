# V7 spatial quote-activity profile return

Sole Program 1 family, derived from frozen Optimization V7R. Replace only Passive with one US30 return-to-prior-profile-mode book; preserve the five original market engines for conditional native integration. Live and Lab are untouched.

Exactly the previous observed source date supplies a 32-bin price profile weighted by M1 tick counts, with fixed Dirichlet 1 smoothing. These counts describe quote activity, not exchange volume or order flow. The first busy-to-thin crossing each date consumes the event. Direction aims toward the frozen prior profile mode. One fixed 16-cell shrunk return estimate is trained on complete 2024 labels. Fresh Gather ONNX performs every 2025 forecast. STATIC retains that fit; ONLINE discounts cell sums/counts by .99 for every strictly mature shadow label. No label from the current decision can update that decision.

The ordinary producer has only `prepare` and `run`: physical timestamp-only copies of original anchored sources, followed by one fixed fit and complete STATIC/ONLINE 2025 production. Full calendar grids, first events, profile weights, shadow paths, forecasts, updates and final pending/model states remain attributable. The declaration is authoritative for the complete fixed bundle and gates.

Selection uses fixed .01 quantity, 200-point protected stops, profile targets, four-hour deadlines and conservative M1 barrier ordering. Actual and stressed quote results are selection evidence only. Shared capital/risk, actual fees and financing, changing stop geometry, native ticks, DD and the original daily lot staircase require a fresh full adjacent control/candidate run if one learned role qualifies. Candidate 2026 data stays unopened before that selection.

No parameter rescue, external economic data, test infrastructure or Live promotion. Preserve the whole bundle on closure. Maintain at least 30 GiB free; own initial input/model/output cap 256 MiB. Conditional runtime and output caps are 6 GiB and 2 GiB.
