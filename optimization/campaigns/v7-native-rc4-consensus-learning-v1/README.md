# Closed original V7 RC4 consensus source phase

This sole Program 4 family is preparing original V7 records before any learning model is declared. The prior return-verification lifecycle ledger contains 2024, but its rotating A/B core event files retain no 2024 checkpoint records. The retained source-view result is unavailable input, not proof that the original strategy had no decisions.

The owned control derives once from the exact frozen original V7R Optimization baseline. It preserves all six strategies, entry and exit rules, quantities, risk limits, the original RC4 three head formulas, ordinal cutoffs, eight-bar checkpoint, quarter-loss stop and shadow occupancy. It changes only its identity, fresh tester-only output namespace and event capacity to the original permitted 8192.

The current source run is full 2024, starting at $100 with leverage 1:100, using all three required symbols and native Model 4. Complete 100% actual ticks and valid operating evidence are required. Every attempt and its artifacts are retained. Source availability and the previously fixed population/diversity requirements must pass before a full static/online model contract or any target is opened. Source counts cannot establish an economic improvement.

Compile-v2 is clean. All required December 2023 through December 2024 tick months were acquired in bounded memory; the native caches and monthly counts/hashes are retained without duplicate large NumPy arrays. The current 681-file `evidence/SOURCE_INPUT_FREEZE_V1.json` binds exact source, binary, settings, public platform/SDK and consumed 2023/2024 history. Later automatic terminal cache is outside the source experiment and is not candidate input.

Runtime 6 GiB, raw artifacts 2 GiB and family source 32 MiB are prospectively funded above a 30 GiB free-space floor. Live-Dev and Lab are unchanged. No closed campaign source, model, label trajectory or runtime is executed; public platform/history files are separately attributed physical copies.

The complete 2024 native source now passes 100% real ticks and unchanged inputs/history. Its 4061 core events span START on January 1 through STOP on December 31. All 48 RC4 checkpoints join unique complete lifecycles: 24 in each half, 29 long and 19 short, with 13 ordinal vote patterns. The 19 nonunanimous decisions fall below the prospectively fixed minimum of 24. No record is excluded and no source gap remains.

The family closes without a learning target, model or candidate. Do not lower the count requirement or widen the question. The original archive correction is complete. `evidence/COMPLETE_SOURCE_METADATA_RESULT_V1.json`, `ROOT_COMPLETE_SOURCE_EVIDENCE_V1.json`, `COMPLETED_SOURCE_RETENTION_V1.json` and `CLOSURE_V1.json` retain the full result. All 19 native artifacts and every receipt remain preserved.
