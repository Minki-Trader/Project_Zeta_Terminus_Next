# DD20 Frequency Execution-Lane Combination Proxy V1

This source-free campaign evaluates the user's proposed frequency/symbol separation without changing the current anchor, H4 role, Live, Lab or Master.

## Frozen structure

- High/mid-frequency execution lane: the qualified paired-month anchor, with US30 and US100 order ownership.
- Low-frequency execution lane: the exact lineage-pinned US500 H4 staged overlay at fixed `0.01` volume and unchanged signal, hold, balance and margin gates.
- Final gate: one combined full-portfolio MT5 candidate after the source-free proxy. Isolated lane results are never summed into a final verdict.

The split is an execution-ownership split, not a complete market-data split. The high/mid lane's US100 Cross signal uses synchronized US100, US30 and US500 H1 closes, so US500 H1 remains a read-only input. Removing that dependency would be a separate entry-signal research hypothesis and is not part of this campaign.

## Current boundary

The contract froze one candidate and no grid. It transferred only already complete, hash-pinned Optimization and lineage numbers into this self-contained root. One `0.0009185`-second process completed with `VALID_PROXY_COMPLETE_ONE_COMBINED_MT5_SHORTLIST`.

The direct-only projection adds only the prior shared H4 trade contribution and reaches selection actual/stressed `+$5,793.39 / +$5,483.6992`. The nonbinding coupled-information projection is `+$5,829.67 / +$5,519.3732`. All four direct-only projected epochs remain positive. Adding the previously observed H4 native DD charge produces `20.861542647%`, a nominal `0.861542647`-point miss and `4.307713235%` proportional overshoot. The nominal gate is false; the proxy pragmatic reference gate passes below the conservative `6%` end of the user's example.

The high/mid forward maximum closed balance is only `$146.88`, below the unchanged `$500` H4 entry gate. The projection therefore retains forward `+$32.74 / +$30.626` at `18.675302130%` DD and zero H4 entries.

Exactly one combined candidate may advance to `dd20-frequency-lane-combined-portfolio-mt5-v1`: unchanged paired-month US30+US100 execution plus the unchanged fixed US500 H4 lane, sharing real balance, equity, margin and downstream state. Earlier selection activation on the stronger anchor makes every projection nonbinding; only the combined MT5 result may judge economics. No adjacent H4 parameter, Cross input, lane allocation or threshold may be searched inside this family.
