# DD20 Native-Gap July-Robustness Proxy V1

This independent fast proxy uses the valid but rejected `1.8 / 1.3 / 1.4 / 2.6 / 1.5 / 0` MT5 result as a fourth native anchor. That point kept explosive selection profit at actual/stressed `+$4,169.94 / +$3,942.2615`, but native selection equity DD reached `20.4883486%` and its continuous July stressed net was `-$2.2375`. It is evidence for this successor, not a candidate that may rerun.

## Frozen local search

- Component order remains range-61 / range-64 / US100 cross / US30 intraday pressure / US30 return / disabled impulse-passive.
- The grid intentionally cuts the three July loss sources and expands the profitable intraday source: range-61 `1.2..1.8`, range-64 `0.8..1.4`, cross `0..1.2`, intraday `2.4..3.4`, return `1.0..1.6`, all by `0.1`; impulse stays `0`. This is exactly `49,049` compositions.
- Executable `0.01`-lot materialization, passive reservation volume, the `$150` stressed-balance ladder, `0.04` position risk and `0.18` aggregate admission remain unchanged.
- Selection floating-equity DD was non-monotonic across the preserved base, near-miss, high-exposure and exposure-margin MT5 points. Each candidate therefore pays the worst positive observed-minus-raw gap, `1.7826874563` percentage points, plus a frozen `0.75`-point reserve before the hard `20%` gate. This implies raw selection DD at or below `17.4673125437%`.
- Full June/July DD similarly pays the worst four-anchor positive gap, `5.6021825077` points, plus `0.5` point. Raw paired-forward DD must therefore be at or below `13.8978174923%`.
- Independent-July proxy net overstated continuous MT5 July net by as much as `$6.82` actual and `$6.8535` stressed across the three candidate anchors. Both candidate nets are reduced by those worst shortfalls and an additional frozen `$1` reserve; both conservative July nets must remain positive.
- Long selection and every epoch, the full paired forward, independent June and raw independent July must remain actual/stressed positive with positive balances and their declared DD gates.
- Exactly one role may freeze: maximum selection stressed profit, then conservative July stressed profit, full paired-forward stressed profit, weaker independent-month stressed profit, selection actual, recent-selection stressed and lower budgeted DD.

The input is a physical 15-file copy with no mutable cross-family link. The original 15 combinations and all three prior MT5 candidates remain closed and will not rerun. No MT5 launches during this proxy, and a valid result may nominate at most one new hypothesis.

## Current boundary

Declaration commit `37edf73` reached `origin/main` before one valid proxy invocation. The process completed all `49,049` candidates in `14.34` seconds without MT5. `1,509` passed selection, the full pair and June; `815` passed raw July and `647` remained after the worst continuous-July shortfalls and the additional `$1` reserve.

The sole maximum-profit role is `1.6 / 0.8 / 0.4 / 3.2 / 1.2 / 0`. Selection actual/stressed proxy net is `+$1,763.4819 / +$1,693.3221`, improving the preserved comparator by `51.13% / 56.01%`. Raw/calibrated/budgeted selection DD is `17.428221% / 19.210908% / 19.960908%`. Full paired-forward actual/stressed is `+$28.87 / +$27.236` at budgeted DD `15.310773%`; June stressed is `+$18.48`, raw July stressed is `+$8.756`, and conservative July stressed is `+$0.9025`.

Status is `VALID_PROXY_COMPLETE_ONE_MT5_SHORTLIST`. The narrow budgeted DD headroom and conservative July margin make this exactly one MT5 hypothesis, not confirmed success. All anchors and the original 15 remain closed without rerun.
