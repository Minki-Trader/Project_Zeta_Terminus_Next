# V7 winning-parent inventory branch

Program 5: preserve all six original strategies and add at most one independently protected same-direction child after a market parent first reaches one original gross-stop distance in favorable completed-minute price movement. Parent entry quantity, stop, ARC management and exit remain unchanged. Child uses parent quantity and a fixed midpoint stop; both full planned-risk reservations count within original4%/12%/45% rules. No profit collateral, risk credit or child-of-child is assumed.

The prospective three-role bundle contains unfiltered child reference, static Gaussian-kernel ONNX and strict matured-label RLS online ONNX. The unfiltered reference is never a native finalist. Learning must improve the new action relative to unfiltered additions and clear complete profit/half/activity/closed-DD gates before one actual native candidate opens.

`pyramid_model.py prepare` physically copies anchored original input through2025. `pyramid_model.py run` creates all-parent status/child label evidence, one fixed2024kernel fit and the entire2025teacher-basket comparison. It does not execute a terminal or query a broker. No source/model/state from a closed candidate is imported.

Original lifecycle time fields are observer seconds, not broker deal milliseconds. Selection excludes the parent closing minute from child entry, defers proxy parent-driven child exit through that minute, and excludes entry-minute high/low from post-entry extrema. Same-second basket closes are aggregated for the closed-balance path. These are explicit price/occupancy approximations; actual synchronized parent-child orders, capital/risk/margin, financing/fees, compounding and equity DD require a complete unchanged real-tick control/candidate pair.

Thirty GiB free storage and whole-family byte preservation remain mandatory. No Live/Lab changes, external economic data, tests/checkers, or adjacent rescue after closure.
