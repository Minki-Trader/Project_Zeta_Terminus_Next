# DD20 V8 Attained-Profit Floor Fine Adjacency Proxy V1

This is mandatory Stage B of `V8-OPT-U004-ATTAINED-PROFIT-FLOOR-PROTECTION`, still inside the sole Program-4 Optimization stream.

## Frozen question

Stage A produced no economically eligible coarse role, but its least-deficient surface moved toward higher activation and higher retained floor. Stage B therefore exhausts the complete predeclared local activation/floor neighborhoods around all three immutable near-miss centers before the mechanism may be rejected.

Every role keeps all five exact-V8 entries, directions, timestamps, weights, initial stops and native maximum holds. It may only tighten an already open position after the position reaches its frozen planned-risk-R activation. The adverse partial-identification books, original native occupancy, source volumes and zero freed-capacity credit remain unchanged.

## Frozen map

- Center 1 `A/F = 0.75/0.25R`: activation `0.65 / 0.75 / 0.85`, floor `0.20 / 0.25 / 0.30`.
- Center 2 `A/F = 0.75/0.10R`: activation `0.65 / 0.75 / 0.85`, floor `0.05 / 0.10 / 0.15`.
- Center 3 `A/F = 0.50/0.25R`: activation `0.40 / 0.50 / 0.60`, floor `0.20 / 0.25 / 0.30`.

Each center contributes a complete `3 × 3` map. The union is exactly `27` unique roles with `36` center-local immediate edges. Economic/identification gates and the declared near-miss rule remain unchanged. Stage B cannot close U004.

## Current boundary

Stage-A result commit `355702a` reached `origin/main`. Stage B is declared pre-input, pre-implementation and pre-outcome. Declaration `DD20_V8_ATTAINED_PROFIT_FLOOR_FINE_ADJACENCY_PROXY_V1_DECLARATION.json` is `10,371` bytes / `FD8D55FA...5F33D1`. No Stage-B copy, config, proxy source, fine result, validation economics, holdout, code candidate or MT5 path exists before the declaration reaches `origin/main`.
