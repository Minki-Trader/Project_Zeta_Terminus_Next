# DD20 Component Equity-State Native-Margin MT5 V1

This dedicated tester-only family materializes one exact information clue after the closed component-equity-state proxy was compared with every active optimization mechanism. It is not a reopened proxy winner: the proxy shortlist was empty, but one profit-valid boundary improved conservative actual/stressed profit while adding exactly zero raw closed-balance DD. Its `20.300371765%` budget consists of the qualified native gap plus a frozen `0.75`-point unobserved state/execution reserve, so one native MT5 selection is the bounded way to resolve the remaining information.

## Frozen role

- Component order is range-61 / range-64 / US100 cross / US30 intraday pressure / US30 return / disabled impulse-passive.
- Base weights are exactly `1.6 / 0.8 / 0.4 / 3.2 / 1.2 / 0`, with `$150` stressed-balance daily growth, `0.04` position risk and `0.18` aggregate admission.
- Each active component owns a candidate-local deque of its last `40` admitted fully completed lifecycle observations. One observation is the lifecycle's accumulated doubled-cost-stressed net divided by its original admitted planned risk.
- Before 40 observations the exposure overlay is `1`. At a full close, a mean below `-0.10R` makes that component's next exposure `0`; a mean above `+0.10R` makes it `1.125`; otherwise it remains `1`.
- Effective component weight is base weight times overlay and materializes through positive `MathRound` volume-step semantics. Rejected, skipped and disabled births never enter state. Partial exits accumulate against the original lifecycle planned risk and append only once on the full exit.
- State is intentionally tester-only and in memory. Every selection or forward process starts with empty deques and no partial lifecycle accumulator. This family is not a restart-resilient Live implementation and has no promotion authority.

The frozen proxy clue used lookback `40`, band `0.10R`, loss `0`, gain `1.125`. It made `46` non-neutral executable decisions across four active components, earned raw actual/stressed `+$1,850.789202 / +$1,778.655702`, and conservative `+$1,728.847345 / +$1,661.593631`, beating the qualified native anchor by `+$37.307345 / +$35.333631`. Raw DD stayed exactly `17.4282210109%`; only the frozen native gap and `0.75`-point uncertainty reserve raised the budget to `20.300371765%`.

## Source and isolation

The source is derived once from the frozen optimization baseline `optimization/baseline/NEXT-E01-V7-RLO1-b32e7e176f2e/`, not from another campaign. The family owns a new expert, Include namespace, release/execution/Portfolio/schema, six Magic values, state/research paths, settings, reports and a dedicated physical Portable. It never includes, links or executes Live, Lab or another campaign source and never uses the Master terminal.

Identity is frozen as expert file `ZetaDD20CESNM1`, Include root `ZetaOptimizationDD20ComponentEquityStateNativeMarginMT5V1`, release `OPT-LIVE-V7-DD20CESNM1-20260829`, execution `zt-opt-live-v7-dd20-component-equity-state-native-margin-mt5-v1`, Portfolio `ZT-OPT-LIVE-V7-DD20CESNM1-20260829`, schema `opt-dd20-cesnm-mt5-1`, Magic `260829861..260829866`, and candidate-local mutable root `ZetaOptimization\\dd20-cesnm1`. The shorter mutable root is the engineering-corrected form: the initially declared long root made the local-agent ownership filename 261 characters and could not open under MQL error `5003`. The expert file name is likewise shortened from the descriptive declaration because its tester-cache filename reached 262 characters; both corrections change no economic input or behavior.

## Economic contract

One real-tick `US30 M30` selection may run from 2022-08-01 through 2026-06-01 at `$100`, USD, `1:100`. A complete valid selection must have no core/persistence/broker/protection/research-drop fault. Its actual and doubled-cost-stressed net must each strictly exceed the qualified native `+$1,691.54 / +$1,626.26`, native maximum relative equity DD must be at or below 20%, and each of the four frozen selection epochs must remain actual/stressed positive.

Only if selection satisfies every gate may one separately initialized real-tick forward run from 2026-06-01 through 2026-08-01. Full forward and continuous June and July must each remain actual/stressed positive, and native forward relative equity DD must remain at or below 20%. Selection state, files and balances do not carry into forward.

Environment, history, invocation, parser, serialization, source or engineering defects are correction states with no retry cap and no economic verdict. A complete valid economic miss closes the exact role without retuning. No grid, neighbor, qualified anchor, prior candidate or original 15-combination path may rerun.

## Closed result

The first two tester invocations were non-economic engineering corrections. Attempt 1 stopped before any tick because the descriptive ownership path reached 261 characters. Attempt 2 completed the real-tick calculation but produced no HTML report or native cache because the report directory was absent and the descriptive expert-derived cache path reached 262 characters. Only the isolated mutable root and expert filename were shortened; every candidate input and behavior stayed frozen. Both attempts are preserved with `economic_verdict: null`.

The first complete valid selection used 100% real ticks (`132,257,500` ticks, `1,295` closes) and earned actual/stressed `+$1,693.46 / +$1,626.677`. It strictly beats the qualified native anchor by `+$1.92 / +$0.417`. All four epochs are actual/stressed positive. MT5 native maximum relative equity DD is `19.797353497%`, leaving `0.202646503` percentage points under the hard cap. The component state made `76` gain-overlay evaluations and no loss-overlay evaluation; the entire net improvement appears in the latest selection epoch.

Because every selection gate passed, one fresh-process forward ran. It earned actual/stressed `+$23.01 / +$21.256` at native equity DD `12.436759043%`; June is `+$19.79 / +$18.976` and July is `+$3.22 / +$2.280`. No component reached the 40-close warmup threshold, so every executable forward overlay remained `1` and the economics exactly match the qualified anchor forward.

The durable result is `DD20_COMPONENT_EQUITY_STATE_NATIVE_MARGIN_MT5_RESULT_V1.json`, classified `MT5_CONFIRMED_COMPONENT_EQUITY_STATE_NARROW_PROFIT_UPLIFT_DD20_CANDIDATE`. This is a valid but narrow new optimization success anchor, not an automatic Live promotion. The complete source, EX5, reports, native caches, ledgers, logs and both correction attempts are preserved; the reproducible 5.826 GB dedicated Portable was permanently removed after preservation. No candidate, grid, prior path or anchor was rerun or retuned. Any successor must follow a fresh whole-map comparison and be economically distinct rather than an adjacent state threshold, lookback or subgroup continuation.
