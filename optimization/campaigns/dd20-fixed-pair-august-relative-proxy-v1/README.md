# DD20 Fixed-Pair August Relative Proxy V1

This closed independent Optimization campaign corrected one measurement gap left by the August MT5 anchor. The anchor established that the unchanged fixed replacement earned positive native economics, but it had not run the exact active Live-derived control on the same interval. This campaign measured that relative separation and authorized exactly one adjacent paired MT5 check.

## Frozen question

- `LIVE_CONTROL`: weights `1 / 1 / 1 / 1 / 1 / 1`, position risk `0.04`, aggregate risk `0.12`.
- `FIXED_REPLACEMENT`: weights `2 / 1.5 / 2 / 2.5 / 1.5 / 0`, position risk `0.04`, aggregate risk `0.18`.
- Both use the exact shared baseline formulas, clocks, stops, sizing ladder, execution priority and cost model.
- The sole interval is `2026-08-01 00:00:00` through exclusive `2026-08-29 00:00:00`, divided into four declared weekly epochs.
- There is one formula point and two fixed contracts. No parameter, weight, risk, clock, symbol, strategy or threshold is searched or changed.

The broad parameter arrays retained in the contract are lookup tables needed to preserve the proven proxy engine's categorical and array indexes. They are not an executable search space in this campaign.

## Isolation and acquisition

The proven source-free formula proxy engine was copied once into this campaign and then given a new campaign identity, local paths and a direct-execution guard. The campaign does not import or execute another campaign. Its market snapshot is exported only through its own physical Portable using terminal initialization, symbol selection, symbol metadata and rate-history calls. No broker account, position, order or deal API is permitted.

The market request begins in January 2025 to provide ample causal warm-up for every fixed baseline window and ends at the August test boundary. The proxy first has to reproduce the already preserved fixed-replacement August native result within the frozen lifecycle and dollar tolerances. Any miss is an engineering correction state with no economic verdict.

## Relative decision

After the native candidate anchor passes, the campaign compares actual/stressed net, profit factor, closed-balance drawdown, lifecycle count, risk admissions, weekly breadth, active-component breadth and concentration. The nominal checks and the separately stated practical rule are frozen in `config/campaign-contract.json`.

A meaningful clue authorized only one new paired MT5 campaign: exact control followed immediately by exact candidate under one unchanged tester fingerprint in a new dedicated non-Master Optimization Portable. The prior candidate-only August run remained an anchor and could not stand in for that binding adjacent pair. No result here changes Live, Lab or release state.

## Closed result

The fixed-replacement proxy reproduced the native August anchor within every frozen tolerance: actual `+$29.76` versus `+$29.62`, doubled-observed-cost stress `+$28.8748` versus `+$28.586`, and exactly `29` closes. Against the same proxy control it improved actual/stressed net by `+$13.21 / +$12.8996`, raised stressed PF from `1.7096` to `1.8969`, was not worse in three of four weekly epochs, kept all five active components positive and held its largest component share to `32.88%`. All seven nominal clue checks passed.

The proxy therefore closed `MEANINGFUL_FIXED_PAIR_AUGUST_RELATIVE_CLUE_ADVANCE_TO_PAIRED_MT5`. It selected no new formula, weight, risk or candidate. Its sole escalation was consumed by `dd20-fixed-pair-august-relative-mt5-v1`; no further proxy successor is implied.
