# DD20 V8 Attained-Profit Floor Causal Ablation Validation Proxy V1

This is mandatory Stage C of `V8-OPT-U004-ATTAINED-PROFIT-FLOOR-PROTECTION`, still inside the sole Program-4 Optimization stream.

## Frozen question

The three Stage-B near misses are immutable: `A/F = 0.40/0.30`, `0.85/0.30`, `0.75/0.30R`. Each center owns one unchanged all-five role plus five diagnostics that return exactly RC61, RC64, Cross, Intraday or Return to exact-V8 native exit behavior while the other four retain the center floor.

The `18` roles open fresh January-May validation from `$100`. A center must keep lower actual/stressed/minimum positive, DD at most `30%`, stressed retention at least `65%` of exact V8 and its already frozen identification density. It also fails causal nondominance if any one-component native-exit ablation has no lower stressed net and no higher DD with at least one strict improvement beyond `1e-9`.

An ablation can reject a center but cannot replace, retune, rescue or advance instead of it. No floor `0.35R`, adjacent activation, component-specific floor or new entry opens.

## Current boundary

Stage-B result commit `20d403e` reached `origin/main`. Stage C is declared pre-input, pre-implementation and pre-validation-outcome. Declaration `DD20_V8_ATTAINED_PROFIT_FLOOR_CAUSAL_ABLATION_VALIDATION_PROXY_V1_DECLARATION.json` is `9,382` bytes / `68E0304B...5A165`. No Stage-C copy, role table, config, source, January-May economic result, locked holdout, code candidate or MT5 path exists before the declaration reaches `origin/main`.
