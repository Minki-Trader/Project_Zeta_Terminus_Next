# DD20 Qualified Profit-Redistribution Proxy V1

This independent fast proxy starts from the first fully qualified DD20 MT5 result rather than reopening it. The preserved `1.6 / 0.8 / 0.4 / 3.2 / 1.2 / 0` anchor earned selection actual/stressed `+$1,691.54 / +$1,626.26` at `19.5503718%` native equity DD and kept both continuous June and July positive. A successor must conservatively improve its profit while retaining the same hard economic protections.

## Frozen redistribution search

- Component order remains range-61 / range-64 / US100 cross / US30 intraday pressure / US30 return / disabled impulse-passive.
- The grid expands the two strongest qualified-anchor selection contributors and opens the July-loss channels down to zero: range-61 `1.4..2.4`, range-64 `0..1.0`, cross `0..0.8`, intraday `2.8..4.0`, return `0..1.2`, all by `0.1`; impulse stays `0`. This is exactly `184,041` compositions.
- Executable `0.01`-lot materialization, passive reservation volume, the `$150` stressed-balance ladder, `0.04` position risk and `0.18` aggregate admission remain unchanged.
- Five valid MT5 anchors now calibrate selection DD. The qualified success has the largest positive native-minus-raw gap, `2.1221507541` percentage points. Every unknown candidate pays that gap plus a frozen `0.25`-point reserve before the hard `20%` gate, so raw selection DD must be at or below `17.6278492459%`.
- Selection profit is also conservatively corrected. Across all five anchors, the proxy overstated actual/stressed selection net by at most `$659.7999404762 / $615.1108690477`. Every candidate loses those amounts plus a frozen `$50` reserve, and both corrected nets must strictly exceed the qualified MT5 anchor's `+$1,691.54 / +$1,626.26`.
- Full June/July DD retains the worst five-anchor positive gap `5.6021825077` points plus `0.5` point, requiring raw paired-forward DD at or below `13.8978174923%`.
- Independent-July net retains the worst proxy-to-continuous-MT5 shortfalls `$6.82 / $6.8535` plus the frozen `$1` reserve. The new qualified anchor adds another observed July check but does not enlarge either worst shortfall.
- Long selection and every epoch, the full paired forward, independent June and raw and conservative July must remain actual/stressed positive with positive balances and their declared DD gates.
- Exactly one role may freeze: maximum conservative selection stressed profit, then conservative selection actual, conservative July stressed, full paired-forward stressed, weaker independent-month stressed, recent-selection stressed and lower budgeted DD.

The input is an independent physical 19-file copy totaling `34,559,535` bytes, including the four new qualified-anchor lifecycle/cache files and no mutable cross-family link. The qualified anchor is present only for calibration; it, the higher-profit near miss, all earlier candidates and the original 15 combinations remain closed without rerun. No MT5 launches during this proxy, and a valid result may nominate at most one new hypothesis.

## Current boundary

All selection, paired-forward, June, July, ranking and shortlist economics are unopened. Commit and push this declaration before the single proxy invocation.
