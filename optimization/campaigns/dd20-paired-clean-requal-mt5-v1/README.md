# DD20 Paired Clean-History Requalification MT5 V1

This declaration-only campaign repairs economic authority for the unchanged V8 paired-month candidate after the 2022-08 through 2026-06 binding Selection was invalidated by required-symbol real-tick generation fallback. It is not a new allocation search, a retune or a pristine out-of-sample claim.

## Frozen comparison

- Clean binding interval: `2024-01-01` through `2026-08-01`.
- One self-contained Tester binary derived once from the exact V8 Optimization baseline.
- Adjacent CONTROL: position risk `0.04`, aggregate cap `0.18`, six multipliers `1 / 1 / 1 / 1 / 1 / 1`.
- Adjacent CANDIDATE: exact V8 multipliers `2 / 1.5 / 2 / 2.5 / 1.5 / 0`, position risk `0.04`, aggregate cap `0.18`.
- Execution order: one complete valid CONTROL immediately followed by one complete valid CANDIDATE under exact pre/mid/post history and symbol-spec fingerprints.

Every path must stop normally, report MT5 `Every tick based on real ticks` at `100%`, and have zero required-symbol absent/discarded real-tick, price/volume mismatch or `every tick generation used` line in its own detailed agent segment. Any environment or engineering defect is corrected without an arbitrary retry cap and has no economic verdict.

## Requalification gate

The candidate must be positive actual and doubled-cost-stressed, exceed the control by at least `$100` in both measures, reach at least `2.0x` control actual and stressed net when the corresponding control measure is positive, improve stressed-net-to-native-equity-DD, keep native maximum relative equity DD at or below the disclosed pragmatic `21.2%` envelope, remain positive actual and stressed in calendar 2024, calendar 2025 and 2026 through July, keep all five active components positive actual and stressed in the pooled interval, produce zero Passive close and finish with zero safety, persistence, broker, foreign, protection, research-drop or pending-reconciliation fault.

The nominal `20.0%` line is reported separately and never hidden. A pass opens only a separately named Lab economic-handoff correction; it does not authorize Live. A valid nonconfirmation leaves V8 entries disabled and returns the serial research stream to whole-map family ranking. No weight, risk, cap, threshold, period or source rescue exists inside this campaign.

## Current boundary

Declaration commit `ed9df135940511ce8d2213dbde8ec82340ac25b5` reached `origin/main` before implementation. The exact active V8 package remains frozen independently at `optimization/baseline/NEXT-E02-V8-PMLR1-b1c77d3b6356/`: `20` files / `660,794` bytes / ordered manifest `7BCB5B9A5182E3ADE2AEA3954025BEFB9897FB685201200CAB04C5FCBB78ACDF`.

The self-contained campaign implementation is now frozen. Its `16` source files are `422,885` bytes at manifest `C7B5403A043C66E4D39F73741FCED3E5B481F8067FEDA251F1E366AECAFD03E3`; four SET/INI files are `3,511` bytes at `F5D1B5EC19811B52FF1D07446BF8B483F6FD9E5B9FADB677B8F2600D11BF7CD3`. Twelve of fifteen inherited Include modules remain byte-identical. Domain changes only campaign identity and mutable namespaces; Orders and Passive change only the order comment. The assembly changes namespace, accepts only the two declared tuples, rejects non-Tester execution, and emits read-only START/END contract and swap fingerprints for US30, US100 and US500. No signal, hold, exit, stop, protection, sizing, rounding, capital-ladder, margin, admission or aggregate-risk algorithm changed.

The first `0 errors / 0 warnings` compile revealed before Tester that the declaration-required contract/swap rows were absent from inherited V8 telemetry. It is preserved as a nonfinal implementation artifact. After that observation-only correction, the second and final MetaEditor build 6140 compile completed in `2,069 ms` at `0 errors / 0 warnings`; final EX5 is `230,600` bytes at `02C69200091D105C2AAB07CDD67240338F42203C9AF571133966E2C34E806A5B` and matches the runtime copy.

The dedicated non-Master `optimization/runtime/dd20-pcr1-portable/` owns only this campaign's Zeta source, SET and EX5 plus standard MT5 support. All `21/21` campaign source/configuration/EX5 overlays match. Copied donor TST, logs and EA output were moved recoverably out of the new runtime; active TST, Tester log, agent log, report and campaign output counts are zero, and the runtime has zero process owner. The pre-CONTROL binding environment contains `12` Tester HCS files plus `93` Tester and `93` Base TKC files at combined manifest `995AC8A37F6D91B2E9D97DEE1C31D0DB78ABE31D96E5B1EF1C707BDE171CA5E6`; the full symbol database is frozen separately.

## Correction-required closure

After implementation commit `f795bf59d14f1d7d6d9219544a5c256bc9ab50e5` reached `origin/main`, the first launch stopped before Tester because the intentionally lean runtime lacked saved account support. That prestart correction produced zero report, TST or economic output and is preserved separately. The corrected CONTROL then completed normally on `114,350,355` US30 ticks and `30,487` bars with Model 4, HTML `100% 실제 틱`, zero detailed required-symbol fallback line, exact START/END contract/swap rows and exact pre/post implementation, 198-file history and full-symbol fingerprints.

The path nevertheless violated its own frozen zero-data-unavailable gate: the byte-identical inherited V8 RC4 management source reported `138` successful checkpoints plus `12` aggregate unavailable attempts across `150` RC4 lifecycles. All other reported fault and pending counters were zero. V1 did not emit a per-attempt reason, so it cannot determine whether those 12 are missing input or exact-policy no-actions on complete but mathematically undefined windows. CANDIDATE was never started. Any money observed while discovering this final integrity failure is quarantined and has no success, failure, nonconfirmation, tuning or promotion authority.

V1 is closed `CORRECTION_REQUIRED_CONTROL_INVALID_DATA_UNAVAILABLE_NO_ECONOMIC_VERDICT_CANDIDATE_NOT_STARTED`. Its 17-file invalid CONTROL bundle is recoverably preserved at `30,229,899` bytes / `384EB8044FFAF43A7808B6EA5C6E28C6336ECCFF06E489D59F72FCFD6EC1DE09`; the active runtime again has zero report, TST, log or campaign output. Durable evidence is `evidence/DD20_PAIRED_CLEAN_REQUAL_MT5_CORRECTION_REQUIRED_V1.json`. The versioned successor `dd20-paired-clean-requal-mt5-v2` keeps the exact comparison and economic gates but adds reason-level read-only observability under a predeclared corrected validity contract.
