# DD20 legacy US500 H4 staged-overlay MT5 V1

This self-contained Optimization campaign consumes the sole exact seed from closed Lab Unit 098. Its parent is the qualified `dd20-component-equity-state-native-margin-mt5-v1` economic behavior, reconstructed from a one-time physical copy of the frozen Optimization baseline rather than copied or included from another campaign. The six-component anchor remains fixed at weights `1.6 / 0.8 / 0.4 / 3.2 / 1.2 / 0`, `$150` growth, `0.04 / 0.18` risk and the 40-close `0.10R`, `0 / 1 / 1.125` component-state overlays.

The only candidate change is exact `ZT-H4-US500-V2-VOLATILITY-EXP-b4d28831f9`: US500 H4, fixed `0.01`, 12-return signed volatility expansion, opposite-direction entry at `|state|>=1.5`, exit at `|state|<=0.6`, direction flip or 30 H4 bars, no initial SL or TP. New H4 entries are allowed only while actual account balance is at least `$500`; an existing H4 position is not closed merely because balance later falls below the gate. The original used-plus-required-margin gate remains `20%` of equity.

The overlay owns one separate Magic and position. It is outside the six-component planned-risk cap, component-state deques and three-slot accounting; it shares actual balance/equity/margin, price execution and every downstream account-state effect. The unchanged anchor event order completes before the H4 overlay is evaluated on the same dispatched tick.

Selection is one complete `2022-08-01..2026-06-01` 100%-real-tick run. Success requires actual net strictly above `+$1,693.46`, doubled-cost-stressed net strictly above `+$1,626.677`, all four epochs positive and native maximum relative equity drawdown at most `20%`. Only a complete selection pass may open one independently initialized `2026-06-01..2026-08-01` forward, where full June-July and each continuous month must have positive actual/stressed net and native DD at most `20%`.

There is no grid, parameter search, stop insertion, alternate stage gate, volume, H4 threshold, horizon, order, nearby legacy candidate or rescue. Missing history, compile/runtime/configuration faults and implementation defects are correction states with no economic verdict. The campaign has no Live or promotion authority.

## Closed result

The fixed candidate compiled once on MetaEditor build 6140 at `0 errors / 0 warnings`. One initial terminal observation ended before Strategy Tester started and produced no economic output; the unchanged candidate was relaunched in the same dedicated Portable and completed one valid `100%` real-tick selection with `45,160` bars and `132,257,500` ticks. The final run closed `1,294` core lifecycles plus `40` H4 overlay lifecycles, with `40 / 40 / 0` H4 entries, exits and remaining positions, normal tester shutdown and zero core, persistence, protection, research-drop or H4 fault.

Selection actual/stressed net is `+$1,736.50 / +$1,668.5262`, exceeding the current qualified anchor by `+$43.04 / +$41.8492`. All four epochs remain positive. The H4 overlay directly contributes `+$6.76 / +$6.1752`; shared account-state effects leave the core ledger `+$36.28 / +$35.674` above the current anchor despite one fewer core lifecycle.

Native maximum relative equity drawdown is `20.402008579%`, transparently `0.402008579` percentage points above the nominal `20%` line. That is a `2.010043%` proportional overshoot of the line, materially smaller than the tolerance scale in the user's explicit `10% → 10.6%..11.5%` example. The nominal gate is recorded false while the user-directed pragmatic tolerance and effective selection gates are recorded true.

The independently initialized `2026-06-01..2026-08-01` forward is `100%` real ticks and exactly retains the prior anchor at full actual/stressed `+$23.01 / +$21.256`, native relative equity DD `12.436759%`, June `+$19.79 / +$18.976` and July `+$3.22 / +$2.280`. Fresh balance never reached `$500`, so the H4 overlay remained `0 / 0 / 0` and the forward did not inherit selection state. The campaign closes `VALID_MT5_COMPLETE_USER_TOLERANCE_ACCEPTED_SELECTION_AND_FORWARD_PASS_PROFIT_UPLIFT`, becomes the qualified Optimization anchor under the updated user judgment, remains frozen without rescue or retuning, and grants no Live authority. Useful reports, caches, ledgers, state/event segments and logs are preserved under `optimization/artifacts/raw/dd20-legacy-us500-h4-staged-overlay-mt5-v1/`.

Immediately before the whole-unit push, six completed duplicate Portable roots (`4,649` files / `35,284,295,451` logical bytes) were converted to OneDrive online-only placeholders after their useful raw evidence was confirmed. This recovered local C: space from `17.913` to `50.731 GiB` while keeping the reusable native-gap copy source and every closed runtime cloud-recoverable.
