# DD20 V8 Signal-Strength Causal Ablation and Validation Proxy V1

This is mandatory Stage C inside `V8-OPT-U003-EXISTING-SIGNAL-STRENGTH-SUPPRESSION`, still the sole Program-1 Optimization stream.

## Immutable roles

The three robust Stage-B centers are fixed. Each owns its unchanged center plus five diagnostics that return exactly one component threshold to the exact-V8 native gate while leaving the other four unchanged. This yields `3 × 6 = 18` unique roles.

Ablations remain inside the exact-V8 opportunity universe: they can restore only opportunities exact V8 already emitted. They cannot add a strategy, signal, direction or timestamp. An ablation may reject an over-restricted center but can never replace, retune or advance in its place.

## Fresh validation

All 18 roles start from fresh `$100` for 2026 January-May. An immutable center must keep actual/stressed net and minimum balance positive, DD at or below `30%`, and at least `65%` of exact-V8 stressed net. It also fails if a one-component native-gate ablation weakly improves both stressed net and DD with at least one strict improvement.

Only unchanged centers that pass both economic and causal nondominance gates may enter Stage D. Locked June-July remains unopened. Stage C cannot close U003; if no center survives, mandatory Stage D records empty input and closes the Unit without an ablation, neighbor or threshold rescue.

## Current boundary

Declaration commit `906f7b0` reached `origin/main` before inputs and implementation. Exact V8 ledgers, complete Stage-B raw/durable results and five source semantics are staged byte-equal. Both Stage-B statuses and all three immutable center q/threshold vectors agree.

Structural precheck constructs exactly `18` unique roles at or above the native gates and reproduces exact V8 whole actual/stressed `+$409.81 / +$367.818`, DD `35.46%`, plus all three Stage-B development center records. Config/source are `7,059 / 50,978` bytes at `D2571494...A03E45 / F06816FA...C1C7BC`; freeze is `5,113` bytes / `B096D3AE...CE49BA`. The raw output is absent. One complete Stage-C process may run only after the implementation freeze reaches `origin/main`. January-May, locked June-July and MT5 remain unopened.

## Final Stage-C result

Freeze commit `fdc2d6e` reached `origin/main` before one complete `0.3369881s` process. All three immutable centers passed the frozen January-May economic gates. Ranks 1/2/3 earned stressed `+$109.0642 / +$97.7193 / +$115.7462`, retained `84.01% / 75.27% / 89.16%` of exact V8 and reduced closed-balance DD to `15.3920% / 9.15% / 15.3715%`.

All three nevertheless failed causal nondominance. Restoring Intraday to its native gate dominated ranks 1 and 3, adding stressed `+$8.0413` while lowering DD `1.6693` points or leaving it equal. Restoring RC64 dominated rank 2, adding stressed `+$16.628` at identical `9.15%` DD. Raw result is `73,085` bytes / `51471315...0A7729`; durable result is `7,669` bytes / `C51783B6...100230`. No ablation can replace a center, so Stage-D role count is zero. Locked June-July and MT5 remained unopened; mandatory Stage D records an empty boundary and closes U003 without component-specific or adjacent strength rescue.
