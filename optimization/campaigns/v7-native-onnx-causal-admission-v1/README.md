# V7 native causal admission

Sole Program 7 campaign. The frozen V7R remains the economic control. The new question is the causal effect of allowing an original market signal on later whole-account wealth, including actual native capital, risk, costs and other strategies.

Eight prospectively fixed full-2024 training trajectories randomize original market admission at probability .5. No favorable trajectory is selected. Every passed market signal records its pre-treatment context, probability and action, even if the original risk engine subsequently refuses a trade. Passive, signal definitions, original stop/management, order timing, quantities and risk caps remain unchanged.

The model fits a fixed ridge baseline and a propensity-corrected causal-effect head. Fresh ONNX MatMul runs the actual economic policy. STATIC allows positive predicted effects. ONLINE uses explicit .9/.1 exploration and updates its effect state only from completed 24-hour account labels available strictly before a new decision. Exploration losses remain part of its investment result. The declaration fixes all normalization, matrices, penalties, seeds, periods, gates and closure rules.

The baseline's stressed balance is realized only. This campaign adds a separate conservative account mark using actual owned positions, recorded entry costs and extra liquidation costs, with no positive swap credit. Quotes are the latest actually observed native values, and their age is recorded. This mark does not change the original core ledger or new-order freshness requirements. Native TesterStatistics, complete actual/stressed paths, lots, risk and financing determine economic results.

Each control/learner identity has its own physical Include root, Magic, schema and per-run state namespace. All code rejects non-Tester execution. Existing episode files are preserved and cause a fresh startup refusal. Every decision, pending label, RNG/posterior state, completed reward and learning update is durable. There is no shared Live/Lab/closed-family code, state, model, output or Portable.

`acquire_history.py` is the ordinary whole-history input producer; it never queries account, position, order or deal state. `fit_causal_model.py` consumes the complete declared native training matrix once. They are production/model tools, with no test-only mode or checker infrastructure.

Only complete unchanged full-2025 native selection can freeze one finalist. That same policy must then pass an adjacent unchanged full-2025 through August-2026 native pair. Training economics or a favorable forecast metric cannot establish improved compounding. No 2026 candidate data is opened during fitting or selection. No Live promotion is authorized.

Maintain 30 GiB free. Own runtime cap is 6 GiB and total training/selection/confirmation output cap is 2 GiB. Full used input hashes, symbol contracts/swaps, platform and source/model/SET hashes bind each native matrix. Preserve all exclusions; correct environmental or engineering faults without an arbitrary retry cap.

Implementation semantics: [OnnxRun](https://www.mql5.com/en/docs/onnx/onnxrun) and [OnnxCreateFromBuffer](https://www.mql5.com/en/docs/onnx/onnxcreatefrombuffer). These are official software references only, not economic inputs.

The native restart and path-length corrections are complete. Current mutable outputs use `<Role>/ca/<run-tag>` within this own Portable. Full 2024 control logging now completes without dropped records, but its native report remains 99% real ticks, including after canonical neutral-cache reconstruction. This does not pass the declared 100% requirement. The preserved result and proposed training-only exception are in `evidence/NATIVE_CONTROL_COMPLETE_TRAINING_QUALITY_PENDING_V1.json` and `evidence/TRAINING_HISTORY_EXCEPTION_PROPOSAL_V1.json`. No training proceeds unless that boundary is resolved; final economic gates remain unchanged.
