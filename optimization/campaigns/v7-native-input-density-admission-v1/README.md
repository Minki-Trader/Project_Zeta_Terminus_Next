# Existing V7 input-density admission

This sole Program 7 family learns the distribution of each existing V7 signal feature. It uses no trade-return or future-account target. The six original signal formulas, directions, clocks, quantities, stops and exits stay unchanged; a low-density passed signal may abstain under the frozen model rule. Static and strictly delayed online EM form one declared bundle.

All six original 2024 recorded-feature populations satisfy the fixed fit/calibration counts. The deterministic two-Gaussian model is fitted on January–June, with a fixed 95th-percentile density cutoff calibrated on July–December through the exported ONNX graph. Unknown pre-signal sentinels and monetary fields are excluded. The recorded population is not every calendar or potential opportunity.

The initial fit, graph and original-derived native implementation are complete. Compile-v4 produced clean binaries for all three identities. Both V3 controls and static ONNX completed full 2025 native runs; the online path stalled during initialization and has no economic result. Static reduced DD but lost wealth against the original control. The bundle remains open; see [the completed-path report](results/V3_COMPLETED_PATHS.md).

The CPU-only engineering correction and its small durable initialization/release ledger now compile cleanly for all three roles as compile-v5, after the previous owned agent finally exited and released its lock. Fresh v4 settings, complete history observations and the 768-file input freeze are complete. The new whole control/static/control/online bundle starts with cs-v4; no CPU economic result is claimed yet. The graph, fit, cutoffs, online learning rate and original trading rules remain fixed.

Every result and input artifact is preserved. Runtime 6 GiB, raw 2 GiB and source 32 MiB remain funded above a 30 GiB reserve. All Live-Dev and Lab remain untouched. Neither calibration nor the three completed paths demonstrate the requested compounding improvement.
