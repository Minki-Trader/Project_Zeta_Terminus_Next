# Existing V7 input-density admission

This sole Program 7 family learns the distribution of each existing V7 signal feature. It uses no trade-return or future-account target. The six original signal formulas, directions, clocks, quantities, stops and exits stay unchanged; a low-density passed signal may abstain under the frozen model rule. Static and strictly delayed online EM form one declared bundle.

All six original 2024 recorded-feature populations satisfy the fixed fit/calibration counts. The deterministic two-Gaussian model is fitted on January–June, with a fixed 95th-percentile density cutoff calibrated on July–December through the exported ONNX graph. Unknown pre-signal sentinels and monetary fields are excluded. The recorded population is not every calendar or potential opportunity.

The initial fit and graph are complete. Native implementation follows by a one-time original-baseline copy with separate tester-only identities and a physical owned Portable. The complete 2025 control/static/control/online matrix remains required; model calibration cannot establish profit, native DD or compounding. Every result and input artifact is preserved. Runtime 6 GiB, raw 2 GiB and source 32 MiB remain funded above a 30 GiB reserve. All Live-Dev and Lab remain untouched.
