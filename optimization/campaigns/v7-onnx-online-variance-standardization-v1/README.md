# V7 conditional variance standardization

Program 1 improves the original V7 entry scales for Cross, Return and Passive. It owns fresh ONNX static and online models fitted only on 2024 native prices; existing entry directions and clocks, every position-management rule, sizing and risk remain governed by the prospective declaration.

`evidence/DECLARATION_V1.json` owns the whole bundle and conditional native gates. `build_model.py` produces the attributed input slice and immutable 2024 models. `run_selection.py` produces the complete 2025 forecast and signal-supply comparison. Neither producer launches or modifies Live or reads broker account state.

This family has no economic result or Live promotion authority before complete adjacent native confirmation.
