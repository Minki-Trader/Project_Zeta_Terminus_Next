# DD20 Gain-Overlay Base-Quantum Reservation MT5 V1

This dedicated tester-only family evaluates the sole optimization seed frozen by Lab Frontier Unit 097. It keeps the current qualified component-equity-state anchor intact and changes only the admission capacity available to executable gain-overlay volume above the same-path neutral volume.

## Frozen role

- Base component weights remain `1.6 / 0.8 / 0.4 / 3.2 / 1.2 / 0`, with `$150` stressed-balance daily growth, `0.04` position risk and `0.18` aggregate risk.
- Component state remains lookback `40`, symmetric band `0.10R`, loss/neutral/gain overlays `0 / 1 / 1.125`, based on completed admitted lifecycle doubled-cost-stressed R.
- When gain-overlay executable volume exceeds neutral executable volume, only incremental volume steps are removed until one `0.04 × current risk capital` quantum remains below the `0.18` aggregate cap, or neutral volume is reached.
- Neutral volume is an immutable floor. If neutral itself leaves less than the reserve, the original first-come admission semantics decide the entry; the reserve never shrinks or suppresses base volume.
- Signals, clocks, directions, ordering, stops, exits, position risk, aggregate cap and fresh-process state are unchanged. There is no reserve grid, component exception, preemption, partial close or retune.

The candidate is derived once from the frozen optimization baseline `optimization/baseline/NEXT-E01-V7-RLO1-b32e7e176f2e/`, not from another campaign. It owns expert `ZetaDD20GOBQR1`, Include root `ZetaOptDD20GOBQR1`, release `OPT-LIVE-V7-DD20GOBQR1-20260829`, portfolio `ZT-OPT-LIVE-V7-DD20GOBQR1-20260829`, Magic `260829871..260829876`, mutable root `ZetaOptimization\dd20-gobqr1`, settings, reports and one dedicated physical Portable. It never uses the Master terminal and has no Live authority.

## Economic contract

The one fixed candidate may run once on `US30 M30`, Model `4` real ticks, `$100` USD and `1:100`, from 2022-08-01 through 2026-06-01. A complete valid selection must have no state, persistence, broker, protection or research-drop fault; actual and doubled-cost-stressed net must each strictly exceed the qualified current anchor `+$1,693.46 / +$1,626.677`; all four frozen epochs must remain actual/stressed positive; and native maximum relative equity DD must be at most `20%`.

Only if every selection gate passes may one fresh forward process run from 2026-06-01 through 2026-08-01. Full forward plus continuous June and July must each be actual/stressed positive and native forward DD must remain at most `20%`. Environment or engineering defects are correction states without an economic verdict or retry cap. A complete economic miss closes this exact candidate without rescue or retuning.

## Closed result

MetaEditor build 6140 compiled the candidate once with `0 errors, 0 warnings`. The first and only Tester invocation completed `132,257,500` real ticks, `1,295` lifecycles and a native report/cache in `852.10` seconds. It earned actual/stressed `+$1,692.45 / +$1,626.089`; all four epochs remained positive and native maximum relative equity DD passed at `19.550371765%`.

The reserve evaluated `37` executable gain-overlay opportunities, bound `9`, removed one lot step at each, and preserved the neutral floor once. At the identified 2026-05-15 interaction it reduced Intraday-R only from `0.36` to `0.35`; the remaining one-quantum headroom was still below the `1.2`-weighted Return requirement, so Return remained blocked and Cross remained admitted. The fixed mechanism therefore did not realize its fitted substitution benefit.

Selection actual/stressed profit missed the current anchor by `$1.01 / $0.588`. Both frozen profit gates fail, so no forward was opened and the current component-equity-state anchor remains authoritative. The durable classification is `VALID_MT5_COMPLETE_SELECTION_PROFIT_GATES_FAIL_NO_FORWARD_RETAIN_CURRENT_ANCHOR`. This is an economic rejection, not an implementation failure, and it creates no adjacent reserve-fraction or component-specific follow-up authority.

After hash-pinned preservation, the reproducible dedicated Portable (`754` files / `5,818,005,662` logical bytes) and inherited-source staging (`36` files / `1,302,477` bytes) were removed from the workspace to the Windows Recycle Bin. Tracked source/EX5 and the useful `20`-file raw artifact bundle remain.
