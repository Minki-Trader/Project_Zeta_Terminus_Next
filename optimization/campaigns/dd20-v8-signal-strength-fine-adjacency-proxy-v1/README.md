# DD20 V8 Signal-Strength Fine Adjacency Proxy V1

This is mandatory Stage B inside `V8-OPT-U003-EXISTING-SIGNAL-STRENGTH-SUPPRESSION`. It remains Macro Program 1 and the sole active Optimization judgment stream.

## Frozen question

Stage A found economically eligible component-relative gates but no coarse point with the required neighbor support. Stage B asks whether the three frozen separated centers sit inside a stable local surface when each component quantile moves by `-0.10 / 0 / +0.10`, clipped to `0..0.60`.

The centers are not winners. Their local role counts are `243 / 162 / 243`, exactly `648` total and duplicate-free in quantile space. Thresholds are reconstructed outcome-free from the same frozen 2024-2025 signal-passed feature distributions. Every role keeps all five strategies, formulas, directions, timestamps, weights `2 / 1.5 / 2 / 2.5 / 1.5`, position risk `0.04` and cap `0.18`; it can only suppress exact-V8 births.

## Gates and succession

Development common, primary and fallback economic gates are unchanged. A robust fine point needs at least four eligible immediate local neighbors spanning three component axes in one center provenance. At most three separated points advance. If robustness is again empty, the declared normalized near-miss path preserves mandatory Stage C depth without claiming confirmation.

Stage B opens neither January-May validation nor locked June-July and cannot close U003. No MT5 path is authorized. Stage C remains mandatory and may accept only immutable Stage-B centers; no new entry, Lab, Live change or external data is permitted.

## Current boundary

Declaration commit `bc77471` reached `origin/main` before inputs and implementation. Exact V8 ledgers, complete Stage-A raw/durable results and five source semantics are staged byte-equal. Stage-A statuses and all three center q/threshold vectors agree.

Outcome-free precheck reproduces exact center counts `243 / 162 / 243`, total `648` raw/unique roles, zero duplicates and edges `810 / 513 / 810 = 2,133`; every fine gate remains at or above native. Exact V8 neutral again reproduces accepted `965`, actual/stressed `+$409.81 / +$367.818` and DD `35.46%`. Config/source are `7,074 / 58,409` bytes at `549F5321...AEED59 / 427D0B8E...F458F8`; freeze is `5,020` bytes / `7DCE2EB8...A7D86`. Raw output is absent. One fine-map process may run only after this freeze reaches `origin/main`; validation, holdout and MT5 remain unopened.

## Final Stage-B result

Freeze commit `e03ca81` reached `origin/main` before one complete `0.5116846s` process. The fine map contains `212` four-half-positive, `77` primary, `134` fallback and `39` robust-primary points. Rank 1 q-vector `0.40 / 0.50 / 0.25 / 0.10 / 0.15` earns development actual/stressed `+$128.55 / +$113.306`, DD `20.3128%`, weakest-half `+$18.032` and has six eligible neighbors across all five axes. Ranks 2/3 earn stressed `+$113.871 / +$108.527` with five/four eligible neighbors.

Raw result is `59,794` bytes / `B0A8EBA9...3B1E62`; durable result is `6,179` bytes / `5B3C5E84...CE3218`. Fine adjacency confirms a stable development basin but does not open or read January-May. All three centers advance unchanged to mandatory Stage C; validation, locked holdout and MT5 remain unopened.
