# DD20 Paired Anchor August 2026 MT5 V1

This isolated Optimization campaign asks one bounded confirmation question about the already fixed replacement-development candidate `dd20-paired-month-stability-mt5-v1`: how did its unchanged native portfolio behave from the start of August through the last completed U.S. cash session available before declaration?

## Frozen attribution

- Source parent: the byte-pinned Optimization baseline `NEXT-E01-V7-RLO1-b32e7e176f2e`, copied once into this campaign before any execution.
- Economic transformation: the already native-confirmed executable-volume and protective-stop treatment with component multipliers `2.0 / 1.5 / 2.0 / 2.5 / 1.5 / 0.0`, position risk `0.04`, aggregate risk `0.18` and unchanged global admission, margin, capital-ladder, stop, signal, hold and exit rules.
- Fixed reference economics: selection actual/stressed net `+$5,786.63 / +$5,477.524` at `20.2568876%` relative equity DD; June-July actual/stressed net `+$32.74 / +$30.626` at `18.6753021%` DD.
- Identity: release `OPT-LIVE-V7-DD20PAAUG1-20260830`, portfolio `ZT-OPT-LIVE-V7-DD20PAAUG1-20260830`, Magic `260830831..260830836`, and campaign-owned state, event, research, source, settings, report and runtime paths.

The implementation is the fixed candidate economic patch applied to the frozen baseline under a new namespace. It does not include, link or execute Live, Lab or another Optimization campaign. The zero-weight Passive component remains disabled. This is not a weight, risk, threshold, date or formula search.

## One native path

The sole configuration runs Model `4` real ticks from `2026-08-01` through the exclusive boundary `2026-08-29`, covering the completed August 3-28 market sessions. It runs once in `optimization/runtime/dd20-paired-anchor-august-2026-mt5-v1-portable/`. Any incomplete required-symbol history, symbol/spec drift, generated-tick warning, abnormal stop, missing report/ledger, source/config/runtime error or EA fault is an engineering correction state without an economic verdict or retry cap.

Economic output is read only after a complete valid path. Actual and doubled-observed-cost-stressed net, native balance/equity DD, closed-lifecycle breadth and concentration, risk-admission skips, stop exits and component contributions are retained. The nominal DD line is `20%`; the already adopted pragmatic envelope is disclosed separately at `21.2%`. Positive actual and stressed net with DD inside the pragmatic envelope supports the fixed candidate; otherwise the exact interval nonconfirms it. Either outcome is economic information, not authority for a retune.

August 31 is not in the interval. Therefore even a valid result is a bounded near-month confirmation, not a completed-August claim, pristine project-wide OOS claim, or Live promotion authority. The fixed candidate was selected without August economics, but other project research has observed portions of August, so both facts remain explicit.

## Current boundary

The campaign is closed `VALID_FIXED_PAIRED_ANCHOR_AUGUST_THROUGH_28_SUPPORT`. After one invalid missing-report-directory environment preflight received no economic verdict, the unchanged path completed at `100%` real ticks through server `2026-08-28 23:59:58` with a normal stop and zero EA fault.

Actual/doubled-observed-cost-stressed net is `+$29.62 / +$28.586` across `29` closes. Native maximum relative equity drawdown is `13.44%`, passing both the nominal `20.0%` line and separately disclosed practical `21.2%` envelope. All five active components, US30/US100 and long/short were positive over the full interval; the largest component supplied `32.52%` of stressed net.

The support is bounded. The second half remained positive at `+$3.294` stressed, but the last week lost `-$1.118`, `88.48%` of stressed net came from the first half, and the aggregate-risk cap blocked `8/37` actionable admission attempts, including six Cross opportunities. Only `29` closes exist. August 31 remains excluded and broader project work had already observed portions of August, so this is neither completed-August nor pristine project-wide OOS evidence. It strengthens the fixed candidate without retuning it and has no Live promotion authority.
