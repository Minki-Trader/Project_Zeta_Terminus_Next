# DD20 V8 Component-Relative Signal-Strength Suppression Proxy V1

This is Stage A and the declaration root of `V8-OPT-U003-EXISTING-SIGNAL-STRENGTH-SUPPRESSION`, the sole active Optimization Unit under Macro Program 1. It opened only after U002 was closed, Programs 1-5 and 7 were re-compared, and that comparison reached `origin/main`.

## Question and boundary

The Unit asks whether stricter use of each existing V8 strategy's own logged signal strength can refuse weak births, repair early temporal loss and reduce drawdown without destroying whole and recent value. It keeps RC61, RC64, Cross, Intraday and Return active at the exact V8 weights, `0.04` position risk and `0.18` aggregate cap.

Every candidate threshold equals or exceeds the strategy's native gate. A candidate may therefore suppress an exact-V8 signal-passed birth but can never add a symbol, timeframe, signal family, direction or opportunity timestamp. Entry loosening and newly manufactured opportunities are Lab research and remain unopened.

## Outcome-free threshold construction

Development signal-passed rows from 2024-2025 define one normalized strength distribution per component. Quantile zero means the exact native gate. Positive quantiles use the deterministic nearest-rank feature value and keep values equal to the threshold. Threshold construction reads no lifecycle outcome.

The complete Stage-A map uses quantile coordinates `0 / 0.25 / 0.50` across all five components: `3^5 = 243` raw roles including the exact-V8 neutral. This prevents selecting only RC61 or Cross from descriptive hindsight. All roles and thresholds are frozen as one bundle before economic evaluation.

## Frozen deep sequence

1. Stage A evaluates the complete 243-role coarse map on four development half-years and selects at most three robust or explicitly ranked near-miss centers without opening validation or holdout.
2. Stage B exhausts center-local `±0.10` quantile adjacency, at most `729` raw roles, under the same development gates and neighbor robustness.
3. Stage C keeps each selected center immutable, checks its January-May economics and compares diagnostic one-component native-gate ablations. Ablations can reject an over-restricted center but cannot replace or retune it.
4. Stage D requires every immutable role to pass six independent actual/stressed blocks, including locked June-July, plus whole stressed-retention and DD-improvement gates.
5. Stage E is conditional on one unchanged Stage-D winner and permits at most the exact contemporaneous V8 control and one finalist in native MT5.

Unknown capacity-freed blocked opportunities always receive zero proxy profit credit. Stages A and B cannot close the Unit. Engineering, design or environment faults are corrected without becoming economic failures. No adjacent threshold, component, subgroup, direction, window or retained-seed rescue opens after closure; Programs 1-5 and 7 must be re-compared first.

## Current boundary

Declaration commit `164061c` reached `origin/main` before input staging and implementation. The two exact V8 ledgers and five feature-source semantics are byte-equal in the isolated raw input root. Outcome-free precheck joins all `965` lifecycles, observes `1,108` signal-passed rows, builds exactly `243` unique threshold roles with `810` adjacency edges and proves every threshold is at or above native.

The exact-neutral precheck reproduces accepted `965`, actual/stressed `+$409.81 / +$367.818`, DD `35.46%` and zero strength suppression or aggregate skip. Config is `5,583` bytes / `83837272...014AF`; source is `52,110` bytes, `1,178` lines / `CCA820B7...BB855`; implementation freeze is `5,790` bytes / `4370C71A...4AAC4`. Raw candidate output is absent. One complete Stage-A process may run only after the freeze reaches `origin/main`; validation, locked holdout, MT5, Lab, Live and external data remain unopened.
