# DD20 paired-month component-state combination proxy V1

Status: closed `VALID_PROXY_COMPLETE_NO_MT5_SHORTLIST_COMPONENT_STATE_COMBINATION_NONCONFIRMATION`.

This source-free campaign combines two already-complete Optimization structures exactly once: the current paired-month anchor weights `2 / 1.5 / 2 / 2.5 / 1.5 / 0` and the previously native-qualified component-local state role `40 closes / symmetric 0.10R band / loss 0 / neutral 1 / gain 1.125`. It does not search either structure, alter a signal or reopen either closed family.

## Whole-map selection

The complete map was compared after Unit 100 closed with no seed. Entry strength, market-state routing, external/event context, order/session mechanics, stops, profit-side exits, account-level drawdown governors, static composition, risk/capital ladders, US500 H4, frequency lanes, gain reservation and causal label replacement are closed. The sole ready nonadjacent interaction is that the component-state mechanism passed native economics on the earlier lower-profit anchor, while the later pragmatic recomparison independently made the paired-month portfolio the current anchor. Their exact combination has never been measured.

No Lab unit is needed because this is a direct economic combination of two frozen Optimization structures, not a new explanatory hypothesis. No sub-agent is used because one serial judgment stream owns the campaign.

## Fixed proxy

- Selection reads only a one-time campaign-owned copy of the paired-month selection lifecycle ledger. Forward reads only its independently initialized June/July lifecycle copy.
- Component state is fresh and local to each segment. Only admitted completed stressed net divided by admitted planned risk enters the component's 40-close deque.
- Until 40 admitted closes, and while mean stressed R is within `[-0.10,+0.10]`, exposure is neutral. Below `-0.10R` it is zero; above `+0.10R` it is multiplied by `1.125`.
- Daily `$150` stressed-balance steps, executable `0.01`-lot rounding, `0.04 / 0.18` admission, original opportunities and natural exits remain fixed. A candidate skip creates no synthetic replacement.
- The current Live-Dev RLO1 candidate/lifecycle ledgers are copied once only as a no-retune boundary. Their three complete closes are far below every 40-close component warmup, so the combined policy must remain neutral on all currently observed Live rows.

The sole candidate is eligible for one later non-Master MT5 selection only if it has at least 25 non-neutral executable decisions across at least three active components, keeps actual/stressed capital and all four epochs positive, and conservatively beats the exact anchor after subtracting the prior observed component-state proxy-to-native incremental errors `$85.3873452381 / $84.9166309524` plus a new `$10` combination reserve. Nominal `20%` DD is reported separately. Proxy eligibility uses the already-established `6%` proportional screening ceiling (`21.2%`) after native-anchor, prior component-state DD and combination reserves; only complete native MT5 numbers can make a final DD judgment.

Fresh forward must remain exactly the current anchor because no component reaches 40 closes. At most one exact MT5 shortlist may result. No grid, adjacent lookback/band/overlay, component exception, seed, Live change or automatic promotion exists.

## Result

Two pre-output Python literal errors and one forward-gate design mismatch were corrected without an economic verdict. The design mismatch had incorrectly required the already-accepted marginal July slice to be positive; the valid implementation instead requires exact candidate/control equality for full, June and July, positive capital and no state/path divergence. The final single process completed in `0.0994514` seconds with all input, selection-control and forward-control checks passing.

The exact combination made `64` non-neutral executable decisions across all five active components. All `71` fitted state evaluations were gain states, but the larger volumes displaced nine source admissions, all visible in the Cross count `660 → 651`. Gross selection actual/stressed fell from `+$5,786.63 / +$5,477.524` to `+$5,599.628922 / +$5,298.288581`, deltas `-$187.001078 / -$179.235419`. After the predeclared prior native-error and `$10` reserves, conservative deltas are `-$282.388423 / -$274.152050`.

Every epoch and capital path stayed positive, but raw closed-balance DD worsened from `20.401029%` to `21.235792%`. Budgeted DD is `21.732774%`, an `8.66387%` proportional overshoot of the nominal line and above the frozen `6%` proxy ceiling. Both profit gates and the DD gate fail.

The independent forward remains exactly `+$32.74 / +$30.626`; June is `+$33.33 / +$32.109`, July remains the disclosed `-$0.59 / -$1.483`, and the state makes zero non-neutral decisions. The current Live-Dev snapshot likewise remains in warmup with only one close per observed component and no research drop. No MT5 shortlist opens. The exact combination and all adjacent state/admission rescues are frozen; the paired-month anchor remains qualified pending another full-map comparison.
