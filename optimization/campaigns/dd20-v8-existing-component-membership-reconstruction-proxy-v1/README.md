# DD20 V8 Existing-Component Membership Reconstruction Proxy V1

This is Stage A and the declaration root of `V8-OPT-U002-MEMBERSHIP-RECONSTRUCTION`, the sole active Optimization Unit under Macro Program 5. It was selected only after the completed Unit-001 boundary reached `origin/main` and Programs 1-5 and 7 were re-compared from exact V8 and completed Unit outputs.

## Question and boundary

The Unit asks whether a complete reconstruction of the existing five-component V8 membership can reduce temporal loss concentration and closed-balance drawdown while retaining stressed value. It is not a one-off component deletion. Each of the three frozen Unit-001 static seeds receives all `31` nonempty membership masks; `93` raw seed-mask roles deduplicate to exactly `79` executable weight vectors.

Optimization is not limited to parameters. The ordered Unit includes static strategy removal/reconstruction, executable reallocation inside a frozen membership and a component-local causal loss-suspension mechanism. Those changes may require isolated Optimization source code. They may only suppress or resize opportunities exact V8 already emits.

A new symbol, timeframe, signal family, direction or opportunity timestamp, including entry-condition loosening that manufactures an opportunity, is new-entry Lab research and is not opened or implemented. No separate Lab research or engineering runs during this Unit.

## Conservative executable model

Stages A-C inherit Unit-001's validated exact-V8 daily volume, lot rounding, conservative capital, position-budget and aggregate-admission replay at base position risk `0.04` and aggregate fraction `0.12`. Disabled members have weight zero. The proxy may subtract or resize known accepted lifecycles but assigns zero profit to the `143` observed existing-V8 opportunities whose source run was blocked by aggregate risk. Only one final native pair may establish freed-capacity economics, open-equity DD and stop quantization.

## Frozen deep sequence

1. Stage A maps all `79` unique membership vectors on development only and selects robust or explicitly ranked near-miss centers without opening validation or holdout.
2. Stage B applies `-0.25 / 0 / +0.25` executable weight adjustments only to active members, keeping inactive members zero, aggregate risk `0.12` and position risk `0.04`. It completes even when Stage A has only near misses.
3. Stage C compares each static seed with component-local causal suspension after `2 / 3 / 4` consecutive admitted losing closes for the next `2 / 4 / 8` same-component source births. It opens January-May validation for at most three immutable development roles; the neutral static role remains eligible when the causal mechanism does not improve it.
4. Stage D judges immutable validation roles on six fresh clean-state half-year/forward blocks and whole 2024-July-2026 continuity under actual and doubled-observed-cost stressed net. June-July is the locked holdout and cannot select an alternate.
5. Stage E is conditional on one unchanged Stage-D winner and permits at most two valid native paths: the exact contemporaneous V8-derived control and that finalist.

Stages A and B cannot close the Unit. A correction to environment, design or implementation is not an economic failure. U002 closes only after its declared layers are exhausted and then requires a fresh Programs 1-5/7 comparison; no adjacent rescue or successor opens automatically.

## Current boundary

The declaration reached `origin/main` at `7b3ff51`. During authorized input staging, the expanded full SHA for the durable Stage-B interpretation was found to contain a transcription error even though its path, byte size and abbreviated identity named the intended file. `DD20_V8_EXISTING_COMPONENT_MEMBERSHIP_RECONSTRUCTION_UNIT_002_DECLARATION_INPUT_HASH_CORRECTION_V1.json` replaces only that full hash with `D569A72E9647287D165C341FEDCA62D15FAFF89CF3FF0874F584285E591A74E4`.

The correction is pre-implementation and pre-outcome. Four input copies exist byte-equal, and an unchanged Unit-001 proxy template was copied without a config, but no U002 logic, executable configuration, result or economic run exists. Implementation may resume only after the correction reaches `origin/main`. No MT5, Lab, new-entry, Live-package, Live-runtime or order-authority change occurred.

## Frozen Stage-A implementation

Correction commit `7fac0b5` reached `origin/main` before implementation resumed. The campaign now owns a corrected self-contained config and proxy. Execution aborts before output if either declaration authority, any of four staged inputs, the raw/durable Stage-B seed agreement, the `965`-birth join, the `93 → 79` enumeration or exact V8/static-seed controls drift.

The structural precheck produced `93` raw roles, `79` unique roles, `14` duplicates, all `31` nonempty masks and `207` unique-role adjacency edges. Exact V8 reproduced `+$409.81 / +$367.818 / 35.46%`, and every static seed reproduced development `+$150.105 / +$133.103 / 24.099813585%`. Candidate membership economics were not executed. Config is `5,423` bytes / `39DB7375...BA155`; source is `46,688` bytes / `F22C7C7B...713D0` and passes Python `3.13.9` syntax with NumPy `2.3.4`.

No raw result exists. The frozen candidate map may execute once only after the implementation-freeze boundary reaches `origin/main`; validation, locked holdout, MT5, Lab/new-entry and Live remain unopened.

## Execution-key correction

After freeze commit `88255fe` reached `origin/main`, the first process stopped before result serialization with `KeyError: maximum_valid_economic_paths`. The config correctly freezes `maximum_valid_economic_paths_after_all_proxy_stages`; only the result-metadata source reference retained the shorter old key. No output or economic summary exists. Although candidate arrays were calculated in memory before metadata construction failed, no metric or rank was exposed and no economic verdict is valid.

`DD20_V8_EXISTING_COMPONENT_MEMBERSHIP_RECONSTRUCTION_PROXY_V1_EXECUTION_KEY_CORRECTION_DECLARATION.json` permits exactly one source-token replacement and no config, input, lattice, gate, period or selection change. The declaration must reach `origin/main`, followed by a corrected implementation freeze, before one clean replay.

Correction declaration commit `b578f13` reached `origin/main` before the source changed. Git records exactly one removed and one added line: only `maximum_valid_economic_paths` became `maximum_valid_economic_paths_after_all_proxy_stages`. Config remains `39DB7375...BA155`; corrected source is `46,711` bytes / `86FD7AFE...FF46E` and passes the unchanged structural/control precheck. No candidate map was re-executed and no result exists. One clean replay waits for the corrected implementation freeze to reach `origin/main`.
