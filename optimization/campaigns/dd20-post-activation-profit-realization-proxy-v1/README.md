# DD20 Post-Activation Profit Realization Proxy V1

This proxy follows the valid empty permanent terminal-lock frontier without rerunning any closed path. The rejected accelerator's weights and causal `$150 → $50 after +$450`, cap-`14` schedule remain fixed. The new mechanism realizes profit on one position at a declared R multiple after the accelerator has armed, then continues the already observed later entry path instead of terminating the portfolio.

## Why this is distinct

The permanent high-watermark lock made every candidate DD-safe but its best `14.5%..16.5%` lock earned only `+$1,428.82 / +$1,373.40`, because it ended before the profitable recovery after March 2026. In the rejected accelerator's already completed MT5 lifecycle, the `458` positions entered after activation closed at actual/stressed `+$1,699.34 / +$1,636.1475`, while many positions surrendered marked profit before their original exit. A position-level realization target can cut that giveback without shutting down later opportunities.

This is not a terminal-lock reset, new high-watermark threshold, risk-cap, accelerator-step or component-weight variation. It changes only the exit of a post-activation position whose recorded peak proves the target was reached.

## Frozen proxy

- Candidate profit targets are exactly `0.05R..2.45R` by `0.05R`, or `49` new values. The maximum observed post-activation peak is `2.488840R`, so every candidate must bind at least once.
- The rule latches only when the fixed doubled-cost-stressed closed balance first reaches `$550`, observed at `2025.04.23 17:30:00`. Only positions entered at or after that time are eligible. The fresh forward never arms because its maximum stressed closed balance is `$129.946`.
- When `peak_mark_r` reaches a candidate target, the counterfactual exit is recorded at `peak_time_server`. This is no earlier than the unknown first crossing and therefore delays realization rather than granting earlier balance capacity.
- Counterfactual actual net is target R times the frozen planned risk, less `max($0.25, 2.5% of planned risk)` per realization. Stressed net subtracts the same lifecycle's observed nonnegative actual-minus-doubled-cost cost drag.
- Every later birth already present on the accelerated MT5 path is retained with frozen volume and risk. Freed capacity creates no synthetic births. The mechanism never permanently stops the portfolio.
- The already completed accelerator MT5 selection/forward, terminal-lock and risk-ceiling grids, qualified MT5 anchor, all prior candidates and the original 15 combinations remain closed and are not rerun.

## Economic gates

Selection actual and stressed net each subtract a further `$100` global path/implementation uncertainty reserve, then both must strictly exceed the qualified observed MT5 anchor `+$1,691.54 / +$1,626.26`. All four modified chronological epochs and both modified closed-balance paths must remain positive.

Budgeted native DD is the larger of:

- qualified native DD `19.550372%` plus `0.25` points; and
- the worse actual/stressed modified closed-balance DD plus the rejected accelerator's observed native-minus-raw gap `5.126601` points plus a `0.5`-point realization-timing/path reserve.

Budgeted DD must remain at or below the hard `20%` limit. The sole role, if any, maximizes conservative stressed selection profit, then conservative actual profit and recent-epoch stressed profit, before preferring lower budgeted DD and the larger R target.

All `49` candidates must preserve the exact unarmed forward at actual/stressed `+$23.01 / +$21.256`, native DD `12.436759%`, June `+$19.79 / +$18.976`, and July `+$3.22 / +$2.280`. No MT5 may launch during this proxy, and at most one role may be shortlisted afterward.

## Declared boundary

All `49` candidate economics are unopened. The copied raw input contains `8` files / `4,083,775` bytes with canonical manifest `5E00A8A048A13088F111D7608F0FDB0BB54D61C0BB06A31C7C95DD9BA8E70D80`. Freeze and push this declaration before exactly one proxy process opens the candidate frontier.
