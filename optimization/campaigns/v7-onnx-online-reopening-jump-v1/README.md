# V7 reopening-jump learning

One Program 1 Optimization family, derived only from the frozen V7R parent. Replace Passive with a US30 first-observed reopening jump response and preserve five original strategies and shared-account risk limits. This event is distinct from original scheduled signals and continuous activity buckets.

The declaration fixes all features, event/entry clocks, paired FADE/FOLLOW first-passage labels, previous20date scale, symmetric price barrier, four-hour deadline and full static/online bundle. A new six-input, eight-hidden-unit Tanh ONNX predicts both cost-stressed action outcomes. All eligible events produce both causal shadow labels regardless actual choice; no accepted-trade-only training population is substituted.

`jump_model.py prepare` makes an attributable own timestamp-only original copy through2025. `jump_model.py run` performs the one fixed2024 training and entire2025 preliminary quote selection. It does not access a broker or execute a terminal. ONNX inference uses float32; ordinary model fitting and fixed-step SGD use float64. Frozen source/input hashes precede production.

M1 side-quote accounting, conservative barrier ordering and fixed0.01 volume cannot establish actual native capital, intraminute order, financing, fees, equity DD or compounding. Only a sole frozen selection survivor can open2026 candidate values and a complete unchanged native control/candidate pair. All five original engines and4%/12%/45% caps remain; desired strategy barrier must fit native protection legality at the original daily lot, or entry is refused.

Keep30GiBfree. No external economic data, closed-candidate source/model/state, Live/Lab writes, tests/checkers or adjacent rescue after closure.
