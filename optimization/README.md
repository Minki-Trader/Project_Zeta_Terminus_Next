# Independent Live-Dev Optimization

This root owns continuous economic optimization of the exact active Live-Dev behavior. It is physically and logically separate from both `live-dev/` and `lab/`.

## Fixed parent

- Source release: `NEXT-E02-V8-PMLR1-b1c77d3b6356`
- Source root at derivation: `live-dev/package/active/`
- Local frozen copy: `baseline/NEXT-E02-V8-PMLR1-b1c77d3b6356/`
- Source commit at derivation: `9e0e2d4fe5c8b553216dd801d51ba2e130a5c6e0`
- Frozen manifest: `7BCB5B9A5182E3ADE2AEA3954025BEFB9897FB685201200CAB04C5FCBB78ACDF`
- Status: exact 20-file V8 baseline frozen at `660,794` bytes with zero mismatch and zero filesystem link; the prior V7 baseline remains immutable historical derivation material

The local baseline is never compiled or executed with Live identity. It is copied once so later campaign work no longer depends on a Live or Lab path.

## Root roles

- `baseline/`: byte-pinned, read-only copy of the active Live package used only for campaign derivation.
- `campaigns/<family>/`: one self-contained active or frozen optimization campaign, including source, configuration and small durable evidence.
- `runtime/<family>-portable/`: Git-ignored dedicated physical MT5 Portable; never the Master, Live or Lab terminal.
- `artifacts/raw/<family>/`: Git-ignored large optimization reports, logs and caches that durable evidence may pin and preserve.
- `artifacts/temp/`: replaceable staging only.

## Operating rules

Only one optimization judgment stream is active. A complete valid economic comparison may judge improvement; missing history, runtime/configuration failure, design defect or engineering defect is corrected and rerun without an arbitrary retry cap and without an economic verdict. An HTML 100-percent-real-ticks label is insufficient when the detailed agent segment reports required-symbol absent/discarded ticks or generation fallback. The latest two completed months remain isolated from search by default.

Optimization candidates are not Live EA edits and may be economically radical. Work is not limited to parameter changes: a campaign may change isolated source code and mechanisms, remove or disable existing V8 strategies, reconstruct membership from existing V8 components, and alter admission, collision, priority, sizing, risk, hold, protection or exit behavior. It may suppress, reorder or resize only opportunities exact V8 already emits. Adding a new symbol, timeframe, signal family, direction or opportunity timestamp—or loosening entry logic to manufacture one—is new-entry Lab research and is not opened or implemented under the active Optimization-only direction.

One completed campaign does not pause the Goal. Under the active exact-V8 deep-optimization direction, V8 is its own economic parent and only exact-V8 code, clean V8 ledgers/results, naturally accruing V8 research logs and prospective outputs of the active V8 Optimization Unit may select or tune work. V7 and historical Lab economics are lineage context only. Within the allowed existing-opportunity boundary, the agent chooses parameter, code, mechanism, removal and reconstruction axes autonomously from V8 evidence. No separate Lab research or engineering Unit opens while this scoped program is active.

No optimization result changes Live automatically. Any later promotion requires explicit user authorization and a separately named Lab engineering handoff under `docs/OPERATING_DIRECTION.md`.

## Active campaign

`V8-OPT-U003-EXISTING-SIGNAL-STRENGTH-SUPPRESSION` is the sole active Optimization Unit under Program 1. It opened only after post-U002 recompare commit `9eafe6f` reached `origin/main`. Its pre-input declaration freezes `243` complete all-five coarse roles, up to `729` center-local fine roles, immutable one-component causal ablation/validation, six-block temporal/cost stability and at most one native control/finalist pair. Declaration `DD20_V8_COMPONENT_RELATIVE_SIGNAL_STRENGTH_SUPPRESSION_UNIT_003_DECLARATION_V1.json` is `17,130` bytes / `615BCB7A...97E4CB`. No inputs, thresholds, config, source, result or MT5 path open until this declaration reaches `origin/main`.

Stage-A implementation is frozen after declaration commit `164061c` reached `origin/main`. Two exact V8 ledgers and five source-semantic files are byte-equal in the isolated raw input root. Outcome-free precheck passes `1,108` signal-passed rows, component development populations `151 / 121 / 420 / 55 / 109`, exactly `243` unique roles, `810` adjacency edges and zero gate below native. The neutral role reproduces `965` accepted births, actual/stressed `+$409.81 / +$367.818` and DD `35.46%`. Config/source are `5,583 / 52,110` bytes at `83837272...014AF / CCA820B7...BB855`; freeze is `5,790` bytes / `4370C71A...4AAC4`. Candidate-map output remains absent until this boundary reaches `origin/main`.

Stage A is valid complete after freeze commit `1d4c4f2`. Among `243` roles, `30` make all four development half-years positive, `12` pass primary and `21` pass fallback, but zero primary point has four eligible neighbors spanning three axes. Three separated declared near-miss centers advance to Stage B. Rank 1 q-vector `0.50/0.50/0.25/0.50/0.50` earns actual/stressed `+$115.27 / +$102.107`, DD `21.5249%` and weakest-half stressed `+$6.616`; ranks 2/3 earn stressed `+$83.396 / +$106.811` at DD `24.76%`. Raw is `57,187` bytes / `8A36AA2D...ECD3E`; durable result is `6,633` bytes / `E9ABF1D3...F0E496`. Validation, holdout and MT5 remain unopened; mandatory fine adjacency follows.

Mandatory Stage B `campaigns/dd20-v8-signal-strength-fine-adjacency-proxy-v1/` is declared after Stage-A result commit `d7894ed` reached `origin/main`. Local `±0.10` quantile sets around the three immutable centers produce exact counts `243 / 162 / 243 = 648`, expected duplicate-free, with `2,133` center-local edges. Economic gates and four-neighbor/three-axis robustness are unchanged; Stage B cannot close U003 and opens no validation, holdout or MT5. Declaration is `8,200` bytes / `F55F5A7F...7D3600`.

Stage-B implementation is frozen after declaration commit `bc77471` reached `origin/main`. Four main inputs and five source semantics are byte-equal; Stage-A raw/durable statuses and all three centers agree. Precheck reproduces `648` raw/unique roles, zero duplicates, center edges `810 / 513 / 810`, all gates at or above native and exact V8 `+$409.81 / +$367.818 / 35.46%`. Config/source are `7,074 / 58,409` bytes at `549F5321...AEED59 / 427D0B8E...F458F8`; freeze is `5,020` bytes / `7DCE2EB8...A7D86`. Fine economics/output, validation, holdout and MT5 remain absent pre-origin.

U002 may autonomously change isolated Optimization code and mechanisms, remove existing strategies and reconstruct the portfolio, but it may only suppress or resize exact-V8 opportunities. New symbols, timeframes, signal families, directions, opportunity timestamps or entry loosening are new-entry Lab research and remain prohibited. The `143` observed aggregate-blocked existing opportunities have no outcomes and receive zero proxy profit credit.

Authorized post-declaration input staging exposed one declaration transcription fault before implementation or outcome: the `6,804`-byte durable Stage-B result's correct full SHA is `D569A72E9647287D165C341FEDCA62D15FAFF89CF3FF0874F584285E591A74E4`, not the expanded value written in `frozen_future_inputs[3]`. `DD20_V8_EXISTING_COMPONENT_MEMBERSHIP_RECONSTRUCTION_UNIT_002_DECLARATION_INPUT_HASH_CORRECTION_V1.json`, `3,386` bytes / `9E3F1FAD...3C165`, supersedes only that field. Four inputs are byte-equal; no U002 config, logic, output or economic run opens before the correction reaches `origin/main`.

Stage-A implementation is frozen pre-outcome after correction commit `7fac0b5` reached `origin/main`. The self-contained config/source are `5,423 / 46,688` bytes at `39DB7375...BA155 / F22C7C7B...713D0`; structural controls pass `965` lifecycles, `93 → 79` roles, `31` masks, `207` adjacency edges, exact V8 and three static seeds. Freeze `DD20_V8_EXISTING_COMPONENT_MEMBERSHIP_RECONSTRUCTION_PROXY_V1_IMPLEMENTATION_FREEZE.json` is `5,091` bytes / `89B8503E...653C5B`. No candidate-map result, validation, holdout or MT5 exists before this boundary reaches `origin/main`.

The first frozen process stopped before result serialization with `KeyError: maximum_valid_economic_paths`; the config freezes the longer `maximum_valid_economic_paths_after_all_proxy_stages` key. No raw result or economic summary exists, and in-memory candidate arrays have no verdict. `DD20_V8_EXISTING_COMPONENT_MEMBERSHIP_RECONSTRUCTION_PROXY_V1_EXECUTION_KEY_CORRECTION_DECLARATION.json`, `3,117` bytes / `6A328F5E...2C5410`, permits exactly that one source-token replacement before a corrected freeze and one replay.

Correction declaration commit `b578f13` reached `origin/main` before the one-token source change. Config remains `39DB7375...BA155`; corrected source is `46,711` bytes / `86FD7AFE...FF46E`, with exactly one Git line removed/added and unchanged structural/control parity. `DD20_V8_EXISTING_COMPONENT_MEMBERSHIP_RECONSTRUCTION_PROXY_V1_CORRECTED_IMPLEMENTATION_FREEZE.json` is `3,636` bytes / `9E0D954A...541BF`. No clean replay or output exists before this freeze reaches `origin/main`.

Stage A is valid complete after corrected freeze commit `6ef3001`. All `79` roles produced `24` primary/robust and `28` fallback eligible points. Three immutable Stage-B centers are RC61/RC64/Cross `1.25/1.5/1.75` (`+$174.08 / +$159.232`, DD `17.6447%`), RC64/Cross/Return `1.5/1.5/1.25` (`+$143.13 / +$129.229`, DD `18.8169%`) and all five `1.25/1.5/1.5/2/1.25` (`+$150.105 / +$133.103`, DD `24.0998%`). Raw result is `28,410` bytes / `09FADF11...20DB8`; durable result is `8,476` bytes / `1AD8E9F9...038C7C`. This is a robust contrast bundle, not a deletion decision. Stage B is mandatory; validation/holdout/MT5 remain unopened.

Mandatory Stage B `campaigns/dd20-v8-membership-local-reallocation-proxy-v1/` is declared pre-input after Stage-A result commit `1ba952f` reached `origin/main`. The three immutable membership roots receive `18 / 18 / 162 = 198` duplicate-free active-weight vectors at offsets `-0.25 / 0 / +0.25`, fixed risk/cap `0.04 / 0.12`. Declaration is `6,129` bytes / `02CFBB74...92734E`. Stage B uses development only, cannot close U002 and opens no validation, holdout or MT5 before its own frozen boundaries.

Stage-B implementation is frozen after declaration commit `404f7d2` reached `origin/main`. Config/source are `5,206 / 48,766` bytes at `58E774B6...696DAC / 5BA9EEDF...B31EE`; precheck passes `198` roles, zero duplicates, `579` fine-neighbor edges, exact V8 and all three Stage-A centers. Freeze `DD20_V8_MEMBERSHIP_LOCAL_REALLOCATION_PROXY_V1_IMPLEMENTATION_FREEZE.json` is `4,126` bytes / `4A2AD084...6AFEF1`. Candidate economics/output, validation, holdout and MT5 remain absent until this reaches `origin/main`.

Stage B is valid complete after freeze commit `19e9b90`. The `198` roles produced `82` primary, `118` fallback and `70` robust-primary points. Three Stage-C seeds are RC61/RC64/Cross `1.5/1.25/1.75` (`+$167.63 / +$153.542`, DD `18.1367%`), all-five `1.5/1.25/1.5/1.75/1` (`+$142.86 / +$126.707`, DD `25.0782%`) and RC61/RC64/Cross `1/1.25/1.75` (`+$134.65 / +$120.916`, DD `15.8658%`). Raw is `28,115` bytes / `D1114F97...BABE30`; durable result is `7,413` bytes / `64BE90A7...06630C`. Mandatory Stage C follows; validation/holdout/MT5 remain unopened.

Mandatory Stage C `campaigns/dd20-v8-component-local-loss-suspension-proxy-v1/` is declared after Stage-B result commit `da95b52` reached `origin/main`. Its `30` roles are three seeds × one neutral plus `2/3/4` consecutive-loss triggers × `2/4/8` future same-component source-birth suppressions. Causal roles must be active and robust, and displace neutral only by frozen weakest-year/efficiency precedence. Exactly one role per seed receives fresh January-May validation; June-July and MT5 remain closed. Declaration is `7,255` bytes / `06F0A653...689B6C`.

Stage-C implementation is frozen after declaration commit `18d7433` reached `origin/main`. Config/source are `5,405 / 54,646` bytes at `21367DE0...F2D1B7 / 745879B7...C3BEB`; precheck passes `30` unique roles, `36` nonneutral grid edges, exact V8 and all three Stage-B neutral controls with zero state activity. Freeze `DD20_V8_COMPONENT_LOCAL_LOSS_SUSPENSION_PROXY_V1_IMPLEMENTATION_FREEZE.json` is `5,746` bytes / `E75A693F...FD2D4`. Nonneutral candidate economics, January-May validation, June-July holdout and MT5 remain absent until this boundary reaches `origin/main`.

Stage C is valid complete after freeze commit `73eba26`. Its `30` roles produced `7` primary, `12` fallback and `4` active nonneutral-primary points, but no nonneutral point had the required two eligible neighbors over both state axes, so all three neutral memberships remained. January-May validation passed only the all-five neutral `1.5/1.25/1.5/1.75/1`: actual/stressed `+$113.995 / +$109.449333`, DD `13.4376%`, exact-V8 stressed retention `84.3054%`. Raw is `56,364` bytes / `9B879B57...FD7875`; durable result is `6,109` bytes / `FFF22C8D...80533F`. Exactly this one unchanged role enters mandatory Stage D; June-July and MT5 remain unopened.

Mandatory Stage D `campaigns/dd20-v8-membership-temporal-cost-stability-proxy-v1/` is declared after Stage-C result commit `3cd1cd4` reached `origin/main`. The one immutable all-five neutral role must remain positive with DD `<=30%` in six fresh `$100` blocks, including locked June-July, then retain `>=75%` of exact-V8 whole stressed net while improving whole actual DD by `>=5pp`. Failure closes U002 without an alternate or MT5; pass opens exactly one unchanged Stage-E finalist. Declaration is `6,672` bytes / `6BDCE06B...442B90`.

Stage-D implementation is frozen after declaration commit `ba744a4` reached `origin/main`. Config/source are `5,256 / 44,708` bytes at `D003CCF0...2D15E / 9E2F885E...1B4DE`; precheck passes one immutable neutral role, `965` joins, six contiguous blocks, exact V8 and the already known January-May role control. Freeze `DD20_V8_MEMBERSHIP_TEMPORAL_COST_STABILITY_PROXY_V1_IMPLEMENTATION_FREEZE.json` is `4,823` bytes / `0A460453...29EC2`. The other five blocks, locked June-July, continuous whole outcome and MT5 remain absent until this boundary reaches `origin/main`.

Stage D closes U002 exhausted after freeze commit `503fd9d`. The role passed whole stressed retention `76.0185%`, whole DD improvement `10.3818pp` and locked June-July `+$4.6497 / +$2.6697`, but fresh 2024-H1 remained actual/stressed `-$1.3983 / -$6.0763`; only `5/6` block gates passed. A negative stressed half-year is not accepted as a marginal DD-line miss. Raw is `51,072` bytes / `E1259AA3...9C6086`; durable closure is `7,233` bytes / `755D7475...61D77B`. Stage E and MT5 remain closed; no adjacent rescue opens before a new whole-program comparison.

`evidence/V8_OPT_U002_POST_CLOSE_WHOLE_MAP_RECOMPARE_V1.json` completes that required Programs 1-5/7 comparison. All `741` accepted 2024-2025 births used daily multiplier `1`, so changing the positive-growth capital ladder cannot alter U002's failed 2024-H1 block or the historical DD path. Entry hours alias component identities, weekday/relation signs reverse across blocks, exit work lacks intraposition counterfactual paths, and no independent meta action is ready. Program 1 alone is selected: all five exact signal features and native gates are pinned, and a component-relative monotone strengthening map can be built from outcome-free feature distributions without adding an opportunity. Descriptive RC61/Cross strength signs do not authorize selecting only those components; U003 must freeze the complete all-five bundle before economics open. Audit is `13,963` bytes / `D90898B3...F822D1`; U003 remains unopened pre-origin.

`campaigns/dd20-v8-temporal-cost-stability-empty-closure-v1/` is closed `VALID_EMPTY_STAGE_D_NO_SURVIVING_ROLE_UNIT_001_CLOSED_EXHAUSTED_NO_MT5`. Corrected Stage C emitted zero authorized roles, so all six declared Stage-D blocks have zero input and zero economic rows; conditional Stage E remained closed.

`campaigns/dd20-v8-causal-drawdown-state-shaping-proxy-v2/` is closed `VALID_PROXY_COMPLETE_STAGE_C_WHOLE_PATH_GATE_NONCONFIRMATION_NO_STAGE_D_ROLE_STAGE_D_EMPTY_CLOSURE_REQUIRED_NO_MT5`. Exact V1 parity passed. Its robust global state reduced whole actual closed-balance DD to `20.3400%`, but whole stressed net `+$204.7747` retained only `55.6728%` of exact V8 versus the frozen `75%`, so corrected Stage-D role count is zero.

`campaigns/dd20-v8-causal-drawdown-state-shaping-proxy-v1/` is closed `CORRECTION_REQUIRED_PRE_FINAL_STAGE_D_ROLE_SERIALIZATION_NO_ECONOMIC_VERDICT`. Its raw status reported whole-path nonconfirmation at only `55.6728%` stressed retention versus the required `75%`, but the script had already serialized one Stage-D role after validation and before `final_pass`. The state and economic arithmetic exposed no fault, yet the contradictory successor authority invalidates the final verdict until exact V2 replay.

`campaigns/dd20-v8-clean-executable-basin-refinement-proxy-v1/` is complete `VALID_PROXY_COMPLETE_STAGE_B_SURVIVOR_STAGE_C_REQUIRED_NO_MT5`. All `216` primary-eligible points among the `504` fine paths met the neighbor plateau rule; the selected three centers remained the exact Stage-A seeds, so half-step refinement confirmed local stability without a superior nonseed. All three validation roles advance to Stage C. The sole canonical holdout winner passed, and its whole replay retained `76.4880%` of exact-V8 stressed net at `24.0998%` actual DD. No MT5 ran.

`campaigns/dd20-v8-clean-executable-microfrontier-proxy-v2/` is complete `VALID_PROXY_COMPLETE_STAGE_A_SURVIVOR_STAGE_B_REQUIRED_NO_MT5`. Its exact anchor reproduced `965/965` source contexts; `1,344 / 8,640` paths passed the primary gate and `1,340` formed the eligible plateau. The selected center `1.25 / 1.5 / 2 / 2.25 / 1.25 / 0`, risk `0.04`, cap `0.12` replayed whole-path actual/stressed `+$351.7647 / +$322.3487` at actual DD `24.0998%`, while two other ranked centers were economically identical under lot quantization. This is Stage-A basin evidence only, not an MT5 finalist or Unit closure.

`campaigns/dd20-v8-clean-monotone-microfrontier-proxy-v1/` is closed `CORRECTION_REQUIRED_INVALID_FRESH_CAPITAL_AND_POSITION_RISK_MODEL_NO_ECONOMIC_VERDICT`. It improperly treated base-position-risk as a volume multiplier and carried accumulated source volume into fresh `$100` periods; all `34,560` candidate rows and the 0-trade holdout are invalid for economics. MT5 count is zero and Unit 001 remains open.

`campaigns/dd20-paired-clean-requal-mt5-v2/` is closed `VALID_CLEAN_HISTORY_FIXED_PAIRED_CANDIDATE_NOT_REQUALIFIED_NO_LIVE_AUTHORITY`. Its complete clean exact-V8 candidate lifecycle ledger is the source of Stage A; its failed requalification gates remain unchanged.

`campaigns/dd20-paired-clean-requal-mt5-v1/` is closed `CORRECTION_REQUIRED_CONTROL_INVALID_DATA_UNAVAILABLE_NO_ECONOMIC_VERDICT_CANDIDATE_NOT_STARTED`. Its corrected CONTROL completed with Model 4, HTML 100% real ticks, zero detailed fallback, exact contract/swap rows and unchanged 198-file history/full-symbol fingerprints, but the exact inherited V8 RC4 path ended with `data_unavailable=12` against a frozen zero gate. V1 lacked per-attempt reason telemetry, so the output is invalid rather than economic. CANDIDATE never started; observed money is quarantined. The 17-file invalid bundle is `30,229,899` bytes / `384EB804...1DE09`, and active V1 runtime output is zero.

Current authority notice: historical campaign summaries below preserve their original closed wording and numbers. For every 2022-08 through 2026-06 Selection named by `lineage/OPTIMIZATION_REAL_TICK_CONTINUITY_AUTHORITY_CORRECTION_V1.json`, that correction supersedes the historical validity label; the economics are descriptive only and cannot select, rank or promote a candidate.

`campaigns/dd20-frequency-lane-core-growth-quarantine-mt5-v1/` remains closed, but its final authority is now the declaration-fixed `INVALID_QUARANTINED_RECOMBINATION_ENVIRONMENT_OR_ENGINEERING_NO_ECONOMIC_VERDICT`. Its signed result and all economics remain descriptive audit material; no recent path, candidate, Lab handoff or Live authority survives.

`campaigns/portfolio-risk-cap-envelope-v1/` preserves all 15 selection and 15 isolated-forward passes without rewriting or rerun, but the result is now `QUARANTINED_SHARED_SELECTION_CORPUS_WITHOUT_COMPLETE_DETAILED_HISTORY_PROOF_NO_ECONOMIC_AUTHORITY`. Its prior parent/frontier numbers are descriptive only until independently requalified.

`campaigns/dd20-profit-frontier-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_MT5_SHORTLIST`. Near-uniform gross leverage almost doubled selection profit, but every frozen role amplified the isolated later loss and exceeded the proxy DD cap; none reached MT5.

The user's prospective objective remains maximum actual and doubled-cost-stressed profit around a nominal `20%` MT5 equity-drawdown line. Nominal and effective pragmatic gates are reported separately; marginal misses are judged from their proportional size and the complete economic evidence under `docs/OPERATING_DIRECTION.md`. `campaigns/dd20-capital-composition-proxy-v1/` is closed `VALID_PROXY_COMPLETE_ONE_MT5_SHORTLIST`: of `29,016` fixed-budget compositions, weights `3.0 / 3.0 / 1.0 / 2.5 / 1.0 / 0.0` were the sole frozen role to turn the isolated later actual/stressed net positive while staying inside the proxy DD cap.

`campaigns/dd20-capital-composition-mt5-v1/` is closed `EXPLOSIVE_PROFIT_CONFIRMED_BUT_SELECTION_MT5_DD_EXCEEDS_20_PERCENT`. Its exact `3 / 3 / 1 / 2.5 / 1 / 0` exposure earned selection actual/stressed `+$2,822.33 / +$2,648.883` and forward `+$36.67 / +$34.6175`, but selection maximum relative MT5 equity DD was `27.072835%`; the valid path is rejected under the user's hard `20%` prospective budget and will not rerun.

`campaigns/dd20-executable-volume-lattice-proxy-v1/` is closed `VALID_PROXY_COMPLETE_SELECTION_WINNER_FAILS_LATER_NO_MT5_SHORTLIST`. Its maximum-profit selection role `3 / 1 / 2.5 / 2 / 1 / 0.5` reached actual/stressed `+$6,976.9515 / +$6,588.5518` at `19.278821%` proxy DD, but the untouched later segment lost `-$9.28 / -$12.808` at `28.019366%` DD. US100-cross supplied `-$37.278` of later stressed net, so the exact winner and its quantized tie close without MT5 or retuning. The next serial stage must use an independent chronological/component-stability proxy before any MT5 shortlist.

`campaigns/dd20-chronological-stability-proxy-v1/` is closed `VALID_PROXY_COMPLETE_JUNE_STABLE_WINNER_FAILS_JULY_NO_MT5_SHORTLIST`. Its June-stable maximum-profit `3 / 1 / 2 / 1.5 / 1 / 0` role earned selection actual/stressed `+$6,532.4496 / +$6,186.2932` at `19.977049%` DD and June `+$8.95 / +$7.284`, but untouched July lost `-$10.25 / -$11.376`; no MT5 ran. The next distinct proxy may require paired June-and-July stability for all candidates before one possible MT5 shortlist.

`campaigns/dd20-paired-month-stability-proxy-v1/` is closed `VALID_PROXY_COMPLETE_ONE_MT5_SHORTLIST`. Of `112,752` compositions, `2,824` passed long selection plus independent June/July gates. Maximum-profit weights `2 / 1.5 / 2 / 2.5 / 1.5 / 0` produced selection actual/stressed `+$5,551.5636 / +$5,237.1427` at `19.423399%` proxy DD, June `+$29.21 / +$27.474`, and July `+$4.10 / +$3.00`. Exactly this one hypothesis may advance to a new dedicated MT5 family.

`campaigns/dd20-paired-month-stability-mt5-v1/` is historically closed `VALID_MT5_COMPLETE_SELECTION_DD_MISSES_CAP_BY_0_2569_PERCENTAGE_POINTS` under its then-binding mechanical line. Its exact `2 / 1.5 / 2 / 2.5 / 1.5 / 0` exposure earned selection actual/stressed `+$5,786.63 / +$5,477.524` and full June/July forward `+$32.74 / +$30.626`. Forward DD passed at `18.675302%`, while selection maximum relative MT5 equity DD was `20.256888%`, only `0.256888` percentage points above the nominal line. The frozen result is not rewritten; its current qualification is recorded by the later whole-map pragmatic recompare.

Current terminology correction: June and July were both used in the preceding proxy candidate gate and ranking, so later native runs over that same interval are proxy-selected native execution/cost confirmation, not independent untouched/OOS forward evidence. Closed campaign evidence remains byte-immutable; `lineage/REPOSITORY_INTEGRITY_ERRATA_V1.json` governs current interpretation.

`campaigns/dd20-mt5-calibrated-exposure-margin-proxy-v1/` is closed `VALID_PROXY_COMPLETE_ONE_MT5_SHORTLIST`. After the outcome-free paired-DD correction, `16,807` candidates completed in `4.13` seconds: `127` passed selection margin, `52` passed full paired forward and June, and `5` passed July. The sole maximum-profit role `1.8 / 1.3 / 1.4 / 2.6 / 1.5 / 0` produced selection actual/stressed `+$4,425.6202 / +$4,176.4760` at calibrated/budgeted DD `19.288409% / 19.788409%`, plus full paired-forward `+$26.42 / +$23.987` at budgeted DD `15.843967%`. Exactly this one hypothesis may advance to a new dedicated MT5 family.

`campaigns/dd20-exposure-margin-mt5-v1/` is closed `VALID_MT5_COMPLETE_SELECTION_DD_MISSES_CAP_AND_CONTINUOUS_JULY_NEGATIVE`. Its sole `1.8 / 1.3 / 1.4 / 2.6 / 1.5 / 0` exposure earned selection actual/stressed `+$4,169.94 / +$3,942.2615`, but native maximum relative equity DD was `20.488349%`, missing the hard budget by `0.488349` percentage points. Full June/July forward passed at `+$27.87 / +$25.9725` and `15.487346%` DD, while the continuous July stressed slice was `-$2.2375`. The exact candidate is closed without rerun; its valid lifecycle/native-cache evidence becomes an anchor for a distinct fast proxy.

`campaigns/dd20-native-gap-july-robustness-proxy-v1/` is closed `VALID_PROXY_COMPLETE_ONE_MT5_SHORTLIST`. Of `49,049` compositions, `1,509` passed selection/full-pair/June, `815` passed raw July and `647` passed the worst observed continuous-July shortfalls plus the additional `$1` reserve. The sole maximum-profit role `1.6 / 0.8 / 0.4 / 3.2 / 1.2 / 0` produced selection actual/stressed `+$1,763.4819 / +$1,693.3221` at raw/calibrated/budgeted DD `17.428221% / 19.210908% / 19.960908%`, full-pair stressed `+$27.236`, and conservative July stressed `+$0.9025`. It is exactly one MT5 hypothesis; no anchor or original combination reran.

`campaigns/dd20-native-gap-july-robustness-mt5-v1/` is closed `VALID_MT5_COMPLETE_ALL_ECONOMIC_GATES_PASS`. Its sole `1.6 / 0.8 / 0.4 / 3.2 / 1.2 / 0` candidate earned selection actual/stressed `+$1,691.54 / +$1,626.26`, beating the preserved comparator by `44.9614% / 49.8294%`; all four selection epochs were positive and native maximum relative equity DD passed at `19.550372%`. The independent full June/July forward earned `+$23.01 / +$21.256` at `12.436759%` DD, with June `+$19.79 / +$18.976` and July `+$3.22 / +$2.280`. This is the first fully qualified DD20 optimization success anchor, not an automatic Live promotion. Continue proxy-first toward more stressed profit through component redistribution; do not rerun this path, any prior candidate or the original 15 combinations.

`campaigns/dd20-qualified-profit-redistribution-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `87.9067`-second process evaluated all `184,041` redistributions plus four external anchor rows; every copied-input and proxy/native anchor check passed. Under the global worst selection-profit correction `+$659.7999 / +$615.1109` plus `$50`, the five-anchor DD correction and four positive epochs, selection eligibility was `0`, so all conditional paired/month counts and the MT5 shortlist are also `0`. This valid empty frontier retains the first qualified MT5 anchor and closes without MT5 or any rerun. The next stage must use an economically distinct proxy mechanism rather than repeat this grid.

`campaigns/dd20-local-error-profit-frontier-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `97.8265`-second process evaluated all `204,490` new half-step rows plus five external anchors; all copied-input, proxy/native and local-envelope checks passed. Candidate-local selection DD charges ranged `1.148018..5.185475` points and actual/stressed profit charges ranged `$94.2485..$1,968.0084 / $88.6346..$1,900.7276`, but no row simultaneously cleared the corrected qualified-profit floor, native-DD budget and four positive epochs. No MT5 shortlist exists. After two distinct static-weight frontiers, retain the qualified anchor and change the next proxy mechanism to capital-addition-ladder growth rather than another adjacent weight grid.

`campaigns/dd20-capital-addition-ladder-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `1.0581`-second process evaluated all `50` new constant addition steps plus the closed `$150` anchor, and every copied lifecycle/native/proxy fact passed. All candidates were positive; `18` passed the conservative qualified-profit floor, `26` passed budgeted DD and `30` passed every epoch, but the full selection intersection was empty. The `$50` explosive endpoint reached proxy stressed `+$44,789.1023` and conservative stressed `+$23,124.1501` at `48.924974%` budgeted DD; the highest-profit DD-eligible `$155` endpoint reached only conservative stressed `+$1,540.0281` at `19.460019%`, below the qualified `+$1,626.26`. The qualified MT5 anchor remains, and the next distinct proxy should cap the day multiplier instead of repeating the constant ladder.

`campaigns/dd20-capped-profit-accelerator-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `1.0330`-second process evaluated all `168` new binding `step × cap` paths plus the closed calibration row. Every input, native/proxy anchor and candidate-construction gate passed. All `168` candidates were positive and conservatively exceeded the qualified profit floor, but none passed budgeted DD or every selection epoch. The maximum-profit step `$50`, cap `60` path retained conservative stressed `+$6,988.2869`, yet budgeted DD stayed `48.924974%`. Capping terminal exposure alone therefore cannot repair the too-early low-step ramp; the next distinct proxy must delay acceleration until after a causal stressed-profit buffer.

`campaigns/dd20-deferred-profit-accelerator-proxy-v1/` is closed `VALID_PROXY_COMPLETE_ONE_MT5_SHORTLIST`. One `1.0472`-second process evaluated all `256` new paths plus the closed calibration row: `56` passed the entire selection and forward/month funnel. The sole maximum-profit role retains `$150` growth until `+$450` stressed closed profit, then uses a `$50` step capped at `14`. Its selection proxy actual/stressed net is `+$2,647.4747 / +$2,554.1534`, conservative `+$2,083.5364 / +$2,006.6757`, at `19.800372%` budgeted DD with all four epochs positive. Fresh June/July also pass. Exactly this one hypothesis may advance to a dedicated MT5 family; no grid or anchor reruns.

`campaigns/dd20-deferred-profit-accelerator-mt5-v1/` is closed `VALID_MT5_COMPLETE_SELECTION_DD20_FAIL_FORWARD_ALL_GATES_PASS`. Its sole causal `$150 → $50 after +$450`, cap-`14` candidate earned selection actual/stressed `+$2,185.39 / +$2,095.664`, improving on the qualified anchor by `+$493.85 / +$469.404`, and all four epochs were positive. Native maximum relative equity DD was `28.087819%`, however, exceeding the hard budget by `8.087819` points. The independently initialized forward reproduced `+$23.01 / +$21.256` at `12.436759%` DD with positive June and July. This is a valid higher-profit economic rejection; the qualified anchor remains authoritative. Continue with a distinct proxy-first drawdown-shaping mechanism calibrated by the observed `10.659598`-point native-minus-raw candidate gap, without rerunning this path, any prior candidate or the original 15 combinations.

`campaigns/dd20-profit-protected-risk-ceiling-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `0.2862`-second process evaluated all `31` new hard aggregate-risk caps `$30..$330`; all bound in selection and none changed the exact positive forward. All candidates were positive, `18` passed conservative profit and `17` passed every epoch, but budgeted DD passed `0`. The best profit-and-epoch cap `$200` retained conservative actual/stressed `+$1,983.62 / +$1,900.222` but budgeted `25.237963%` DD; the lowest-DD `$30` cap still budgeted `20.679738%` and retained only conservative stressed `+$453.1055`. Aggregate admission ceilings have a valid disjoint profit/DD frontier and no MT5 shortlist. Continue proxy-first with a distinct equity high-watermark giveback or open-profit realization mechanism; do not repeat a risk-cap, accelerator, prior candidate or original-15 grid.

`campaigns/dd20-equity-high-watermark-terminal-lock-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `0.5008`-second process evaluated all `37` permanent locks after declaration commit `18196ee` reached `origin/main`. All `37` candidates were positive and passed budgeted DD, `24` retained every epoch, but profit passed `0`. The maximum-profit `14.5%..16.5%` tie locked at `2026.03.06 15:30`, earned `+$1,428.82 / +$1,373.40` at budgeted `19.800372%` DD and fell short of the qualified anchor by `$262.72 / $252.86`; permanent termination discarded the later recovery. Forward remained exact and no MT5 shortlist exists. Continue proxy-first with a distinct nonterminal open-profit realization or temporary exposure-release mechanism, not another terminal-lock trigger grid or any closed-path rerun.

`campaigns/dd20-post-activation-profit-realization-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `0.1753`-second process evaluated all `49` uniform `0.05R..2.45R` targets after declaration commit `0a19a7b` reached `origin/main`. Conservative profit passed `38`, every epoch passed `48`, but budgeted DD passed `0`. Maximum-profit `1.20R` earned `+$2,321.208959 / +$2,231.482959`, improving the rejected accelerator by `+$135.818959`, yet budgeted `25.237963%` DD. Minimum-DD `0.10R` still budgeted `21.270438%` while conservative stressed profit collapsed to `+$644.361240`. Uniform realization has another valid disjoint frontier and no MT5 shortlist. Continue with a state-dependent nonterminal exposure control, not component/subgroup retuning, another R threshold, or a closed-path rerun.

`campaigns/dd20-drawdown-responsive-exposure-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `0.1653`-second process evaluated all `72` state-dependent paths after declaration commit `32c95f0` reached `origin/main`. Conservative profit passed `32` and every path retained four positive epochs, but budgeted DD passed `0`. Maximum-profit `7% × 0.75` earned `+$2,099.9125 / +$2,016.883375` at budgeted `25.628513%`; the nearest `8% × 0.25` path scaled `305` births, retained conservative `+$1,751.785 / +$1,686.828875`, yet its family-minimum budgeted DD was `21.270438%`. Future-birth scaling cannot cut the existing-exposure/pre-activation floor. No MT5 shortlist exists. Continue with a nonterminal release of already-open aggregate exposure, not a retune or closed-path rerun.

`campaigns/dd20-one-shot-open-exposure-release-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `1.0714`-second process evaluated all `136` one-shot paths after declaration commit `d0c7bf3` reached `origin/main`. All `136` remained profitable and positive through every epoch, but budgeted DD passed `0`. The maximum-profit and minimum-DD trigger `17.5%`, retain-`0` role earned actual/stressed `+$2,188.60 / +$2,098.874`, conservative `+$2,138.60 / +$2,048.874`, yet budgeted `30.248564%` DD. Releasing only the book present at the first trigger does not govern later full-exposure births; no MT5 shortlist exists. Continue proxy-first with a recurring open-exposure governor, not a retune or closed-path rerun.

`campaigns/dd20-recurring-open-exposure-ratchet-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `0.4921`-second process evaluated all `27` recurring paths after declaration commit `bd14cca` reached `origin/main`. Conservative profit passed `13`, but budgeted DD passed `0`. Maximum-profit `18% × retain 0.50` kept conservative `+$1,995.58 / +$1,905.854` at budgeted `36.237509%`; the minimum-DD `12% × retain 0.50` kept `+$1,984.095 / +$1,894.369` but still budgeted `35.785203%`. Resetting protection to successive lower local highs creates staircase DD against the global account high. No MT5 shortlist exists. Continue with a persistent global-high drawdown regime, not a local-ratchet retune or closed-path rerun.

`campaigns/dd20-persistent-global-drawdown-regime-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `1.0245`-second process evaluated all `42` immutable-global-high paths after declaration commit `e19e977` reached `origin/main`. All paths retained positive capital and epochs, but conservative profit passed only `1` and budgeted DD passed `0`. The maximum-profit and minimum-DD `10% / current retain 0 / future 0.50` role earned actual/stressed `+$1,856.42 / +$1,788.52775`, conservative `+$1,756.42 / +$1,688.52775`; stressed closed-balance DD fell to `18.628193%`, yet the conservative open-path envelope across `347` scaled births budgeted `27.852956%`. No MT5 shortlist exists. Continue with a time-bounded drawdown quarantine, not a persistent-regime retune or closed-path rerun.

`campaigns/dd20-time-bounded-drawdown-quarantine-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `2.3497`-second process evaluated all `144` paths after declaration commit `0597956` reached `origin/main`. Conservative profit passed `140`, but budgeted DD passed `0`. Maximum-profit `10% / retain 0 / birth 0 / 336h` kept conservative `+$2,130.81 / +$2,049.024` but budgeted `34.837040%`; family-minimum-DD `8% / retain 0 / birth 0.50 / 336h` kept `+$2,095.765 / +$2,010.895` but still budgeted `25.738134%`. No MT5 shortlist exists. The first optimization phase is frozen here without opening a successor; the qualified DD20 MT5 anchor remains authoritative.

`campaigns/dd20-component-equity-state-allocation-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE`. One `1.5086`-second process evaluated all `144` causal component-local stressed-R policies. Conservative profit passed `14`, budgeted DD passed `21`, but their intersection was empty. Maximum-profit lookback `40`, band `0.05R`, loss `0.25`, gain `1.25` retained conservative `+$1,837.1140 / +$1,765.4396` at `22.499077%` budgeted DD. The minimum-DD profit pass at lookback `40`, band `0.10R`, loss `0`, gain `1.125` retained conservative `+$1,728.8473 / +$1,661.5936`; raw DD exactly matched the neutral control, but the frozen calibration/reserve budgeted `20.300372%`. No winner exists, so June/July and MT5 remained unopened. Freeze this grid and recompare its boundary clue before an economically distinct successor.

`campaigns/dd20-component-equity-state-native-margin-mt5-v1/` is closed `VALID_MT5_COMPLETE_ALL_ECONOMIC_GATES_PASS_COMPONENT_STATE_NARROW_PROFIT_UPLIFT`. The one frozen native-information successor required two unlimited non-economic Windows path corrections; neither incomplete invocation received an economic verdict. The first complete selection earned actual/stressed `+$1,693.46 / +$1,626.677`, strictly improving the qualified anchor by `+$1.92 / +$0.417`; all four epochs passed and native maximum relative equity DD was `19.797353%`, leaving `0.202647` points. The independently initialized forward exactly retained `+$23.01 / +$21.256` at `12.436759%` DD, with positive June and July. This becomes the current qualified optimization anchor, not Live authority. All realized state changes were gain overlays, the narrow uplift occurred in E4, and the forward never reached the 40-close warmup. Recompare the full map before an economically distinct proxy-first successor; do not continue into adjacent state thresholds, lookbacks, component subgroups or any closed-path rerun.

`campaigns/dd20-gain-overlay-base-quantum-reservation-mt5-v1/` is closed `VALID_MT5_COMPLETE_SELECTION_PROFIT_GATES_FAIL_NO_FORWARD_RETAIN_CURRENT_ANCHOR`. The sole Unit097 seed reserved one `4%` risk-capital quantum only from executable gain-overlay volume above neutral. One complete `132,257,500`-tick selection earned actual/stressed `+$1,692.45 / +$1,626.089` with all four epochs positive and native relative equity DD `19.550372%`. The reserve bound `9` times and removed one volume step each, but at the identified May 15 interaction its `$61.21644` headroom remained below the unchanged `1.2`-weight Return budget `$73.459728`, so the adverse admission substitution persisted. Candidate profit missed the current anchor by `$1.01 / $0.588`; both strict profit gates failed and forward remained unopened. Freeze this role without an adjacent reserve, weight-aware, component-specific or admission-order retune; retain the component-equity-state anchor and recompare the full macro map before any distinct successor.

`campaigns/dd20-legacy-us500-h4-staged-overlay-mt5-v1/` is closed `VALID_MT5_COMPLETE_USER_TOLERANCE_ACCEPTED_SELECTION_AND_FORWARD_PASS_PROFIT_UPLIFT`. The exact Unit098 lineage-pinned US500 H4 role ran as one separately owned `0.01` staged overlay outside core planned-risk/state/three-slot accounting while sharing balance, equity, native margin and downstream state. One complete `132,257,500`-tick selection earned actual/stressed `+$1,736.50 / +$1,668.5262`, improving the prior anchor by `+$43.04 / +$41.8492`; all four epochs remained positive. The overlay closed `40 / 40` entries/exits and directly earned `+$6.76 / +$6.1752`, while the shared-state core ledger gained another `+$36.28 / +$35.674`. Native maximum relative equity DD is transparently `20.402009%`: a nominal `0.402009`-point miss but only a `2.010043%` proportional overshoot of the line, accepted by the user's explicit pragmatic-threshold direction. The independent forward exactly retained `+$23.01 / +$21.256` at `12.436759%` DD with positive June and July; fresh balance kept H4 inactive. This became the qualified Optimization anchor under the updated judgment, without Live authority. The exact H4 role remains frozen without an adjacent threshold, stop, hold, gate, volume, margin, event-order or legacy-candidate rescue.

`campaigns/dd20-pragmatic-threshold-anchor-recomparison-v1/` is closed `VALID_EXISTING_MT5_EVIDENCE_REQUALIFIED_PAIRED_MONTH_AS_PRAGMATIC_ANCHOR`. It reran nothing and applied the 2026-08-29 user judgment consistently across all eight complete MT5 results. The paired-month candidate's `20.256888%` selection DD is a `0.256888`-point / `1.284438%` proportional miss, smaller than the already accepted H4 miss; selection actual/stressed is `+$5,786.63 / +$5,477.524`, all four reconstructed epochs are positive, and full forward is `+$32.74 / +$30.626` at `18.675302%` DD. Ledger reconstruction discloses June `+$33.33 / +$32.109` and July `-$0.59 / -$1.483`; the small July loss is accepted as a marginal temporal miss equal to `1.8021% / 4.8423%` of positive full-forward net. Against H4 it adds `+$4,050.13 / +$3,808.9978` selection profit, adds `+$9.73 / +$9.37` forward profit and lowers selection DD by `0.145121` points, so `dd20-paired-month-stability-mt5-v1` is now the qualified Optimization anchor without Live authority. Its active universe is US30+US100; the user-directed distinct successor is a serial high/mid-frequency US30+US100 lane and low-frequency US500 lane followed by one combined MT5 confirmation, never arithmetic summation of isolated results.

`campaigns/dd20-frequency-execution-lane-combination-proxy-v1/` is closed `VALID_PROXY_COMPLETE_ONE_COMBINED_MT5_SHORTLIST`. The user-directed split is feasible for execution ownership, but not as a two-symbol data universe: the frozen US100 Cross still requires read-only US500 H1 closes. One source-free process evaluated the sole exact paired-anchor + fixed-H4 combination in `0.0009185` seconds. Direct-only selection projects `+$5,793.39 / +$5,483.6992`; prior coupled information projects `+$5,829.67 / +$5,519.3732`; every projected epoch stays positive. Prior observed H4 DD charge projects `20.861543%`, nominally false but only `4.307713%` proportionally above the line and below the proxy's conservative `6%` user-example reference. Forward remains `+$32.74 / +$30.626` at `18.675302%` because its maximum closed balance `$146.88` cannot open the `$500` H4 gate. Exactly one combined MT5 candidate advances; these projections have no final economic authority.

`campaigns/dd20-frequency-lane-combined-portfolio-mt5-v1/` is closed `VALID_MT5_COMPLETE_SELECTION_NONCONFIRMS_COMBINED_REPLACEMENT_FORWARD_NOT_OPENED`. Its sole paired-month-plus-H4 candidate completed one valid `100%`-real-tick selection at `+$5,546.61 / +$5,237.2907` and reported `20.26%` maximum relative equity DD. All epochs and the pragmatic DD gate passed, and H4 itself earned `+$10.14 / +$8.8217`, but total profit trailed the paired-month control by `$240.02 / $240.2333`; shared account sizing changes overwhelmed direct H4 profit. Both profit gates failed, so forward remained unopened and the exact combination is frozen without retune.

`campaigns/dd20-live-rlo1-post-forward-economic-proxy-v1/` is closed `VALID_LIVE_DERIVED_POST_FORWARD_PROXY_SUPPORTS_CURRENT_ANCHOR_NO_NEW_MT5_SHORTLIST`. A one-time byte-equal snapshot of three complete RLO1 Live-Dev lifecycles earned observed actual/stressed `+$0.66 / +$0.593`. With no tuning permitted after intake, the already-qualified paired-month weights projected `+$1.08 / +$0.968` on the same fixed paths and passed conservative risk/margin admission. This is supportive post-forward information only, not native profit/DD proof or a new candidate; no MT5 path opened.

`campaigns/dd20-uniform-entry-strength-floor-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE_UNIFORM_FLOOR_DEGRADES_PROFIT_AND_DRAWDOWN`. One source-free process evaluated the blind global floor factors `1.00..1.50`. The highest-profit noncontrol `1.05` removed `84` lifecycles, lost `$944.16 / $922.287`, and raised conservative DD to `25.422685%`; every stronger factor also failed both profit and DD. No holdout or MT5 shortlist opened, and no component/direction/period/subgroup/loosening rescue is permitted.

`campaigns/dd20-protective-stop-headroom-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE_TIGHTER_PROTECTIVE_STOPS_DEGRADE_PROFIT_AND_DRAWDOWN`. Both frozen first-crossing bounds agreed: even the smallest tighter move converted `142` lifecycles, lost `$721.174228 / $721.174228`, and raised conservative DD to `23.190667%`; every larger move was worse. No holdout or MT5 shortlist opened, and the global stop/reserve neighborhood remains frozen.

`campaigns/dd20-market-state-router-mt5-v1/` is closed `VALID_MT5_COMPLETE_SELECTION_PROFIT_GATES_FAIL_FORWARD_NOT_OPENED`. The exact Unit 099 expanding quarterly pooled Ridge router completed one valid `100%`-real-tick selection with zero core or router fault. It blocked `377 / 1,620` independent signals and reduced reported maximum relative equity DD to `12.52%`, but actual/stressed profit collapsed to `+$914.29 / +$869.242`, trailing the paired-month anchor by `$4,872.34 / $4,608.282`; all four epochs and all five active components stayed positive. Both profit gates failed, so forward ran zero times. The candidate is valid economic nonconfirmation and every adjacent router/model/label/percentile/feature/hold/risk/volume rescue is frozen.

`campaigns/dd20-paired-month-component-state-combination-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_MT5_SHORTLIST_COMPONENT_STATE_COMBINATION_NONCONFIRMATION`. It combined the exact paired-month weights with the exact previously native-qualified `40 / 0.10R / 0 / 1 / 1.125` component-state role without a grid. Sixty-four non-neutral decisions across five components led to nine Cross admission losses; gross actual/stressed fell by `$187.0011 / $179.2354`, conservative deltas were `-$282.3884 / -$274.1520`, and budgeted DD reached `21.732774%`, above the frozen `21.2%` proxy ceiling. Forward remained exactly `+$32.74 / +$30.626`, and the current three-close RLO1 Live snapshot remained in warmup. No MT5 shortlist exists; both source mechanisms remain frozen and the paired-month anchor is unchanged.

`campaigns/dd20-portfolio-occupancy-exposure-proxy-v1/` is closed `VALID_PROXY_COMPLETE_NO_SELECTION_ELIGIBLE_PORTFOLIO_OCCUPANCY_EXPOSURE_PROFIT_DD_DISJOINT`. One complete source-free process evaluated the exact `5 × 5` empty-book/occupied-book global exposure grid after two unlimited pre-result source-representation corrections. The exact control shows both occupancy cohorts strongly profitable at `+$3,405.42 / +$3,197.325` and `+$2,381.21 / +$2,280.199`; mild occupied-book suppression loses profit and worsens DD. Eight noncontrols conservatively beat both profit anchors, but DD passed `0/24`. The nearest empty `1.125` / occupied `1` role retained conservative `+$90.717 / +$90.581` improvement yet budgeted DD was `22.468569%`, above the pragmatic `21.2%` proxy ceiling. Selection eligibility is zero, so June/July candidate outcomes and MT5 remain unopened. The occupancy neighborhood is frozen and the paired-month anchor is unchanged pending a new whole-map comparison.

`campaigns/dd20-paired-month-live-control-cost-resilience-proxy-v1/` is closed `PASS_FIXED_DEVELOPMENT_CANDIDATE_COST_RESILIENCE_VS_ACTIVE_LIVE_CONTROL`. One fixed-policy process applied the same arithmetic `1x..4x` observed-cost books to the exact paired-month candidate and active Live-derived `0.04 / 0.12` control. At `4x`, the candidate retained selection `+$4,859.312` at PF `1.4011` and full forward `+$26.398` at PF `1.2504`, versus control `+$778.2375 / -$6.237`; candidate advantages were `+$4,081.0745 / +$32.635`. All four selection epochs and all five active components stayed positive. June remained `+$29.667`; July weakened to `-$3.269`, `12.38%` of positive full forward and inside both frozen pragmatic bounds. Arithmetic `3x/4x` books do not establish native capital-ladder feedback or open-equity DD. The fixed replacement-development candidate is strengthened, not changed, and no new MT5 or Live authority exists.

`campaigns/dd20-paired-month-live-control-economic-efficiency-proxy-v1/` is closed `PASS_FIXED_DEVELOPMENT_CANDIDATE_ECONOMIC_EFFICIENCY_WITH_LOWER_TURNOVER_DISCLOSED`. The unchanged paired-month candidate closes only `67.39%` as many selection lifecycles as the exact active Live-derived control, or `91.54%` after excluding the control Passive closes deliberately disabled by the candidate. Even so, its actual/stressed net per close is `8.72x / 8.87x` control and net per native equity-DD point is `2.67x / 2.71x` control; all `5/5` candidate and `6/6` control components remain positive. The economic cost is explicit: selection DD is `20.2569%` versus `9.1948%`, `+11.0621` points, and admission skips rise from `2.75%` to `11.85%`. Forward stays `+$32.74 / +$30.626` versus `-$1.11 / -$2.819`, with `64.29%` turnover and `+1.8432` DD points. This is an economic-density qualification, not raw DD dominance or Live authority.

`campaigns/dd20-paired-month-live-control-native-quality-recomparison-v1/` is closed `VALID_FIXED_DEVELOPMENT_CANDIDATE_NATIVE_QUALITY_TRADEOFF_SELECTION_MIXED_FORWARD_DOMINANT`. Existing native reports show selection PF `1.49547` versus control `1.413892`, but Recovery Factor `8.69634` versus `10.965367` and Sharpe `5.35` versus `5.625108`. Thus the candidate's PF is `5.77%` higher while native dollar recovery and smoothness are `20.69% / 4.89%` lower, consistent with its late compounding-dollar drawdown. Untouched forward is dominant at PF `1.32038 / 0.984324`, Recovery `1.17054 / -0.055472` and Sharpe `5.57 / -0.340238`. The prior net/stressed-net and relative-DD-point efficiency pass remains exact; this mixed quality classification changes neither the candidate nor Live authority.

`evidence/OPTIMIZATION_POST_NATIVE_QUALITY_WHOLE_MAP_AUDIT_V1.json` closes a scheduler/readiness boundary as `NO_INDEPENDENT_READY_ECONOMIC_MECHANISM_OBSERVATION_BOUNDARY_FIXED_DEVELOPMENT_CANDIDATE_RETAINED`. The complete candidate schema exposed no causal role outside already-closed signal/overlap, entry/stop/occupancy, capital-risk and exit neighborhoods. All explicit positive or advance decisions in the permitted legacy lineage are current ancestry or already consumed by Units 093/098/099 and their campaigns; `836/1,011` records remain policy-authoritative `unknown` and were not inferred. Natural RLO1 metadata has not advanced since the reboot. No campaign, MT5 shortlist or Live authority opens; the paired-month candidate remains fixed and the Goal stays active pending materially new independent evidence.

`campaigns/dd20-dual-portfolio-internal-formula-lattice-proxy-v1/` is closed `NO_ROBUST_SHARED_FORMULA_REPLACEMENT_CLUE_FIXED_CANDIDATE_RETAINED_LAB_ONLY_INTERACTION_SEEDS_RECORDED`. Its final owned topology evaluated `8,639` unique 59-dimensional points and `17,278` paired E1-E3 economic tasks under exact Live control and fixed replacement contracts. Baselines already occupied the upper profit and lower-DD tails; owned-plateau robustness was Live/replacement/paired `7 / 0 / 0`, replacement PBO was `74.286%` and Reality Check `p=0.21368`. No point reached MT5. Two exact post-selection interactions were recorded only for an independently declared Lab causal check.

Lab Unit 107 `dual-portfolio-formula-interaction-spillover-causality-v1` closed those two clues as `VALID_NO_TRANSFERABLE_DIRECT_FORMULA_INTERACTION_NO_SEED`. Their E1-E3 improvement was genuinely majority-direct rather than a mislabeled pure spillover, but the changed RC4+Return and RC4+Pressure economics themselves reversed in E4 and continuous June-July. Neither Lab seed, Optimization candidate nor MT5 shortlist survives; no neighbor or retune is permitted. The fixed paired-month development candidate remains unchanged and the continuous Goal returns to a fresh whole-map comparison.

`evidence/OPTIMIZATION_POST_FORMULA_INTERACTION_WHOLE_MAP_AUDIT_V1.json` closes that comparison as `NO_INDEPENDENT_READY_MECHANISM_POST_FORMULA_AXIS_EXHAUSTED_FIXED_DEVELOPMENT_CANDIDATE_RETAINED`. Programs 1-5 and 7 expose no unconsumed fixed action after the formula axis and its two causal clues are exhausted. The candidate's uplift is not merely the `0.12 → 0.18` aggregate-risk change—the exact same-risk equal-weight control already exists—but neither is it pure signal alpha; it combines allocation, Passive removal, shared admission/collision and capital-ladder compounding with lower turnover and mixed Recovery/Sharpe quality. Those facts are already closed diagnostics, not a new treatment. Natural RLO1 metadata has not advanced since the reboot and no terminal is running, so no campaign, Lab unit or MT5 path opens. The Goal remains active at a readiness boundary awaiting genuinely new complete evidence or an independent causal mechanism.

`campaigns/dd20-paired-anchor-august-2026-mt5-v1/` is closed `VALID_FIXED_PAIRED_ANCHOR_AUGUST_THROUGH_28_SUPPORT`. The unchanged fixed candidate completed one valid Model-4 path through August 28 at `100%` real ticks: actual/stressed net `+$29.62 / +$28.586`, native relative equity DD `13.44%`, `29` closes and all five active components positive. The second half remained `+$3.294` stressed but the final week was `-$1.118`, `88.48%` of stressed net came from the first half, and `8/37` actionable admission attempts hit the aggregate cap. This is bounded near-month support only—August 31 is excluded and the wider project had seen portions of August—not a retune, completed-month/OOS claim or Live authority. No Optimization campaign is now open; the continuous serial Goal returns to whole-map discovery.

`evidence/OPTIMIZATION_POST_QUANTIZATION_FIDELITY_WHOLE_MAP_AUDIT_V1.json` closes the consultant-clue comparison as `NO_INDEPENDENT_READY_MECHANISM_POST_CONSULTANT_CLUE_CONSUMPTION_FIXED_DEVELOPMENT_CANDIDATE_RETAINED`. Source-confirmed lot-step quantization became Lab Unit 117 and materially changed `456 / 1,417` joint admissions, but selection stressed net fell `-$158.583262` and raw closed DD worsened `+3.094309pp`, dominated by Pressure `-$181.425381` and E4 `-$240.233429`; no MT5 seed survived. The alleged planned-risk accounting asymmetry is absent from the pinned source, pending-reservation tax still lacks Unit 077's natural density, and the closed symbol-lane/capital-partition neighborhood does not reopen. The fixed candidate remains unchanged and the active Goal waits only for materially new complete natural evidence or a genuinely independent causal mechanism.

`evidence/OPTIMIZATION_POST_SCALE_TAIL_FALSIFICATION_WHOLE_MAP_AUDIT_V1.json` closes the remaining scale/concentration clue as `NO_READY_FACTOR_CAP_OR_OTHER_CONSULTANT_MECHANISM_FIXED_DEVELOPMENT_CANDIDATE_RETAINED`. Lab Unit 118 found that `27` common August trades are scale-dominated and supply `84.43%` of uplift, but stop geometry is not invariant (`12/27` inside the frozen band). More decisively, the `2026-08-25` Pressure US30 LONG stop and Cross US100 LONG native loss were sequential with `789` seconds and zero holding overlap; the exact `0.12` same-direction cap would block neither. Applying it across selection would nevertheless block `154/1,428` cross-index births, so no broad cap is inferred from a tail it cannot address. No neighboring cap, cooldown, dynamic factor rule, proxy campaign or MT5 path opens. The fixed candidate remains unchanged and the Goal stays active for genuinely independent or materially new natural evidence.

`evidence/OPTIMIZATION_POST_UNIT_123_GPT_PRO_CHECKPOINT_WHOLE_MAP_AUDIT_V1.json` closes the mandatory post-Unit-123 external checkpoint as `NO_INDEPENDENT_READY_HYPOTHESIS_AFTER_GPT_PRO_CHECKPOINT_FIXED_DEVELOPMENT_CANDIDATE_RETAINED`. One fresh directly verified `GPT-5.6 Sol / Pro` conversation received only commit `07fca57a...C820B`, the exact result/closure/whole-map pins and fixed candidate anchors; prompt and raw response are preserved beside the audit. GPT returned zero ready hypotheses. Independent adjudication corrected its topology assumption—two adjudicated books with US500 as read-only Cross context—and rejected its suggestion that an exact C1 prospective count already exists. C1/C2 still need future-frozen and naturally written evidence; the old tick-flow paths remain unready; Unit 061's slot correction cannot alter five independent materiality failures. This is a scheduler boundary, not a research unit or recursive consultation trigger. No campaign, Proxy, MT5, candidate change or Live authority opens, and the Goal remains active.

`evidence/OPTIMIZATION_ETF_PRIMARY_MARKET_FLOW_SOURCE_CAUSALITY_READINESS_AUDIT_V1.json` closes one post-checkpoint pre-unit source screen as `PRE_UNIT_SOURCE_CAUSALITY_NOT_READY_NO_RESEARCH_UNIT_OPENED_FIXED_DEVELOPMENT_CANDIDATE_RETAINED`. The primary paper's actionable carrier is a one-year rolling residual of daily share flow after ETF order imbalance and close/NAV mispricing, traded cross-sectionally across hundreds of ETFs; it is not raw creation/redemption or premium/discount. Official DIA/SPY sponsor workbooks expose long NAV/shares histories but premium history only from 2025, while QQQ exposes current shares only, NAV without historical shares, and premium history only for 2025 plus completed 2026 quarters. None supplies common point-in-time release timing, the required all-three order-imbalance carrier or an independently identified ETF-to-FPMarkets-CFD transfer. No project outcome was opened, so this is neither an economic failure nor an adjacent rejected experiment. No Lab unit, Optimization campaign, Proxy, MT5 path, GPT checkpoint or Live authority opens; the fixed candidate and active Goal remain unchanged.
