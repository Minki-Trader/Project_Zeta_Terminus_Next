# Lab runtime

`tester-portable/` is generated locally by `lab/tools/Initialize-ZetaNextLabMt5.ps1` and is ignored by Git. It contains its own MT5 executables, required-symbol real-tick cache and only Lab-targeted junctions.
