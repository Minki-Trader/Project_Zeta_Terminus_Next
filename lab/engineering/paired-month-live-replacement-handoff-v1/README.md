# Paired-Month Live Replacement Handoff V1

Closed and verified serial Lab engineering handoff for the fixed Live replacement development target `dd20-paired-month-stability-mt5-v1`.

- User authorization: 2026-08-29 instruction to fix the replacement target as the development candidate and continue autonomously through Lab or Optimization.
- Economic target: exact component multipliers `2 / 1.5 / 2 / 2.5 / 1.5 / 0`, base position risk `0.04`, aggregate risk `0.18`; Passive remains disabled.
- Economics reproduced: the unchanged Lab implementation completed selection at `+$5,786.64 / +$5,477.564`, `1,428` closes and reported `20.26%` native relative equity DD, plus independent forward at `+$32.75 / +$30.628`, `54` closes and `18.66%` DD. The disclosed cent-level deltas from the Optimization anchor occurred on identical tick and decision-path counts and are bounded to MT5 transaction-settlement timing under the new identity.
- Engineering result: the frozen Optimization implementation is now a self-contained Live-capable Lab candidate with unique Lab identity, state, research logs, ownership and dedicated non-Master Portable. MetaEditor build 6140 compiled it at `0 errors / 0 warnings`, and bounded connected create/recovery both remained entries-disabled, flat, zero-risk and zero-fault before normal stops.
- Existing Live logs: RLO1 canonical ledgers remain immutable historical evidence. This candidate writes only to its own Lab namespace. A later Live translation must use a new release namespace while analysis may read both releases by explicit identity.
- Operator contract: `evidence/PAIRED_MONTH_OPERATOR_AND_LOG_CONTRACT_V1.json` freezes the new `0.18` aggregate-risk display, component multipliers, Lab paths and the rule that old and new state are never merged for recovery.
- Connected preset: `mt5/config/live/terminal-pmlrh1-entries-disabled.ini` starts only the dedicated Lab Portable with terminal EA trading disabled and `InpAllowNewEntries=false`; it exists solely for bounded create/recovery evidence.
- Live authority: none. The current RLO1 package remains unchanged. A later translation requires a new frozen Live manifest, stopped-flat old-owner state, entries-disabled recovery and separate final user authorization before any `0/0 → 1/1` activation.

The authoritative declaration is `lab/engineering/paired-month-live-replacement-handoff-v1/evidence/PAIRED_MONTH_LIVE_REPLACEMENT_HANDOFF_DECLARATION_V1.json`.
The closed result is `lab/engineering/paired-month-live-replacement-handoff-v1/evidence/PAIRED_MONTH_LIVE_REPLACEMENT_RESULT_V1.json`.
