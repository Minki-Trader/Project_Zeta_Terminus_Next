# Legacy US500 H4 staged-overlay optimization eligibility V1

This source-free Lab family owns Frontier Unit 098 under Program 1 `entry_signal_market_structure` at meso-to-macro height. It asks only whether one exact lineage-anchored, unfinished legacy staged extension is sufficiently identified to enter a separately owned Optimization information campaign. It does not claim current portfolio economics and adds no MQL, settings, runtime, compile or Tester path.

The exact legacy candidate is `ZT-H4-US500-V2-VOLATILITY-EXP-b4d28831f9`. Its frozen standalone implementation uses completed US500 H4 closes, a 12-bar signed volatility-expansion state, reversion entry at absolute state `1.5`, state/opposite-direction exit at `0.6`, a 30-H4-bar safety lifetime and fixed `0.01` volume. The lineage-pinned native report summary records a complete 2022-08-01 through 2026-08-13 100%-real-tick path at `+$15.36 / +$13.0091` actual/stressed and `6.32%` native equity drawdown, plus a separately reported 2026-06-14 through 2026-08-13 path at `+$1.93 / +$1.8047` and `0.76%` drawdown. Its durable decision was `STAGED_EXTENSION_CANDIDATE_NOT_FINAL`, with explicit remaining work to preserve the identity and gate new entries below project-stage balance `$500`.

The current qualified Optimization anchor first has an observed decision-row balance at or above `$500` on 2025-04-11. Unit 098 therefore retains exactly one isolated information seed: add the unchanged H4 logic as a fixed-lot staged overlay, permit new overlay entries only while account balance is at least `$500`, preserve the overlay's original 20%-of-equity margin check, and leave every six-component anchor path unchanged. The overlay has no initial stop or take-profit in the pinned identity and is intentionally outside the anchor's planned-risk and slot accounting; this destructive divergence is permitted only in isolated Optimization and grants no Lab engineering or Live authority.

The family is closed after eligibility classification. Native shared-account profit, drawdown, overlap, admission and opportunity cost remain unopened and must be decided only by one separately named Optimization campaign with the current complete selection and conditional-forward gates. No parameter, stop, threshold, H4 horizon, activation balance, volume, component exception or nearby legacy candidate may be tuned or substituted.
