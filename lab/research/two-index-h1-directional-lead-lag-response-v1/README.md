# Two-index H1 directional lead-lag response V1

Status: closed `FAIL_H1_DIRECTIONAL_LEAD_LAG_NO_P1_VARIANT_NO_SEED`, source-free
Frontier Unit 103 under Program 1 `entry_signal_market_structure` at meso height.

The qualified Optimization anchor still needs genuinely independent starts,
while the canonical Live-Dev candidate/lifecycle snapshot has not added a new
row since Unit 97 and therefore cannot be retuned. This family asks a distinct
ordered-transmission question on already frozen Lab H1 data: whether a
completed US100 hour predicts the next US30 hour, or a completed US30 hour
predicts the next US100 hour.

The signal is threshold-free. A non-doji completed leader H1 body supplies only
its sign; the follower enters in the same direction at the next H1 open and
exits at the following H1 open, exactly one hour later. Each ordered pair may
have at most one unresolved observation. There is no return magnitude,
normalization, residual, simultaneous divergence, fixed clock, reversion book,
US500 input or portfolio state.

An outcome-free scan of the family-owned physical copies reconstructed `24,440`
common epochs and `7,191` nonoverlapping signals per ordered pair. Every frozen
period supplied `6.94..6.96` signals per normal day and at least `151` signals
on either leader-sign side. P1 alone could select one ordered pair; only a P1
selection could open P2 through P5.

Before declaration and before any economic output, the period-concentration
calculation was corrected to cover all P1-P5 periods rather than P2-P5 alone.
That was a design correction, not a research failure. The final declaration
then pinned all inputs and the corrected script before exactly one valid formal
process.

The process read only ignored physical copies under
`lab/artifacts/raw/two-index-h1-directional-lead-lag-response-v1/input/`. It
created no MQL, SET, runtime, compile, Tester, test, validator, broker query or
Live change.

## Result

US100-to-US30 P1 contained `2,548` signals. Gross/observed/doubled-spread net
was `+$7.4950 / -$39.3505 / -$86.1960`, doubled-spread PF was `0.83358`, and
maximum closed drawdown was `$96.70975`. Its 2022H2/2023 nets were
`-$38.9285 / -$47.2675`; UP/DOWN leader-sign books were
`-$62.91075 / -$23.28525`.

US30-to-US100 also contained `2,548` P1 signals. Gross/observed/doubled-spread
net was `+$0.8356 / -$36.4665 / -$73.7686`, doubled-spread PF was `0.77490`,
and maximum closed drawdown was `$75.55480`. Its 2022H2/2023 nets were
`-$10.6573 / -$63.1113`; UP/DOWN books were `-$49.32975 / -$24.43885`.

Both ordered pairs failed all seven discovery checks. This is complete valid
economic nonconfirmation: gross edge is negligible, observed executable-cost
economics are already negative, doubled spread worsens them, and both calendar
splits and both leader-sign books agree. P2-P5 remained unopened, no MT5 clue
or Optimization seed exists, and the exact pair/signal/clock/horizon bundle
closes without adjacent rescue.
