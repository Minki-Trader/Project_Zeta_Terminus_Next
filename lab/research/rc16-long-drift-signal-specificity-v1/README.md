# RC16 Long-Drift Signal Specificity V1

This frozen source-free Lab family owns closed Program 1 / Unit 070. It asked whether the existing long-only RC16 component earns a material eight-bar response from its compression condition beyond both unconditional same-clock long drift and a positive-recent-direction no-compression control.

The finite bundle reuses the immutable 19,604-row US30 M30 export and exact Unit 063 reconstruction at the native 13:30 clock. Unit 063's signal-only counts and means are prior evidence; the unconditional and positive-direction control outcomes, excess intervals, monthly breadth and adverse-path contrasts remain unopened until the single aggregation.

The authoritative pre-outcome contract is `evidence/RC16_LONG_DRIFT_SIGNAL_SPECIFICITY_DECLARATION_V1.json`, SHA-256 `F38C32FB882CDAF0048CA8B12FA14FCBFAC8EF2D884ADC1BF497FA3510201446`. Return is excluded because the retained cross-index export contains opens rather than the H1 closes needed for its native reversal feature. No MQL, runtime, acquisition, Tester, broker/account query or Live action is added.

## Frozen premetric native-clock topology correction

- The first invocation stopped at the native 13:30 row-count assertion before emitting any new control, excess, interval or monthly outcome. Unit 063's 258 value is its all-slot eligible-day denominator, not a physical native-clock row count.
- Exact bar inspection established 257 physical 13:30 observations in 2025 and 151 in completed-month 2026. The missing native bar is not synthesized; 79/39 signal counts and every formula, control, threshold and verdict remain unchanged.
- Correction receipt SHA-256 is `C94F596A31CBEDCB85A83275098E3CFAA4C9812AE8FF9CCD137DF1798A335E63`. The correction budget is consumed; one successful aggregation and zero further correction or rerun remain.

## Closed result

- Integrity and density passed. Pooled signal days beat unconditional long drift by `+1.33015 ATR`, with normal 95% interval `[+0.08840,+2.57191]`; RC16 therefore cannot be dismissed as pure passive beta in the pooled window.
- The effect was not stable. In 2025 the signal beat unconditional long by only `+0.33724 ATR` and positive-direction no-compression by `+0.06854 ATR`; both intervals included zero, positive-rate improvement failed, and signal MAE was `0.39205 ATR` worse than the direction-matched control.
- Completed-month 2026 was strongly favorable: excess was `+3.21064 ATR` versus unconditional and `+2.89193 ATR` versus the direction-matched control, with positive lower intervals, `+0.23339` positive-rate improvement, `-0.66754 ATR` MAE difference and `5/6` positive valid months.
- Closed `AMBIGUOUS_RC16_SIGNAL_VERSUS_LONG_DRIFT_NO_CANDIDATE`. Preserve RC16 and the pooled non-beta observation, but do not call compression-specific alpha temporally confirmed and retain no threshold, regime, allocation or implementation candidate.
- Result/closure SHA-256 values are `A414216B4249E3B78A2F930451BEF23F8050566A11C1F01023000043698FE2C3` / `07FB756E73E969C8BDC8F10489A096493DB13D6B80D3AD9D9CBC902D41DC2AC7`.
