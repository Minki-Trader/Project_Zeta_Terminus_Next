# Prior broad-dollar portfolio state V1

Status: closed `FAIL_PRIOR_BROAD_DOLLAR_NO_P1_PORTFOLIO_RISK_STATE_NO_SEED`, source-free
Frontier Unit 104 under Program 2 `external_market_event` at meso to macro height.

The qualified paired-month Optimization anchor remains highly profitable but
operates near its pragmatic native equity-drawdown boundary. The canonical
Live-Dev snapshot still has only three complete lifecycles and therefore cannot
support retuning. This family asks one distinct official-data question: whether
a causally prior strengthening of the Federal Reserve nominal broad dollar
index identifies an economically harmful state in which every paired-anchor
birth should be suppressed.

The state is threshold-free. Official Board H.10 daily observations are grouped
by ISO week and the last finite observation is compared with the previous
week's last observation. A positive change is `STRENGTHENING`; a negative
change is `NONSTRENGTHENING`. For a birth on server date D, only the latest
state with reference date no later than D minus seven calendar days is used.
That conservative exclusion respects the official Monday 4:15 p.m.
previous-business-week publication schedule, including the holiday shift.

Two FRED CSV transports produced no bytes and were treated as acquisition
engineering corrections, not research failures. The family then preserved the
official Board daily table and release schedule, a mechanically normalized
1,057-row CSV, and exact copies of the qualified selection and independent
forward lifecycle ledgers. Six immutable inputs total `3,778,009` bytes with
manifest `FAA4C232817CFD02C44F751ECEA2DC7EFEDBAFF21EFA71C0AAF27A7DF78A83F5`.

The outcome-free structural pass reconstructed `221` weekly records and `220`
nonflat weekly states (`111` strengthening, `109` nonstrengthening). All `1,482`
births mapped at a reference age of `7..14` days. P1 contained `516` births,
including `279` strengthening and `237` nonstrengthening births, with both
books, both calendar splits and all five active components represented.

The frozen discovery contract required strengthening births to lose actual and
stressed money, trail nonstrengthening births by at least `0.10R`, have at least
`5` percentage points more stops, lose in both P1 calendar splits and both
books, and lose in at least four of five components. Only a complete `9/9` P1
pass could open P2 through P5.

Exactly one valid source-free formal aggregation reproduced the selection
anchor's `1,428` closes, actual/stressed `+$5,786.63 / +$5,477.524` and `130`
stops, plus the independent forward's `54` closes,
`+$32.74 / +$30.626` and `17` stops. It created no MQL, SET, runtime, compile,
Tester, test, validator, broker/account query, Optimization mutation or Live
change.

## Result

P1 nonstrengthening births earned actual/stressed
`+$162.01 / +$146.900`; strengthening births earned
`+$225.83 / +$207.394`. The strengthening-minus-nonstrengthening mean
stressed-R effect was `+0.021658R`, the opposite of the required `-0.10R` harm.
The stop-rate effect was `+0.038564`, below the required `+0.05` and contradicted
by positive money.

Strengthening stressed net was positive in both discovery splits:
`+$74.673` in 2022H2 and `+$132.721` in 2023. It was also positive in both
books: US30 `+$153.973` and US100 `+$53.421`. All five components were positive:
Cross `+$53.421`, Return `+$17.360`, Pressure `+$11.910`, Range61 `+$43.856`
and Range64 `+$80.847`.

The fixed P1 gate therefore passed `0/9`. This is complete valid economic
nonconfirmation, not a data, environment, design or engineering failure. P2-P5
economic outcomes remained unopened; there is no suppression seed, MT5 clue,
Optimization candidate or Live authority. Broad-dollar level, magnitude,
threshold, alternate lag/window, AFE/EME subindex, symbol, component, event,
risk, sizing and exit rescues close together.

Official sources: [Federal Reserve H.10 daily broad-dollar table](https://www.federalreserve.gov/releases/h10/summary/jrxwtfb_nb.htm),
[Federal Reserve H.10 release schedule](https://www.federalreserve.gov/releases/h10/),
and [FRED DTWEXBGS series reference](https://fred.stlouisfed.org/series/DTWEXBGS).
