# Paired control blocked opportunity value v1

Program 5 / meso Unit 124 asks one bounded economic question found during the independent Optimization stream: when the fixed replacement candidate records an aggregate-risk block, and the exact Live-derived `1 / 1 / 1 / 1 / 1 / 1`, `0.04 / 0.12` control admits the same component, server time, direction, feature and entry price, did that admitted lifecycle have enough direct economic value to retain one later causal admission-displacement question?

The one-time Lab snapshot contains six pinned candidate/control ledgers totaling `41,554,282` bytes. Before any close economics were opened, the structural screen found `211` candidate blocks and `158` complete exact control matches (`74.8815%`): `141/192` in selection, `10/11` in June-July and `7/8` in August. Coverage spans E1-E4, June, July and August; both US30 and US100 books; and Cross, Return and Pressure. All matched entry prices and signal features are exact.

The frozen primary estimator is deliberately narrow. It multiplies each paired control's actual and doubled-cost-stressed net by candidate volume divided by control volume. It therefore measures direct shadow value at the control lifecycle's realized exit geometry. The blocked candidate has no realized protective stop or exit, and this unit does not simulate displaced incumbents, subsequent admissions, margin, open-equity drawdown or a realizable cap change.

The one complete selection aggregation is valid but economically split. The 141 paired control lifecycles themselves sum to actual/stressed `+$33.32 / +$27.757`, PF `1.2004 / 1.1639`, and mean stressed `+0.03015R`. Scaling those same realized exits by the fixed candidate's attempted volumes reverses the sign to `-$66.838 / -$98.577`, PF `0.9405 / 0.9137`, or `-1.2864%` of the candidate's `$7,663.06` attempted risk. This is the central result: average paired opportunity quality is slightly positive, but it is not favorable under the candidate's actual exposure geometry.

The split is broad enough to matter and too inconsistent to authorize action. E1-E3 are positive at `+$21.006 / +$38.618 / +$136.944`, while E4 is `-$295.145`. US30 is `+$67.083`, but US100/Cross is `-$165.66`; Return is `+$72.78` and Pressure is `-$5.697`. Only one of two books and one of three components is positive. Concentration gates pass, but actual/stressed magnitude, PF, both-book and component-breadth gates fail. The result also misses the complete strong-adverse contract, so cap blocking is not established as protective.

Verdict: `AMBIGUOUS_PAIRED_CONTROL_BLOCKED_OPPORTUNITY_VALUE_NO_SEED`. The favorable selection gate failed, so June-July plus August close economics remained unopened. No cap relaxation, admission reorder, priority, weight, component, period or subgroup rescue opens; no retained seed, Optimization candidate, MT5 shortlist or Live authority survives. The fixed paired-month replacement candidate remains unchanged.
