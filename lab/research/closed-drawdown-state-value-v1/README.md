# Closed Drawdown State Value V1

Unit `026` asks whether the causally known portfolio closed-stressed drawdown fraction immediately before a strategy birth materially separates that lifecycle's stressed return and stop-loss incidence. It uses one source-free all-strategy aggregation over `2,233` already-consumed lifecycles and opens no MQL, Tester path or Live surface.

Status: `OPEN_DECLARATION_FROZEN_OUTCOME_UNOPENED`.
