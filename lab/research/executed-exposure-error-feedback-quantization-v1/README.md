# Executed Exposure Error-Feedback Quantization V1

Status: closed complete valid economic-adverse source-free Frontier Unit 117
under Program 5 `portfolio_capital_risk` at meso-to-macro height.

The fixed replacement declares component weights
`2 / 1.5 / 2 / 2.5 / 1.5 / 0`, but its frozen `0.01` lot materialization
independently rounds every requested volume. At ladder level `L=1`, that makes
the executable vector `2 / 2 / 2 / 3 / 2 / 0`. This unit asks one bounded
question: can parameter-free, component-local error feedback make cumulative
executed exposure follow the declared weights more faithfully without giving
up the candidate's profit frontier?

The Control is the exact fixed candidate's independent positive `MathRound`
rule. The sole Treatment keeps every weight, risk fraction, aggregate cap,
daily `$150` ladder, signal birth, source close economy and first-come order,
but carries each admitted component's fractional lot-step residual into its
next admitted birth. Residual starts at zero in each independent tester-like
segment and changes only after an admission. No grid, phase choice, threshold,
component exception, floor/ceiling variant or residual reset search exists.

The formal process reads only one Lab-owned byte copy of the already-consumed
all-ones `0.18` lifecycle universe. This proxy can value newly admitted events
only when that universe observed their close economy; it does not claim to
reconstruct signals absent from the source ledger. A pass can retain exactly
one Optimization information seed for a dedicated one-point non-Master MT5
comparison. It cannot change the fixed candidate, create Live authority or
open an adjacent quantization variant automatically.

One formal process passed all integrity and material-binding checks. Error
feedback changed volume on `456 / 1,417` jointly admitted selection lifecycles,
raised admissions `1,427 → 1,441` and reduced aggregate skips `191 → 177`.
Nevertheless, selection stressed net fell
`$5,237.142726 → $5,078.559464` while raw closed drawdown worsened
`19.423399% → 22.517708%`. Pressure supplied `-$181.425381` of stressed delta
and E4 supplied `-$240.233429`. June-July continuous improved stressed net by
`+$6.61`, but drawdown also worsened by `1.516823pp`; that local interval has no
authority to override the complete-selection result.

Verdict: `VALID_ERROR_FEEDBACK_QUANTIZATION_ECONOMIC_ADVERSE_NO_SEED`. The
fixed replacement remains unchanged, no Optimization or MT5 seed survives,
and the full adjacent rounding/phase/reset/component-exception/base-volume/
lot-step rescue family is closed. The Lab-owned input and raw formal result are
preserved; no MQL, SET, compile, Tester, MT5, Master, broker/account or Live
surface was opened.
