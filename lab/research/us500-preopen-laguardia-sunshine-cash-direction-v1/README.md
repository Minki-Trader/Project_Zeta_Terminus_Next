# US500 pre-open LaGuardia sunshine cash direction

Status: closed valid ambiguous; no seed.

This Program 2 / micro-to-meso Lab detour transferred Hirshleifer and
Shumway's 2003 morning-weather transaction rule to same-day FPMarkets US500
regular-cash economics. The source rule averages 5 a.m.-through-8 a.m. local
sky cover, goes LONG for an average from zero through four inclusive and SHORT
otherwise. No threshold or direction grid was estimated.

Five frozen NOAA NCEI LCDv2 files supply LaGuardia `USW00014732` routine
`FM-15` observations from July 2022 through July 2026. LCDv2 `DATE` is local
standard time, so the parser first attaches fixed UTC-05:00 and then converts to
actual `America/New_York`. It selects one row nearest minute 51 in each actual
local hour 05, 06, 07 and 08, maps clear/FEW/SCT/BKN/OVC to `0/2/4/7/8`, and
requires all four observations without imputation.

The first direct GHCNh attempt revealed a source-environment gap: LaGuardia
routine rows stopped on 2026-04-13, leaving P5 empty. No price or spread had
opened. The provisional weather surface was removed and all years were replaced
by one uniform current LCDv2 series. This was an engineering correction, not a
research failure or mixed-source tail patch.

The outcome-free geometry froze 985 eligible sessions. P1/P2/P3/P4/latest have
`370/247/234/91/43` days, with LONG `132/80/66/25/15`, SHORT
`238/167/168/66/28`, and P1 splits `123/123/124`. Every last weather observation
precedes the 09:30 entry by 36 to 40 minutes. All source, density, timing and
formal-versus-premetric integrity gates pass.

The one complete formal aggregation produced full primary gross / observed /
doubled-spread stressed net `+$1.42925 / -$2.86960 / -$7.16845`. Gross PF is
only `1.01093`; stressed PF is `0.94700`, closed DD `11.49245%` and net/DD
`-0.62375`. Observed and doubled burdens are `$4.29885 / $8.59770`, or
`300.78% / 601.55%` of the tiny positive gross net.

Costs flip the full sign but do not create the rejection. P1, P4 and latest are
already gross-negative at `-$4.57820 / -$5.85340 / -$0.91390`. Full zero-cost
PF and net/DD are only `1.01093 / 0.16206`, and the early/late chronology,
control comparison and monthly breadth remain failed at zero cost.

P1/P2/P3/P4/latest stressed nets are `-$8.26860 / +$3.83925 / +$4.69820 /
-$6.30840 / -$1.12890`. P1's 2022H2 / 2023H1 / 2023H2 sequence is
`+$0.90155 / -$2.90460 / -$6.26555`; only `6/18` P1 months are positive. P2
and P3 form a genuine mid-sample positive island—both sides are positive and
the primary beats unconditional LONG—but P4 reverses and latest loses both
months. That positive island prevents a uniform strong-null label; it does not
support deployment.

The predeclared side decomposition is informative but not promotable. Sunny
LONG earns full stressed `+$2.92030`, PF `1.07351`; cloudy SHORT loses
`-$10.08875`, PF `0.89439`, across 67.72% of days. Yet sunny LONG is negative
in P1 and latest, while cloudy SHORT is positive in P2, P3 and latest. P4 is the
largest break—cloudy SHORT `-$6.52060` while unconditional LONG earns
`+$6.07280`—but latest rotates again, with cloudy SHORT slightly positive and
sunny LONG negative. No single-side suppression has chronological support.

Unconditional LONG earns full stressed `+$1.42485`; the weather primary ends
`$8.59330` below it under identical costs. Prelatest is `-$6.03955`, PF
`0.95250`, net/DD `-0.52552`, and only `21/47` months are positive. The top-five
positive-day share is just `6.02%`, so winner concentration is not the problem;
the failure is broad regime inconsistency. DD remains below nominal/practical
`20.0%/21.2%`, so risk capacity is not binding and scaling cannot repair the
negative edge.

The frozen verdict is
`AMBIGUOUS_LAGUARDIA_MORNING_SUNSHINE_US500_CASH_DIRECTION_NO_SEED`. It is a
complete valid economic result, not an environment, design or engineering
failure. It tests only one post-paper LaGuardia-to-US500 intraday transfer and
does not refute the paper's historical multi-exchange daily evidence.

No information seed, Optimization candidate, MT5 shortlist, Strategy Tester,
MQL, SET, implementation candidate or Live authority survives. Fixed candidate
`dd20-paired-month-stability-mt5-v1` remains unchanged. The station, hour,
cutoff, transform, weather-field, side, horizon, carrier, cost, sizing and
integration neighborhood closes as one bundle. Useful source and raw economic
artifacts remain preserved; Optimization whole-map discovery resumes without a
reporting pause.

Marker:
`FRONTIER_UNIT_114_CLOSED_VALID_AMBIGUOUS_LAGUARDIA_MORNING_SUNSHINE_US500_CASH_DIRECTION_NO_SEED_MAP_RECOMPARED`.
