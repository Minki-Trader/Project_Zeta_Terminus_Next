# Passive Fill-Age Value V1

Unit `024` asks whether the causally known fraction of a Passive order's one-hour pending life consumed before fill materially separates that filled lifecycle's stressed return and stop-loss incidence. The unit uses one source-free aggregation over `594` already-consumed Passive fills and opens no MQL, Tester path or Live surface.

Status: `OPEN_DECLARATION_FROZEN_OUTCOME_UNOPENED`.
