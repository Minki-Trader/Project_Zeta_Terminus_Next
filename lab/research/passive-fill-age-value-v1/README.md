# Passive Fill-Age Value V1

Unit `024` asked whether the causally known fraction of a Passive order's one-hour pending life consumed before fill materially separates that filled lifecycle's stressed return and stop-loss incidence. Its sole fixed source-free aggregation reconstructed all `707` placements, `594` fills, `113` expirations and `594` closed lifecycles with zero integrity fault.

Longer fill age had a negative stressed-R direction in three periods, but pooled Spearman was only `-0.101379276` and LATE-minus-EARLY return only `-0.028945073R`. The stop effect was small and economically incoherent. No alternate age transform, response question, MQL, Tester path or Live surface opened.

Status: `CLOSED_NO_PASSIVE_FILL_AGE_FIELD_PASSED`.
