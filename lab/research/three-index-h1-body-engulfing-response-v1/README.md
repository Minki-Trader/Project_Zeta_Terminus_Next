# Three-index H1 body-engulfing response V1

Status: closed `FAIL_H1_BODY_ENGULFING_NO_P1_DIRECTION_NO_SEED`, source-free
Frontier Unit 102 under Program 1 `entry_signal_market_structure` at
micro-to-meso height.

The Optimization anchor needs a genuinely independent source of entries rather
than another weight, capital-ladder, drawdown-control, component-state or
router adjustment. This family therefore asks whether a strict opposite-colour
H1 body engulfing event represents a causal transfer of directional control
whose following four-hour response is profitable after doubled spread cost.

The signal uses only two completed open-close bodies. It uses no high/low
breakout, compression, volatility threshold, cross-index residual, round-price
lattice, volume, session clock or existing component state. The next H1 open
is the execution proxy and the fifth following H1 open is the exact four-hour
exit. Each symbol may have at most one unresolved observation.

An outcome-free scan of the family-owned copies found `5.12..5.78` signals per
normal day and `3.80..4.60` distinct signal hours per day across US30, US100 and
US500 in every frozen period. That establishes only structural supply. P1 alone
may select continuation or reversion; 2024, 2025 and 2026 January-May are
independent confirmations, while June-July 2026 is a latest-period veto.

The formal process read only the Git-ignored physical copies under
`lab/artifacts/raw/three-index-h1-body-engulfing-response-v1/input/`. It creates
no MQL, SET, runtime, compile, Tester, test, validator, broker query or Live
change. A pass can retain only one Optimization information seed; it cannot
authorize an EA or MT5 run until this unit closes and the complete map is
recompared.

## Result

One valid process reconstructed all `73,332` physical H1 rows and `5,554`
nonoverlapping signals with every immutable, geometry and density check true.
P1 contained `2,023` signals. Continuation earned gross/observed/double-spread
`-$37.53885 / -$62.59460 / -$87.65035`, with double-spread PF `0.82455`.
Reversion improved to gross/observed `+$37.53885 / +$12.48310`, but doubled
spread made it `-$12.57265` at PF `0.97276`.

Continuation was negative in both P1 calendar splits and every symbol.
Reversion was only `+$2.12865` in 2022H2 before losing `-$14.70130` in 2023;
US30 was `+$1.25775`, while US100 and US500 were `-$12.51930 / -$1.31110`.
Its DOWN-signal half was positive `+$36.26730`, but the UP-signal half lost
`-$48.83995`. Neither fixed direction passed any of the seven discovery gates.

This is complete valid economic nonconfirmation, not an environment, design or
engineering failure. P2 through P5 economics remained unopened, no MT5 clue or
Optimization seed exists, and the exact body/clock/horizon/symbol neighborhood
closes without rescue.
