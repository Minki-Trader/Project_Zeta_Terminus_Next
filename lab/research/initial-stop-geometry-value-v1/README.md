# Initial Stop Geometry Value V1

Unit `025` asked whether a strategy's causal initial absolute stop distance divided by entry price materially separates stressed return and stop-loss incidence. Its sole source-free all-strategy aggregation reconstructed all `2,233` already-consumed lifecycles with zero integrity fault.

Every strategy failed both pooled Spearman `|rho| >= 0.20` and tail separation `>= 0.10R`. RC4 and Return were closest on tail return, but wider stops combined lower return with fewer stops and failed continuous/period evidence. No alternate geometry, response question, MQL, Tester path or Live surface opened.

Status: `CLOSED_NO_INITIAL_STOP_GEOMETRY_FIELD_PASSED`.
