# Initial Stop Geometry Value V1

Unit `025` asks whether a strategy's causal initial absolute stop distance divided by entry price materially separates stressed return and stop-loss incidence. It uses one source-free all-strategy aggregation over `2,233` already-consumed lifecycles and opens no MQL, Tester path or Live surface.

Status: `OPEN_DECLARATION_FROZEN_OUTCOME_UNOPENED`.
