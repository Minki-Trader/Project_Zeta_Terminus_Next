# Dual-Portfolio Formula Interaction Spillover Causality V1

Unit 107 is a closed source-free Program 7 / meso-to-macro Lab family. It tested exactly the two formula-interaction clues frozen by the closed dual-portfolio Optimization lattice: `9FD4A8BEEA47022BDF5C` (`RC4 direction_fraction 0.75 + Return centering 1.0`) and `766FECB1566CBDB2A5A6` (`RC4 direction_fraction 0.75 + Pressure NEGATIVE_SHORT`). No third point, grid, neighbor, retune or alternate attribution rule was opened.

The input anchor passed at `5,085 / 5,085` feature rows with maximum absolute error `1.79412e-13`, `20,676` selection events and `941` June-July events. All four declared selection point/contract books reproduced actual net, stressed net and native-equivalent DD within `1e-7`, with exact close counts. One family-scoped Git raw-byte correction occurred before any later outcome was opened; it changed no formula, input, gate or economics.

The E1-E3 discovery uplifts were real rather than merely shared-account artifacts. For point `9FD...`, stressed uplift was `+$146.565 / +$542.024` for Live control / fixed replacement, and changed RC4+Return supplied `+$160.148 / +$511.556` with direct absolute attribution shares `86.05% / 75.00%`. For point `766...`, stressed uplift was `+$133.164 / +$571.017`; changed RC4+Pressure supplied `+$99.053 / +$370.083`, or `68.34% / 63.12%` of absolute attribution.

Neither interaction transferred. Point `9FD...` produced E4 stressed uplift `-$12.696 / -$69.365` and continuous June-July uplift `-$0.494 / -$0.958`; its changed components themselves contributed `-$13.626 / -$28.777` in E4 and `-$0.534 / -$1.068` in the continuous interval. Point `766...` produced E4 stressed uplift `-$28.611 / -$116.784`; the replacement changed components lost `-$93.462`, with another `-$23.322` of unchanged-component spillover. Its continuous replacement uplift was `-$22.432`, split `-$10.694` direct and `-$11.739` spillover. Both exact points failed their fixed primary gates.

The frozen verdict is `VALID_NO_TRANSFERABLE_DIRECT_FORMULA_INTERACTION_NO_SEED`. The selection improvements were genuine but regime-local; later losses are an economic nonconfirmation, not an environment or engineering failure. No Lab seed, Optimization candidate or MT5 shortlist survives. The fixed paired-month development candidate remains unchanged, neither rejected nor promoted. E4 and June-July are formula-unseen conditional intervals only because the portfolio weights had already used them; they are not clean whole-portfolio OOS evidence.

The sole formal process evaluated `6` selection and `24` later economic tasks in `30.8606964` seconds. It opened no MQL, SET, compile, Tester, MT5, runtime, broker/account, Live or Master path. Useful frozen inputs and the raw result remain preserved; reproducible caches were removed.
