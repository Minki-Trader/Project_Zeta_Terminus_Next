# Legacy market-state router optimization eligibility V1

This source-free Lab family owns Frontier Unit 099 under Program 1 `entry_signal_market_structure` at meso-to-macro height. It asks only whether one exact lineage-pinned, causally fitted market-state router is sufficiently identified and distinct to enter one isolated Optimization information campaign on the current paired-month anchor. It adds no Lab MQL, settings, runtime, compile or Tester path and makes no current shared-account economic claim.

The frozen legacy identity is `zt-market-state-router-economic-v1`. It maintains a separate fixed-hold virtual lifecycle for each of the five active high-turnover components, labels each closed virtual lifecycle at fixed `0.01` volume with quote-derived doubled-spread net, and refits one pooled expanding Ridge model at each calendar-quarter boundary. Its ten features are clipped entry signal, absolute signal, direction, weekday sine/cosine and five component indicators. Ridge alpha is exactly `10`; labels are clipped to the past pooled 2nd/98th percentiles; a component-relative predicted-net rank at or above `0.25` permits the real entry; insufficient history admits during causal warm-up. No model, percentile, feature, memory, update, label, hold or component parameter may be tuned.

The lineage-pinned summary and source were read only from legacy commit `4c0899255c701e2c6b53e7f44457c431aef2ad76`. Raw Git blob bytes reproduce the pinned summary at `10,778` bytes / SHA-256 `95E8997639A0748A151A941605D9FE3EF6FE64C52F6803924940D40266709180` and the router source at `35,041` bytes / `C5C9649170CE73571DAD42AE252444272218C356FBBAE9291D47015EB65FF05A`. Its already-consumed native evidence is `+$556.08 / +$437.3044` actual/stressed at `16.50%` equity drawdown over 2022-08-01 through 2026-08-13 and `+$13.58 / +$11.5235` at `11.04%` over the separately reported recent economic window. The durable decision was `VALIDATED_NUMERIC_PASS_ALTERNATIVE_NOT_PROMOTED`, with an explicit prohibition on nearby router tuning.

The current paired-month candidate ledger cannot provide an exact source-free counterfactual. Of `12,265` selection rows, `7,352` are `GATE / EXISTING_EXPOSURE` with the signal deliberately unevaluated. A router-blocked entry changes later component occupancy, so those otherwise hidden signal decisions become reachable and the virtual training sequence, model, rank, admission, shared risk and account path all change. This is an information limitation, not a negative economic result. The exact fixed candidate therefore requires one native MT5 campaign rather than a misleading ledger subtraction or grid.

Unit 099 retains exactly one Optimization seed: preserve the paired-month anchor's five active signals, weights `2 / 1.5 / 2 / 2.5 / 1.5`, `0.04 / 0.18` risk geometry, sizing, stops, exits and account rules; insert only the exact lineage router as a pre-admission binary gate; keep its virtual book and training economics independent from whether a real candidate is subsequently admitted; and give the new campaign its own source, identity, state, output and dedicated non-Master Portable. The sixth zero-weight Passive path stays disabled. Current selection/forward economics, DD, overlap, skipped opportunity and account feedback remain unopened.

The family is closed after eligibility classification. At most one separately named Optimization campaign may implement this exact seed. There is no router grid, adjacent legacy candidate, component exception, threshold/window rescue, source-free profit estimate, automatic Lab engineering successor or Live authority.
