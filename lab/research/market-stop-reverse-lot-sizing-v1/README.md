# Market Stop Reverse-Lot Sizing V1

Unit 062 is a closed invalid Program 5 micro-to-macro research family. It preserved Passive and the native risk ledger and attempted to observe native stop/ATR14 geometry for five market-entry components before any candidate intervention.

The one-time parent copy, final source, native configurations, dedicated Portable and build-6140 EX5 were frozen before the first Tester path. MetaEditor completed at `0 errors / 0 warnings`; one pre-Tester implementation correction replaced an MQL-invalid `switch` with an equivalent `if` mapping.

The sole fit path completed normally and its detailed log contained no generated-tick fallback, but the required HTML report was absent and the frozen complete environment manifest changed during synchronization. The family therefore closed `INVALID_MARKET_STOP_REVERSE_LOT_EXECUTION_OR_RECONSTRUCTION_NO_VERDICT` before any ATR median was read, candidate SET was created or selection/latest path was opened. Native stop/lot is preserved by default without an economic verdict.

Authoritative records are under `evidence/`. Live, broker/account queries and Program 6 are outside this family.
