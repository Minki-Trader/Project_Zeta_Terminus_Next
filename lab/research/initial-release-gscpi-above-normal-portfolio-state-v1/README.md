# Initial-release GSCPI above-normal portfolio state v1

Program 2 / meso→macro Unit 116 is closed complete valid with verdict `NO_MATERIAL_INITIAL_RELEASE_GSCPI_ABOVE_NORMAL_PORTFOLIO_FIELD`.

## Frozen question and causal source

- The only treatment is whether the latest **strictly prior initial-release** New York Fed Global Supply Chain Pressure Index value is above its natural zero baseline. The current revised history, magnitude, percentile, change, persistence, alternate lag and every subgroup are excluded.
- The official `349 × 57` vintage matrix through August 2026 and its configuration are preserved byte-exactly. A release becomes usable only on a later server date; all same-release-date births are excluded.
- Twelve official New York Fed calendar pages from September 2025 through August 2026 agree with the fixed fourth-business-day publication rule. OFR FSI and market CMDI were screened outcome-free but their natural states were too sparse for this role, so neither became a research unit or substitute axis.

## Outcome-free design correction

The six immutable CP2 event files contain `2,233` complete lifecycles. Causal mapping yields `972 ABOVE_NORMAL`, `1,252 AT_OR_BELOW_NORMAL` and `9 UNAVAILABLE` births. P1–P3 have at least 20 births in every period-book-state cell and are confirmatory. P4 has only eight comparator births, including one US30 birth, so it was frozen as descriptive before any lifecycle outcome was opened. This is a design correction, not an economic failure.

## One fixed economic aggregation

- Source integrity reproduced `16,477` events, `2,233` complete lifecycles, `2,027 CLOSE`, `206 EXTERNAL_CLOSE`, `206` stops, actual net `+$444.19` and doubled-cost stressed net `+$407.0477`.
- Confirmatory P1–P3 above-minus-at-or-below effect is raw `+0.008567R / -0.016325 stop` and component-period centered `+0.008765R / +0.009366 stop`.
- Full-horizon descriptive effect is raw `+0.011158R / -0.001887 stop` and centered `+0.004837R / +0.006391 stop`.
- Books split between one nonconcordant and one adverse reading; all three confirmatory periods are nonconcordant. Components split `3` nonconcordant, `2` favorable and `1` adverse. The largest period contribution is `64.12%`; the largest component contribution is `31.01%`.
- Both material-direction gates fail. The frozen strong-null gate passes: `|centered R| < 0.05`, `|centered stop| < 0.025`, no directional period breadth above one and no directional component breadth above three.

Exactly one successful aggregation was performed with zero metric rerun. Two source-acquisition implementation faults were corrected before any source bundle was written, and the P4 density fault was corrected before outcomes opened. They change no source, state, threshold, economic metric or verdict.

## Closure

No retained seed, Lab follow-up, Optimization candidate, MT5 escalation, sizing rule, promotion or Live authority survives. The source level, magnitude, trend, persistence, lag, period, book, component and substitute-series variants close together with no adjacent rescue. Programs 1–5 and 7 were re-compared; no immediate independent successor opens, and the continuous Goal returns to the serial Optimization whole-map boundary.

Artifacts remain under `evidence/`. No MQL source, compile, Tester runtime, broker/account query, Program 6 or Live surface was opened.

Marker: `FRONTIER_UNIT_116_CLOSED_VALID_NO_INITIAL_RELEASE_GSCPI_ABOVE_NORMAL_PORTFOLIO_FIELD_NO_SEED_MAP_RECOMPARED`.
