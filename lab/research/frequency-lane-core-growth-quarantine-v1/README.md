# Frequency-Lane Core-Growth Quarantine V1

This open source-free Lab family owns Program 5 / meso-to-macro Unit 125. It is the first serial rehabilitation unit under the user's explicit authorization to revisit closed Lab and Optimization work one family at a time.

The prior combined native MT5 run found a profitable fixed US500 H4 lane but a much larger loss in the paired high/mid-frequency core after the H4 lane shared the core's realized and stressed growth ledgers. This unit asks only whether the first executable-volume divergence is causally explained by the H4 profit crossing the core's fixed `$150` daily sizing quantum.

The immutable input is a one-time physical Lab copy of the paired control and combined-candidate research ledgers, state events, agent log and three result authorities. The fixed counterfactual leaves every signal and economic parameter unchanged: H4 closes remain in a separate reporting ledger and do not update the core growth variables used for core sizing, while real account equity, margin and safety remain shared.

Outcome-aware triage had already exposed the first divergence and daily-multiplier mechanism before this family was declared. The declaration discloses those observations and freezes one deterministic integrity/reproduction aggregation; it is not a blinded discovery. A pass retains one isolated native MT5 information seed only and still requires a separately frozen native selection and conditional forward. It creates no Optimization candidate, Lab engineering handoff or Live authority.

The first fixed invocation stopped before output because it incorrectly treated `state_sequence` as a unique event-row key. The immutable A/B event ledgers legitimately contain multiple distinct events at one persisted state sequence and contain zero exact duplicate rows. The refrozen reader accepts shared state sequences, rejects exact duplicate full rows and changes no economic role, formula, gate or verdict.
