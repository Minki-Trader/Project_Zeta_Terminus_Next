# Live runtime

`portable/` is generated locally by `live-dev/tools/Initialize-ZetaNextLiveMt5.ps1` and is ignored by Git. Before final handoff it contains no copied legacy account or broker cache and cannot authorize Live entries.
